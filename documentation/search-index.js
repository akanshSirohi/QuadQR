window.QUADQR_SEARCH_INDEX = [{"title":"API Reference","page":"API Reference","url":"api.html","text":"QUADQR DOCUMENTATION API Reference Every option is documented For accepted types, ranges, defaults, recovery cadence, callback fields, and result-object fields, use the Configuration Reference . Function sections below focus on API purpose and examples. Core encoding encodeText(text, options?) Synchronously encodes UTF-8 text. JS Copy import { encodeText } from \"quadqr-js\"; const code = encodeText(\"Hello\", { ecc: \"M\", version: 5, highDensity: true // optional experimental 4-bit cells }); If version is omitted, QuadQR selects the smallest version that fits. highDensity is a boolean. It defaults to false . Set highDensity: true to enable the experimental Triangle16 layout with two RGBW triangles, 16 states, and 4 raw bits per body cell. The protected header remains solid-color in High Density Mode. Image and camera scanners auto-detect it. encodeBytes(bytes, options?) Encodes arbitrary bytes. JS Copy const code = encodeBytes( new Uint8Array([1, 2, 3, 4]), { ecc: \"Q\" } ); Secure encoding encodeSecureText(text, options) Encrypts text using Secure Payload v1 and encodes the encrypted envelope. Password mode: JS Copy const code = await encodeSecureText(\"secret\", { security: { mode: \"password\", password: \"example-password\" } }); Raw-key mode: JS Copy const code = await encodeSecureText(\"secret\", { security: { mode: \"raw-key\", key } }); encodeSecureBytes(bytes, options) Byte-oriented equivalent of encodeSecureText() . decryptDecoded(result, credentials) Unlocks a secure result returned by matrix, image, camera, or Node.js scanning. JS Copy const unlocked = await decryptDecoded(locked, { password: \"example-password\" }); For raw-key mode: JS Copy const unlocked = await decryptDecoded(locked, { key }); Matrix decoding decodeMatrix(matrix, options?) Decodes an already sampled QuadQR matrix. JS Copy const result = decodeMatrix(code.matrix); console.log(result.text); A secure result remains locked until it is decrypted: JS Copy { secure: true, requiresDecryption: true, text: null, payload: Uint8Array(...), security: { mode: \"password\", algorithm: \"AES-256-GCM\" } } Rendering renderToCanvas(codeOrMatrix, canvas, options?) Renders to an HTML canvas. JS Copy renderToCanvas(code, canvas, { imageSize: 720, quietZone: 4, style: \"classic\", logo: { source: loadedImage, size: 0.12, clearBackground: true, padding: 0.65, radius: 0.8 } }); imageSize sets the exact square output width/height in pixels. The default is 720 when neither imageSize nor moduleSize is provided. moduleSize remains supported as a lower-level pixels-per-module sizing option and is used when imageSize is omitted. quietZone is measured in modules. The default is 4 . Logo options: source : loaded CanvasImageSource for canvas rendering, ImageData-like RGBA data for renderToImageData() , or URL/data URL for SVG. size : fraction of the matrix width/height, clamped to 0.05..0.30 . Default 0.18 . clearBackground : clears modules behind the logo using a solid background before drawing the logo. padding : clear-background padding in modules. Default 0.65 . radius : clear-background corner radius in modules. Default 0.8 . backgroundColor : background color when clearing. Defaults to palette white. Supported styles: classic depth soft inset renderToImageData(codeOrMatrix, options?) Returns runtime-neutral RGBA pixels: JS Copy { width, height, data: Uint8ClampedArray } renderToSVG(codeOrMatrix, options?) Returns a standalone SVG string. It supports the same exact imageSize , palette, render style, quiet-zone, and logo geometry options as the browser canvas renderer. Because the preview/export is vector, it stays sharp when displayed above or below its nominal pixel size. JS Copy const svg = renderToSVG(code, { imageSize: 720, quietZone: 6, style: \"soft\", logo: { source: \"data:image/png;base64,...\", size: 0.12, clearBackground: true } }); Image and camera scanning scanImageData(imageData, options?) Scans an ImageData-like RGBA object. The scanner automatically attempts both normal RGBW center sampling and Triangle16 dual-region sampling when geometry is found. When normal projective geometry is locatable but too coarse for half-cell Triangle16 regions, one bounded precise-alignment recovery pass refines the alignment center at sub-module resolution. Clean frames use the normal observed-RGB path first. Difficult frames then get bounded fallback attempts using per-channel white balancing, spatial black/white normalization, tighter centre sampling, QuadQR module-grid Auto Tone / Auto Contrast / Auto Color recovery, a rectified QR-region pixel enhancement pass, and sub-module geometry micro-refinement before the scan is rejected. Dense versions also use their distributed alignment markers to refine a noisy four-point projective solution when the initial alignment-grid score is plausible but imperfect. If exactly two strong finder patterns survive a steep angle, a bounded perspective-tolerant third-finder recovery pass is attempted before heavier color recovery. JS Copy const result = scanImageData({ width, height, data }); Useful scanner options include sampleRadius , robustSampleRadius , adaptiveSampling , spatialColorNormalization , autoEnhanceRecovery , rectifiedAutoEnhanceRecovery , rectifiedRecoveryModuleSize , geometryRefinement , alignmentRefinement , structureTolerance , maxErasureConfidence , softDecoding , softDecodeConfidence , softDecodeMaxCells , and softDecodePairCells . Spectrum ECC 2.0 soft decoding is bounded and only runs after ordinary hard/error-erasure decoding fails. Browser scanFile(file, options?) Available from quadqr-js/browser and the browser-compatible main entry. Accepts a browser File or Blob . JS Copy const result = await scanFile(file); scanVideoFrame(video, options?) Scans the current frame from an HTML video element. With the default videoCropMode: \"visible\" , an object-fit: cover preview is mapped back to the matching source crop before scanning, so hidden sensor pixels do not shrink the code or skew enhancement statistics. JS Copy const result = scanVideoFrame(video); startCameraScanner(video, options?) Starts live camera scanning. On supported browsers, continuous decoding uses a dual-worker engine: a fast fresh-frame worker runs normal finder/geometry/decode attempts while an independent full-recovery worker retains the complete perspective, color-recovery, high-resolution, ECC, damaged-code, and multi-frame scanner stack. A slow recovery on an older frame therefore cannot prevent the fast worker from inspecting newer video frames. Finder detection remains JavaScript; optional WASM accelerates grayscale/binary preprocessing and CRC. Worker mode uses a 33 ms minimum cadence by default; automatic main-thread fallback uses 80 ms. The default camera request is around 1280×720, and the CSS-visible preview region is cropped/resized to a 640 px bitmap before worker transfer. Finder acquisition uses a streaming 1:1:3:1:1 RGB-value pass, direct cross-checks, local-threshold fallback, and directional module-size/version estimation. Once a geometry candidate validates structure, ECC, and CRC, QuadQR returns it immediately. Format v6 distributed 5×5 nested alignment eyes remain available for perspective refinement. After a fast miss, strong finder evidence triggers parallel recovery quickly. A single finder must persist across consecutive fresh frames before recovery is armed, and zero-finder frames stay on the lightweight detector only. The recovery worker can use up to 960 px and keeps QuadQR Auto Color, precise alignment, perspective recovery, affine color calibration, QR-region enhancement, multi-frame confidence fusion, erasure decoding, and soft-decision Spectrum ECC. JS Copy const scanner = await startCameraScanner(video, { maxDimension: 640, scanInterval: 33, multiFrame: true, multiFrameWindow: 4, multiFrameMinFrames: 2, softDecoding: true, cameraAutoColorEvery: 1, cameraAutoColorCropInsets: [0.08, 0.16, 0.22, 0], cameraAutoColorHighlightPercentile: 0.95, cameraAutoColorOutputHighlight: 190, cameraAutoColorAnalysisInset: 0.10, cameraAutoEnhanceEvery: 2, cameraFinderRecoveryEvery: 2, cameraHighResolutionRecovery: true, cameraHighResolutionMaxDimension: 960, cameraHighResolutionEvery: 2, cameraRecoveryStrongFinderInterval: 120, cameraRecoveryWeakFinderInterval: 260, cameraRecoveryWeakFinderFrames: 2, onResult(result) { console.log(result); }, onScanMiss() { // Keep searching. }, onDiagnostic(event) { // Finder positions are in the scanner frame coordinate space. // Use scanWidth/scanHeight to map them onto a video overlay. if (event.type === \"frame\") { console.log(event.method, event.finderCount, event.geometry?.version, event.elapsedMs); } } }); scanner.stop(); onDiagnostic is opt-in. It reports finder candidates, the locator method ( rgb-value-otsu , threshold-bracket recovery, or luminance fallback), geometry data, and recovery state. Diagnostics reuse the scanner's existing passes and do not add a separate finder scan. rectifyDetectedCode(imageData, options?) Returns the perspective-corrected detected symbol image when detection succeeds. rotateMatrix(matrix, quarterTurns?) Rotates a matrix by 90-degree quarter turns. Node.js helpers Import from quadqr-js/node . toPNG(codeOrMatrix, options?) Returns a Node.js Buffer containing a PNG. savePNG(codeOrMatrix, filename, options?) Writes a PNG file. JS Copy await savePNG(code, \"quadqr.png\", { imageSize: 720, quietZone: 4 }); toSVG(codeOrMatrix, options?) Returns the standalone SVG string from the Node entry point. saveSVG(codeOrMatrix, filename, options?) Writes SVG directly to disk. JS Copy await saveSVG(code, \"quadqr.svg\", { imageSize: 720, quietZone: 4 }); scanBuffer(buffer, options?) Scans an image buffer. PNG is supported without dependencies. Other formats can use the host application's optional sharp installation. Node scanFile(filename, options?) Reads and scans an image file. decodePNG(buffer) Decodes supported PNG data into RGBA pixels. encodePNG(imageData, options?) Encodes RGBA pixels into a PNG Buffer . Security utilities generateRaw256Key() Generates a cryptographically random 32-byte key. normalizeRaw256Key(key) Normalizes a 32-byte key supplied as bytes, ArrayBuffer, or 64-character hexadecimal text. bytesToHex(bytes) Converts bytes to lowercase hexadecimal text. Security constants include: SECURITY_MODES SECURITY_ALGORITHMS SECURE_PAYLOAD_VERSION DEFAULT_PBKDF2_ITERATIONS WASM initWasm(options?) Loads and activates the optional bundled WASM accelerator for CRC-32 and scanner pixel preprocessing: RGBA grayscale conversion, Otsu thresholding, and binary finder generation. JS Copy import { initWasm } from \"quadqr-js\"; await initWasm(); An explicit URL or byte buffer can also be supplied. Initialize WASM once before performance-sensitive synchronous image scans or camera scanning. getWasmState() Returns the active WASM state or null . disableWasm() Returns the codec to the JavaScript implementation. Version and capacity getVersionInfo(version, options?) Returns geometry and payload information for a specific QuadQR version/ECC profile. CRC utilities crc32(bytes) Calculates QuadQR's CRC-32 value for a byte array. installCrc32Accelerator(accelerator?) Installs or removes a synchronous CRC-32 accelerator. Most applications should use initWasm() rather than calling this directly. Constants Common exported constants include: FORMAT_VERSION MIN_VERSION MAX_VERSION DEFAULT_ECC_LEVEL CELL DEFAULT_PALETTE RENDER_STYLES RENDER_MODES COMPRESSION_MODES SIGNATURE_ALGORITHMS STRESS_PROFILES ECC_LEVELS Benchmark entry Benchmark helpers are available from quadqr-js/benchmark : JS Copy import { buildCapacityComparison, benchmarkCodec, calculateCapacityPlan } from \"quadqr-js/benchmark\"; calculateCapacityPlan(options) Estimates the smallest QuadQR version for a byte count or concrete payload and reports remaining capacity plus the standard QR byte-mode version at the same nominal ECC letter. When only a byte count is known, compression gain is intentionally not guessed. Compression encodeText(text, options?) / encodeBytes(input, options?) Both normal encoding APIs accept an optional compression mode: JS Copy const code = QuadQR.encodeText(\"hello hello hello\", { compression: \"auto\", ecc: \"M\" }); compression may be none , auto , smart , brotli , deflate , or lz . none stores the payload directly. auto is the fast balanced mode: one pass each of LZ level 6, DEFLATE level 6, and Brotli quality 6, followed by a complete stored-size comparison including envelope overhead. smart is the CPU-heavy mode. It starts with the Auto pass, checks the resulting QuadQR version, and only escalates to DEFLATE 8 / Brotli 9 and then DEFLATE 9 / Brotli 11 when a smaller physical version is realistically reachable. brotli always stores the payload using QuadQR's bundled synchronous Brotli codec. deflate always stores the payload using QuadQR's synchronous pure-JavaScript raw DEFLATE codec. lz always stores the payload through the original portable LZSS-style compressor for backward compatibility. Explicit LZ, DEFLATE, and Brotli support compressionLevel : JS Copy encodeText(text, { compression: \"lz\", compressionLevel: 9 }); // 1..9, default 6 encodeText(text, { compression: \"deflate\", compressionLevel: 9 }); // 1..9, default 6 encodeText(text, { compression: \"brotli\", compressionLevel: 11 }); // 0..11, default 11 lzLevel , deflateLevel , and brotliQuality are accepted as algorithm-specific aliases. Levels affect encoder effort only and are not serialized because the decoder does not need them. Generated code objects expose compressionLevel , compressionStrategy , and Smart-mode diagnostics; scanned symbols only need the stored compression algorithm ID. There is no public content-type registry. Text remains text and byte arrays remain byte arrays. compressPayload(input, options?) / decompressPayload(input, expectedLength?) Legacy portable synchronous LZSS-style compression helpers. options.level accepts integers 1..9 and defaults to 6. Higher levels walk deeper candidate history and may use bounded lazy matching; level 6 preserves the historical QuadQR search depth. The LZ wire format and decoder are unchanged at every level. compressDeflatePayload(input, options?) / decompressDeflatePayload(input, expectedLength?) Raw-DEFLATE helpers. options.level accepts integers 1..9 and defaults to 6. Higher levels spend more CPU on deeper LZ77 candidate search and lazy matching. The compressor emits standard RFC 1951 fixed-Huffman blocks with a 32 KiB match window and matches up to 258 bytes. The implementation is pure JavaScript and synchronous, so the same core works in browsers, Node.js servers, workers, and other JavaScript runtimes without node:zlib , CompressionStream , DOM APIs, or native dependencies. compressBrotliPayload(input, options?) / decompressBrotliPayload(input, expectedLength?) Bundled synchronous Brotli helpers. compressBrotliPayload() accepts an optional { quality } setting from 0 through 11 for direct codec use. The generated stream is standard Brotli and the decoder accepts standard Brotli streams. QuadQR does not require node:zlib , browser CompressionStream , DOM APIs, a native addon, or a runtime dependency for this path. Binary convenience APIs encodeUint8Array(input, options?) Explicit byte-oriented alias of encodeBytes() . It supports the same optional compression setting. decodeUint8Array(matrix, options?) Decodes a matrix and returns only the application payload bytes. Signed QuadQR generateSigningKeyPair() Generates an extractable Ed25519 key pair using Web Crypto and returns the CryptoKeys, raw public-key bytes, PKCS#8 private-key bytes, and a compact SHA-256-derived keyId . encodeSignedText(text, options) / encodeSignedBytes(input, options) Signs the normal application payload with Ed25519. Any required signature and compression metadata is handled internally. JS Copy const pair = await QuadQR.generateSigningKeyPair(); const code = await QuadQR.encodeSignedText(\"certificate payload\", { ecc: \"Q\", compression: \"auto\", privateKey: pair.privateKey, keyId: pair.keyId }); Only the private key is required for signing. publicKey is accepted only for the explicit embedPublicKey: true compatibility mode. verifyDecodedSignature(result, options?) Verifies a signed decode result against a trusted external Ed25519 public key. JS Copy const decoded = QuadQR.decodeMatrix(code.matrix); const verified = await QuadQR.verifyDecodedSignature(decoded, { publicKey: knownPublicKey }); For multiple issuers, pass a trustedKeys object or Map keyed by the embedded keyId : JS Copy const verified = await QuadQR.verifyDecodedSignature(decoded, { trustedKeys: { [decoded.signingKeyId]: knownPublicKey } }); signatureVerified: true means the signature is mathematically valid. signatureTrusted: true means it was checked using an external trusted key. allowEmbeddedKey: true may be used for compatibility/self-contained integrity checks, but such a result is not considered a trusted signer. Signing + encryption encodeSecureText() and encodeSecureBytes() accept an optional signing object alongside security and compression . The internal pipeline is compression → signing → AES-256-GCM → Spectrum ECC. JS Copy const code = await QuadQR.encodeSecureText(\"private signed payload\", { compression: \"auto\", security: { mode: \"password\", password: \"secret\" }, signing: { privateKey: pair.privateKey, keyId: pair.keyId } }); Print rendering All render APIs accept: JS Copy { mode: \"screen\" | \"print\" } Print mode uses a darker print-safe palette and forces Classic modules by default. quietZone has the same behavior in print and screen modes, including support for values below the recommended four-module default. getPrintGuidance(codeOrMatrix, options?) Returns physical module size, pixels/module at a DPI, recommended minimum physical size, and print recommendations. Automatic logo safety A logo can use: JS Copy logo: { source: logo, size: \"auto\", clearBackground: true } estimateSafeLogoSize() exposes the conservative ratio used by auto mode. findMaxSafeLogoSize() performs an empirical binary search when the logo source is ImageData-like. Scanner confidence and debug mode Normal successful scans now include: confidence geometryConfidence calibrationConfidence structureConfidence eccUtilization correctedErrors diagnostics Use scanImageData(image, { debug: true }) for detailed geometry/sampling information. debugScanImageData() is a non-throwing helper that returns { ok, result, debug } or { ok: false, error, debug } . Scanability and stress testing applyStressDistortion(imageData, type, severity?, options?) Deterministically applies a selected synthetic distortion. In addition to the original blur/exposure/shadow/resampling profiles, perspective-3d accepts pitchDegrees , yawDegrees , and rollDegrees for camera-angle testing. runImageStressTest(imageData, expected?, options?) Runs the standard torture profiles and returns a 0–100 score plus per-scenario decode results. assessScanability(code, renderOptions?, options?) Renders a code and runs the standard test suite. It returns Excellent , Good , Risky , or Likely unscannable together with recommendations. runReliabilityLab(imageData, expected?, options?) Runs the broader Reliability Lab suite. options.suite can be quick , full , or extreme . Results include overall score, per-scenario CRC verification, category scores, confidence, RS corrections, and timing. runPerspectiveSweep(imageData, expected?, options?) Sweeps yaw , pitch , or roll /Z rotation across a list of angles and reports the largest tested angle that decoded successfully. Synthetic scores are regression aids and do not replace physical camera/print testing. See Reliability Lab . Signing key identifiers deriveSigningKeyId(publicKey, bytes?) Derives a lowercase hexadecimal key ID from SHA-256 of the raw Ed25519 public key. bytes defaults to 8 and is clamped to 4..16 ; the returned string therefore contains 8..32 hex characters. Complete public export catalog This catalog is intentionally exhaustive for the supported package entry points. Option objects referenced here are defined field-by-field in the Configuration Reference . Browser scanner tuning is also duplicated on Scanning & Camera . Core functions Export / signature Returns Purpose compressPayload(input, options?) Uint8Array Legacy LZ compressor. Use options.level 1..9; default 6. decompressPayload(input, expectedLength?) Uint8Array Decompresses data produced by compressPayload . Optional expected length is an integrity/shape check. compressDeflatePayload(input, options?) Uint8Array Dependency-free raw fixed-Huffman DEFLATE compressor. options.level is 1..9, default 6. decompressDeflatePayload(input, expectedLength?) Uint8Array Inflates QuadQR raw-DEFLATE payload bytes. compressBrotliPayload(input, options?) Uint8Array Pure-JavaScript Brotli compressor. options.quality is 0..11, explicit default 11. decompressBrotliPayload(input, expectedLength?) Uint8Array Decompresses Brotli payload bytes created by QuadQR. deriveSigningKeyId(publicKey, bytes?) Promise<string> SHA-256-derived lowercase hex identifier for an Ed25519 public key; bytes defaults to 8 and is clamped 4..16. generateSigningKeyPair() Promise<CryptoKeyPair> Generates a WebCrypto Ed25519 signing/verification key pair. installCrc32Accelerator(accelerator?) previous accelerator or null Installs/removes an optional CRC32 accelerator. Passing null restores the built-in JavaScript implementation. crc32(bytes) unsigned 32-bit integer Computes the CRC-32 value used by QuadQR payload framing. getVersionInfo(version, options?) object Returns matrix size, capacity, data-cell count, ECC plan, and High Density information for one version. encodeText(text, options?) encoded code object Encodes UTF-8 text into a QuadQR matrix. Encoding, ECC, version, compression, and High Density options are documented in Configuration Reference. encodeBytes(input, options?) encoded code object Encodes arbitrary bytes into a QuadQR matrix. encodeUint8Array(input, options?) encoded code object Strict Uint8Array convenience alias around byte encoding. encodeSignedBytes(input, options) Promise<encoded code object> Creates an Ed25519-signed binary payload envelope, with optional internal compression. encodeSignedText(text, options) Promise<encoded code object> Text counterpart of encodeSignedBytes . encodeSecureText(text, options) Promise<encoded code object> Encrypts text using Secure Payload v1 password or raw-key mode, then encodes the protected bytes. encodeSecureBytes(input, options) Promise<encoded code object> Binary counterpart of encodeSecureText . decryptDecoded(result, security?) Promise<decode result> Decrypts a locked secure decode result and restores text/binary, compression, and signing metadata. decodeMatrix(matrix, options?) decode result Decodes an already sampled QuadQR cell matrix, including Spectrum ECC error/erasure and optional soft decoding. decodeUint8Array(matrix, options?) Uint8Array Convenience API returning only decoded application bytes. verifyDecodedSignature(result, options?) Promise<decode result> Verifies an Ed25519 signature using a trusted key, trusted-key map, or optionally an embedded public key. estimateSafeLogoSize(codeOrMatrix, options?) number ratio Fast ECC-budget estimate for a conservative centered logo size. findMaxSafeLogoSize(code, options?) object Renders/tests candidate clear-background logo sizes and binary-searches for the largest size that still self-decodes. getPrintGuidance(codeOrMatrix, options?) object Returns recommended quiet zone, physical dimensions, module size, and DPI guidance for printing. renderToCanvas(codeOrMatrix, canvas, options?) canvas Renders to an existing browser canvas and returns that canvas. renderToImageData(codeOrMatrix, options?) ImageData-like object Renders to RGBA pixel data, usable in browser or Node workflows. renderToSVG(codeOrMatrix, options?) string Returns standalone SVG markup. applyStressDistortion(imageData, type, severity?, options?) ImageData-like object Applies one deterministic Reliability Lab distortion such as blur, perspective, shadow, color cast, noise, glare, or downscale. runImageStressTest(imageData, expected?, options?) report object Runs the compact legacy stress profile set against one source image. runReliabilityLab(imageData, expected?, options?) report object Runs quick/full/extreme real-world distortion suites and returns per-case plus aggregate reliability results. runPerspectiveSweep(imageData, expected?, options?) report object Sweeps yaw, pitch, or roll angles and reports decode success/confidence by angle. assessScanability(code, renderOptions?, options?) report object Renders an encoded code and evaluates scanability across configured stress profiles. debugScanImageData(imageData, options?) diagnostic object Runs scanner analysis for debugging and returns finder/geometry/pass information rather than only throwing on failure. scanImageData(imageData, options?) decode result Main synchronous image scanner with geometry detection, calibration, recovery, Spectrum ECC, and optional diagnostics. scanFile(file, options?) Promise<decode result> Browser File / Blob image scanner. Node has a filename-based override in quadqr-js/node . scanVideoFrame(video, options?) decode result Captures one visible video frame/ROI and runs the image scanner. startCameraScanner(video, options?) Promise<{ stream, stop, scanNow, video }> Starts progressive live-camera scanning. Recovery cadence, multi-frame fusion, crop, and callback fields are fully defined on Scanning & Camera. rectifyDetectedCode(imageData, options?) { geometry, imageData } Finds the best symbol geometry and returns a perspective-rectified square image; moduleSize defaults to 8. rotateMatrix(matrix, quarterTurns?) 2D cell matrix Returns a copied matrix rotated clockwise by 90° increments; negative values are normalized modulo four. Core constants Export Value / type Meaning FORMAT_VERSION 5 Current QuadQR matrix-format version written/read by this release. MIN_VERSION / MAX_VERSION 1 / 40 Supported matrix version bounds. DEFAULT_ECC_LEVEL \"M\" Default Spectrum ECC profile for library encoding. ECC_LEVELS frozen object Nominal L/M/Q/H definitions with IDs and Reed-Solomon parity/correctable-symbol counts. CELL frozen object Cell numeric values: BLACK=-1, RED=0, GREEN=1, BLUE=2, WHITE=3. CELL_ENCODINGS frozen object Physical encoding identifiers rgbw and experimental triangle16 . DEFAULT_PALETTE frozen object Screen RGBW/black hex colors used when no palette override is provided. PRINT_PALETTE frozen object Darker print-safe RGB primaries plus true black/white. RENDER_STYLES frozen object classic , depth , soft , and inset style names. RENDER_MODES frozen object screen and print renderer modes. COMPRESSION_MODES frozen object none , auto , smart , lz , deflate , and brotli identifiers. LZ_LEVEL_MIN / LZ_LEVEL_MAX / DEFAULT_LZ_LEVEL 1 / 9 / 6 Supported/default legacy LZ search level. COMPRESSION_LEVELS frozen object Programmatic level metadata for LZ, DEFLATE, Brotli, Auto, and Smart compression stages. SIGNATURE_ALGORITHMS frozen object Currently exposes ED25519: \"Ed25519\" . SECURITY_MODES frozen object Secure Payload credential mode identifiers: password and raw-key. SECURITY_ALGORITHMS frozen object Algorithm identifiers used by Secure Payload v1, including AES-256-GCM and PBKDF2-HMAC-SHA-256. SECURE_PAYLOAD_VERSION integer Current encrypted secure-envelope format version. DEFAULT_PBKDF2_ITERATIONS 600000 Default password KDF work factor. STRESS_PROFILES frozen array Compact predefined scanability stress cases used by runImageStressTest / assessScanability . RELIABILITY_PROFILES frozen array Full categorized quick/full/extreme Reliability Lab profile definitions. internals frozen object, unsupported Implementation/testing helpers. Exported for project diagnostics, not a stable application API; do not build production integrations against these members. Node entry point: quadqr-js/node Export / signature Returns Purpose decodePNG(input) ImageData-like object Dependency-free decoder for supported non-interlaced PNG buffers. encodePNG(imageData, options?) Uint8Array Dependency-free PNG encoder; compressionLevel is 0..9, default 9, and affects PNG file size only. toPNG(codeOrMatrix, options?) Uint8Array Renders and PNG-encodes a code/matrix in memory. savePNG(codeOrMatrix, filename, options?) Promise<void> Writes rendered PNG bytes to disk. toSVG(codeOrMatrix, options?) string Node convenience wrapper returning SVG markup. saveSVG(codeOrMatrix, filename, options?) Promise<void> Writes SVG markup to disk. scanBuffer(input, options?) Promise<decode result> Scans encoded image bytes. PNG works dependency-free; other formats can use host-installed Sharp. scanFile(filename, options?) Promise<decode result> Reads and scans an image path in Node. initWasm(options?) Promise<state> Loads the optional CRC32 WebAssembly accelerator from a URL or supplied bytes. getWasmState() state object Reports whether WASM is loaded/active and any relevant state. disableWasm() state object Disables the installed WASM accelerator and restores JavaScript CRC32. Benchmark entry point: quadqr-js/benchmark Export / signature Returns Purpose STANDARD_QR_BYTE_CAPACITY frozen object Reference ISO QR byte-mode capacities for versions 1..40 at L/M/Q/H, used only for same-size comparison. QuadQR ECC letters are not recovery-equivalent. getStandardQrByteCapacity(version, ecc?) integer bytes Returns the reference standard-QR byte capacity for one version and nominal ECC letter. compareCapacity(version, ecc?, options?) comparison object Compares one QuadQR version with same-size standard QR byte capacity, including bytes, ratio, gain percent, and matrix-cell efficiency. calculateCapacityPlan(options?) capacity-plan object Finds the smallest QuadQR/standard-QR versions for a concrete payload or byte count and can account for compression/signing overhead. buildCapacityComparison(options?) array of comparison rows Builds capacity-comparison rows across a requested version range. benchmarkCodec(options?) benchmark object Measures encode/decode/compression timing for configured payload sizes, iterations, ECC, and High Density settings. benchmarkReport(options?) Promise<report object> Runs capacity and codec benchmarks and returns one combined machine-readable report. Previous ← Reliability Lab Next Configuration Reference → QuadQR Experimental RGBW matrix symbology. AGPL-3.0 licensed Continuous mode: continuous true keeps the camera active and resets scanner freshness between symbols. duplicateCooldown defaults to 1200 ms in continuous mode and suppresses the same CRC result without extra image work. pauseWhenHidden defaults to true. The returned scanner exposes pause() and resume(). signal accepts an AbortSignal for cancellation. onCameraState reports running, paused, ended, and stopped states. Main-thread fallback recovery is candidate-gated by finder or geometry evidence instead of miss count alone."},{"title":"Core encoding","page":"API Reference","url":"api.html#core-encoding","text":"Core encoding encodeText(text, options?) # Synchronously encodes UTF-8 text. JS Copy import { encodeText } from \"quadqr-js\"; const code = encodeText(\"Hello\", { ecc: \"M\", version: 5, highDensity: true // optional experimental 4-bit cells }); If version is omitted, QuadQR selects the smallest version that fits. highDensity is a boolean. It defaults to false . Set highDensity: true to enable the experimental Triangle16 layout with two RGBW triangles, 16 states, and 4 raw bits per body cell. The protected header remains solid-color in High Density Mode. Image and camera scanners auto-detect it. encodeBytes(bytes, options?) # Encodes arbitrary bytes. JS Copy const code = encodeBytes( new Uint8Array([1, 2, 3, 4]), { ecc: \"Q\" } );"},{"title":"encodeText(text, options?)","page":"API Reference","url":"api.html#encodetexttext-options","text":"encodeText(text, options?) Synchronously encodes UTF-8 text. JS Copy import { encodeText } from \"quadqr-js\"; const code = encodeText(\"Hello\", { ecc: \"M\", version: 5, highDensity: true // optional experimental 4-bit cells }); If version is omitted, QuadQR selects the smallest version that fits. highDensity is a boolean. It defaults to false . Set highDensity: true to enable the experimental Triangle16 layout with two RGBW triangles, 16 states, and 4 raw bits per body cell. The protected header remains solid-color in High Density Mode. Image and camera scanners auto-detect it."},{"title":"encodeBytes(bytes, options?)","page":"API Reference","url":"api.html#encodebytesbytes-options","text":"encodeBytes(bytes, options?) Encodes arbitrary bytes. JS Copy const code = encodeBytes( new Uint8Array([1, 2, 3, 4]), { ecc: \"Q\" } );"},{"title":"Secure encoding","page":"API Reference","url":"api.html#secure-encoding","text":"Secure encoding encodeSecureText(text, options) # Encrypts text using Secure Payload v1 and encodes the encrypted envelope. Password mode: JS Copy const code = await encodeSecureText(\"secret\", { security: { mode: \"password\", password: \"example-password\" } }); Raw-key mode: JS Copy const code = await encodeSecureText(\"secret\", { security: { mode: \"raw-key\", key } }); encodeSecureBytes(bytes, options) # Byte-oriented equivalent of encodeSecureText() . decryptDecoded(result, credentials) # Unlocks a secure result returned by matrix, image, camera, or Node.js scanning. JS Copy const unlocked = await decryptDecoded(locked, { password: \"example-password\" }); For raw-key mode: JS Copy const unlocked = await decryptDecoded(locked, { key });"},{"title":"encodeSecureText(text, options)","page":"API Reference","url":"api.html#encodesecuretexttext-options","text":"encodeSecureText(text, options) Encrypts text using Secure Payload v1 and encodes the encrypted envelope. Password mode: JS Copy const code = await encodeSecureText(\"secret\", { security: { mode: \"password\", password: \"example-password\" } }); Raw-key mode: JS Copy const code = await encodeSecureText(\"secret\", { security: { mode: \"raw-key\", key } });"},{"title":"encodeSecureBytes(bytes, options)","page":"API Reference","url":"api.html#encodesecurebytesbytes-options","text":"encodeSecureBytes(bytes, options) Byte-oriented equivalent of encodeSecureText() ."},{"title":"decryptDecoded(result, credentials)","page":"API Reference","url":"api.html#decryptdecodedresult-credentials","text":"decryptDecoded(result, credentials) Unlocks a secure result returned by matrix, image, camera, or Node.js scanning. JS Copy const unlocked = await decryptDecoded(locked, { password: \"example-password\" }); For raw-key mode: JS Copy const unlocked = await decryptDecoded(locked, { key });"},{"title":"Matrix decoding","page":"API Reference","url":"api.html#matrix-decoding","text":"Matrix decoding decodeMatrix(matrix, options?) # Decodes an already sampled QuadQR matrix. JS Copy const result = decodeMatrix(code.matrix); console.log(result.text); A secure result remains locked until it is decrypted: JS Copy { secure: true, requiresDecryption: true, text: null, payload: Uint8Array(...), security: { mode: \"password\", algorithm: \"AES-256-GCM\" } }"},{"title":"decodeMatrix(matrix, options?)","page":"API Reference","url":"api.html#decodematrixmatrix-options","text":"decodeMatrix(matrix, options?) Decodes an already sampled QuadQR matrix. JS Copy const result = decodeMatrix(code.matrix); console.log(result.text); A secure result remains locked until it is decrypted: JS Copy { secure: true, requiresDecryption: true, text: null, payload: Uint8Array(...), security: { mode: \"password\", algorithm: \"AES-256-GCM\" } }"},{"title":"Rendering","page":"API Reference","url":"api.html#rendering","text":"Rendering renderToCanvas(codeOrMatrix, canvas, options?) # Renders to an HTML canvas. JS Copy renderToCanvas(code, canvas, { imageSize: 720, quietZone: 4, style: \"classic\", logo: { source: loadedImage, size: 0.12, clearBackground: true, padding: 0.65, radius: 0.8 } }); imageSize sets the exact square output width/height in pixels. The default is 720 when neither imageSize nor moduleSize is provided. moduleSize remains supported as a lower-level pixels-per-module sizing option and is used when imageSize is omitted. quietZone is measured in modules. The default is 4 . Logo options: source : loaded CanvasImageSource for canvas rendering, ImageData-like RGBA data for renderToImageData() , or URL/data URL for SVG. size : fraction of the matrix width/height, clamped to 0.05..0.30 . Default 0.18 . clearBackground : clears modules behind the logo using a solid background before drawing the logo. padding : clear-background padding in modules. Default 0.65 . radius : clear-background corner radius in modules. Default 0.8 . backgroundColor : background color when clearing. Defaults to palette white. Supported styles: classic depth soft inset renderToImageData(codeOrMatrix, options?) # Returns runtime-neutral RGBA pixels: JS Copy { width, height, data: Uint8ClampedArray } renderToSVG(codeOrMatrix, options?) # Returns a standalone SVG string. It supports the same exact imageSize , palette, render style, quiet-zone, and logo geometry options as the browser canvas renderer. Because the preview/export is vector, it stays sharp when displayed above or below its nominal pixel size. JS Copy const svg = renderToSVG(code, { imageSize: 720, quietZone: 6, style: \"soft\", logo: { source: \"data:image/png;base64,...\", size: 0.12, clearBackground: true } });"},{"title":"renderToCanvas(codeOrMatrix, canvas, options?)","page":"API Reference","url":"api.html#rendertocanvascodeormatrix-canvas-options","text":"renderToCanvas(codeOrMatrix, canvas, options?) Renders to an HTML canvas. JS Copy renderToCanvas(code, canvas, { imageSize: 720, quietZone: 4, style: \"classic\", logo: { source: loadedImage, size: 0.12, clearBackground: true, padding: 0.65, radius: 0.8 } }); imageSize sets the exact square output width/height in pixels. The default is 720 when neither imageSize nor moduleSize is provided. moduleSize remains supported as a lower-level pixels-per-module sizing option and is used when imageSize is omitted. quietZone is measured in modules. The default is 4 . Logo options: source : loaded CanvasImageSource for canvas rendering, ImageData-like RGBA data for renderToImageData() , or URL/data URL for SVG. size : fraction of the matrix width/height, clamped to 0.05..0.30 . Default 0.18 . clearBackground : clears modules behind the logo using a solid background before drawing the logo. padding : clear-background padding in modules. Default 0.65 . radius : clear-background corner radius in modules. Default 0.8 . backgroundColor : background color when clearing. Defaults to palette white. Supported styles: classic depth soft inset"},{"title":"renderToImageData(codeOrMatrix, options?)","page":"API Reference","url":"api.html#rendertoimagedatacodeormatrix-options","text":"renderToImageData(codeOrMatrix, options?) Returns runtime-neutral RGBA pixels: JS Copy { width, height, data: Uint8ClampedArray }"},{"title":"renderToSVG(codeOrMatrix, options?)","page":"API Reference","url":"api.html#rendertosvgcodeormatrix-options","text":"renderToSVG(codeOrMatrix, options?) Returns a standalone SVG string. It supports the same exact imageSize , palette, render style, quiet-zone, and logo geometry options as the browser canvas renderer. Because the preview/export is vector, it stays sharp when displayed above or below its nominal pixel size. JS Copy const svg = renderToSVG(code, { imageSize: 720, quietZone: 6, style: \"soft\", logo: { source: \"data:image/png;base64,...\", size: 0.12, clearBackground: true } });"},{"title":"Image and camera scanning","page":"API Reference","url":"api.html#image-and-camera-scanning","text":"Image and camera scanning scanImageData(imageData, options?) # Scans an ImageData-like RGBA object. The scanner automatically attempts both normal RGBW center sampling and Triangle16 dual-region sampling when geometry is found. When normal projective geometry is locatable but too coarse for half-cell Triangle16 regions, one bounded precise-alignment recovery pass refines the alignment center at sub-module resolution. Clean frames use the normal observed-RGB path first. Difficult frames then get bounded fallback attempts using per-channel white balancing, spatial black/white normalization, tighter centre sampling, QuadQR module-grid Auto Tone / Auto Contrast / Auto Color recovery, a rectified QR-region pixel enhancement pass, and sub-module geometry micro-refinement before the scan is rejected. Dense versions also use their distributed alignment markers to refine a noisy four-point projective solution when the initial alignment-grid score is plausible but imperfect. If exactly two strong finder patterns survive a steep angle, a bounded perspective-tolerant third-finder recovery pass is attempted before heavier color recovery. JS Copy const result = scanImageData({ width, height, data }); Useful scanner options include sampleRadius , robustSampleRadius , adaptiveSampling , spatialColorNormalization , autoEnhanceRecovery , rectifiedAutoEnhanceRecovery , rectifiedRecoveryModuleSize , geometryRefinement , alignmentRefinement , structureTolerance , maxErasureConfidence , softDecoding , softDecodeConfidence , softDecodeMaxCells , and softDecodePairCells . Spectrum ECC 2.0 soft decoding is bounded and only runs after ordinary hard/error-erasure decoding fails. Browser scanFile(file, options?) # Available from quadqr-js/browser and the browser-compatible main entry. Accepts a browser File or Blob . JS Copy const result = await scanFile(file); scanVideoFrame(video, options?) # Scans the current frame from an HTML video element. With the default videoCropMode: \"visible\" , an object-fit: cover preview is mapped back to the matching source crop before scanning, so hidden sensor pixels do not shrink the code or skew enhancement statistics. JS Copy const result = scanVideoFrame(video); startCameraScanner(video, options?) # Starts live camera scanning. On supported browsers, continuous decoding uses a dual-worker engine: a fast fresh-frame worker runs normal finder/geometry/decode attempts while an independent full-recovery worker retains the complete perspective, color-recovery, high-resolution, ECC, damaged-code, and multi-frame scanner stack. A slow recovery on an older frame therefore cannot prevent the fast worker from inspecting newer video frames. Finder detection remains JavaScript; optional WASM accelerates grayscale/binary preprocessing and CRC. Worker mode uses a 33 ms minimum cadence by default; automatic main-thread fallback uses 80 ms. The default camera request is around 1280×720, and the CSS-visible preview region is cropped/resized to a 640 px bitmap before worker transfer. Finder acquisition uses a streaming 1:1:3:1:1 RGB-value pass, direct cross-checks, local-threshold fallback, and directional module-size/version estimation. Once a geometry candidate validates structure, ECC, and CRC, QuadQR returns it immediately. Format v6 distributed 5×5 nested alignment eyes remain available for perspective refinement. After a fast miss, strong finder evidence triggers parallel recovery quickly. A single finder must persist across consecutive fresh frames before recovery is armed, and zero-finder frames stay on the lightweight detector only. The recovery worker can use up to 960 px and keeps QuadQR Auto Color, precise alignment, perspective recovery, affine color calibration, QR-region enhancement, multi-frame confidence fusion, erasure decoding, and soft-decision Spectrum ECC. JS Copy const scanner = await startCameraScanner(video, { maxDimension: 640, scanInterval: 33, multiFrame: true, multiFrameWindow: 4, multiFrameMinFrames: 2, softDecoding: true, cameraAutoColorEvery: 1, cameraAutoColorCropInsets: [0.08, 0.16, 0.22, 0], cameraAutoColorHighlightPercentile: 0.95, cameraAutoColorOutputHighlight: 190, cameraAutoColorAnalysisInset: 0.10, cameraAutoEnhanceEvery: 2, cameraFinderRecoveryEvery: 2, cameraHighResolutionRecovery: true, cameraHighResolutionMaxDimension: 960, cameraHighResolutionEvery: 2, cameraRecoveryStrongFinderInterval: 120, cameraRecoveryWeakFinderInterval: 260, cameraRecoveryWeakFinderFrames: 2, onResult(result) { console.log(result); }, onScanMiss() { // Keep searching. }, onDiagnostic(event) { // Finder positions are in the scanner frame coordinate space. // Use scanWidth/scanHeight to map them onto a video overlay. if (event.type === \"frame\") { console.log(event.method, event.finderCount, event.geometry?.version, event.elapsedMs); } } }); scanner.stop(); onDiagnostic is opt-in. It reports finder candidates, the locator method ( rgb-value-otsu , threshold-bracket recovery, or luminance fallback), geometry data, and recovery state. Diagnostics reuse the scanner's existing passes and do not add a separate finder scan. rectifyDetectedCode(imageData, options?) # Returns the perspective-corrected detected symbol image when detection succeeds. rotateMatrix(matrix, quarterTurns?) # Rotates a matrix by 90-degree quarter turns."},{"title":"scanImageData(imageData, options?)","page":"API Reference","url":"api.html#scanimagedataimagedata-options","text":"scanImageData(imageData, options?) Scans an ImageData-like RGBA object. The scanner automatically attempts both normal RGBW center sampling and Triangle16 dual-region sampling when geometry is found. When normal projective geometry is locatable but too coarse for half-cell Triangle16 regions, one bounded precise-alignment recovery pass refines the alignment center at sub-module resolution. Clean frames use the normal observed-RGB path first. Difficult frames then get bounded fallback attempts using per-channel white balancing, spatial black/white normalization, tighter centre sampling, QuadQR module-grid Auto Tone / Auto Contrast / Auto Color recovery, a rectified QR-region pixel enhancement pass, and sub-module geometry micro-refinement before the scan is rejected. Dense versions also use their distributed alignment markers to refine a noisy four-point projective solution when the initial alignment-grid score is plausible but imperfect. If exactly two strong finder patterns survive a steep angle, a bounded perspective-tolerant third-finder recovery pass is attempted before heavier color recovery. JS Copy const result = scanImageData({ width, height, data }); Useful scanner options include sampleRadius , robustSampleRadius , adaptiveSampling , spatialColorNormalization , autoEnhanceRecovery , rectifiedAutoEnhanceRecovery , rectifiedRecoveryModuleSize , geometryRefinement , alignmentRefinement , structureTolerance , maxErasureConfidence , softDecoding , softDecodeConfidence , softDecodeMaxCells , and softDecodePairCells . Spectrum ECC 2.0 soft decoding is bounded and only runs after ordinary hard/error-erasure decoding fails."},{"title":"Browser scanFile(file, options?)","page":"API Reference","url":"api.html#browser-scanfilefile-options","text":"Browser scanFile(file, options?) Available from quadqr-js/browser and the browser-compatible main entry. Accepts a browser File or Blob . JS Copy const result = await scanFile(file);"},{"title":"scanVideoFrame(video, options?)","page":"API Reference","url":"api.html#scanvideoframevideo-options","text":"scanVideoFrame(video, options?) Scans the current frame from an HTML video element. With the default videoCropMode: \"visible\" , an object-fit: cover preview is mapped back to the matching source crop before scanning, so hidden sensor pixels do not shrink the code or skew enhancement statistics. JS Copy const result = scanVideoFrame(video);"},{"title":"startCameraScanner(video, options?)","page":"API Reference","url":"api.html#startcamerascannervideo-options","text":"startCameraScanner(video, options?) Starts live camera scanning. On supported browsers, continuous decoding uses a dual-worker engine: a fast fresh-frame worker runs normal finder/geometry/decode attempts while an independent full-recovery worker retains the complete perspective, color-recovery, high-resolution, ECC, damaged-code, and multi-frame scanner stack. A slow recovery on an older frame therefore cannot prevent the fast worker from inspecting newer video frames. Finder detection remains JavaScript; optional WASM accelerates grayscale/binary preprocessing and CRC. Worker mode uses a 33 ms minimum cadence by default; automatic main-thread fallback uses 80 ms. The default camera request is around 1280×720, and the CSS-visible preview region is cropped/resized to a 640 px bitmap before worker transfer. Finder acquisition uses a streaming 1:1:3:1:1 RGB-value pass, direct cross-checks, local-threshold fallback, and directional module-size/version estimation. Once a geometry candidate validates structure, ECC, and CRC, QuadQR returns it immediately. Format v6 distributed 5×5 nested alignment eyes remain available for perspective refinement. After a fast miss, strong finder evidence triggers parallel recovery quickly. A single finder must persist across consecutive fresh frames before recovery is armed, and zero-finder frames stay on the lightweight detector only. The recovery worker can use up to 960 px and keeps QuadQR Auto Color, precise alignment, perspective recovery, affine color calibration, QR-region enhancement, multi-frame confidence fusion, erasure decoding, and soft-decision Spectrum ECC. JS Copy const scanner = await startCameraScanner(video, { maxDimension: 640, scanInterval: 33, multiFrame: true, multiFrameWindow: 4, multiFrameMinFrames: 2, softDecoding: true, cameraAutoColorEvery: 1, cameraAutoColorCropInsets: [0.08, 0.16, 0.22, 0], cameraAutoColorHighlightPercentile: 0.95, cameraAutoColorOutputHighlight: 190, cameraAutoColorAnalysisInset: 0.10, cameraAutoEnhanceEvery: 2, cameraFinderRecoveryEvery: 2, cameraHighResolutionRecovery: true, cameraHighResolutionMaxDimension: 960, cameraHighResolutionEvery: 2, cameraRecoveryStrongFinderInterval: 120, cameraRecoveryWeakFinderInterval: 260, cameraRecoveryWeakFinderFrames: 2, onResult(result) { console.log(result); }, onScanMiss() { // Keep searching. }, onDiagnostic(event) { // Finder positions are in the scanner frame coordinate space. // Use scanWidth/scanHeight to map them onto a video overlay. if (event.type === \"frame\") { console.log(event.method, event.finderCount, event.geometry?.version, event.elapsedMs); } } }); scanner.stop(); onDiagnostic is opt-in. It reports finder candidates, the locator method ( rgb-value-otsu , threshold-bracket recovery, or luminance fallback), geometry data, and recovery state. Diagnostics reuse the scanner's existing passes and do not add a separate finder scan."},{"title":"rectifyDetectedCode(imageData, options?)","page":"API Reference","url":"api.html#rectifydetectedcodeimagedata-options","text":"rectifyDetectedCode(imageData, options?) Returns the perspective-corrected detected symbol image when detection succeeds."},{"title":"rotateMatrix(matrix, quarterTurns?)","page":"API Reference","url":"api.html#rotatematrixmatrix-quarterturns","text":"rotateMatrix(matrix, quarterTurns?) Rotates a matrix by 90-degree quarter turns."},{"title":"Node.js helpers","page":"API Reference","url":"api.html#nodejs-helpers","text":"Node.js helpers Import from quadqr-js/node . toPNG(codeOrMatrix, options?) # Returns a Node.js Buffer containing a PNG. savePNG(codeOrMatrix, filename, options?) # Writes a PNG file. JS Copy await savePNG(code, \"quadqr.png\", { imageSize: 720, quietZone: 4 }); toSVG(codeOrMatrix, options?) # Returns the standalone SVG string from the Node entry point. saveSVG(codeOrMatrix, filename, options?) # Writes SVG directly to disk. JS Copy await saveSVG(code, \"quadqr.svg\", { imageSize: 720, quietZone: 4 }); scanBuffer(buffer, options?) # Scans an image buffer. PNG is supported without dependencies. Other formats can use the host application's optional sharp installation. Node scanFile(filename, options?) # Reads and scans an image file. decodePNG(buffer) # Decodes supported PNG data into RGBA pixels. encodePNG(imageData, options?) # Encodes RGBA pixels into a PNG Buffer ."},{"title":"toPNG(codeOrMatrix, options?)","page":"API Reference","url":"api.html#topngcodeormatrix-options","text":"toPNG(codeOrMatrix, options?) Returns a Node.js Buffer containing a PNG."},{"title":"savePNG(codeOrMatrix, filename, options?)","page":"API Reference","url":"api.html#savepngcodeormatrix-filename-options","text":"savePNG(codeOrMatrix, filename, options?) Writes a PNG file. JS Copy await savePNG(code, \"quadqr.png\", { imageSize: 720, quietZone: 4 });"},{"title":"toSVG(codeOrMatrix, options?)","page":"API Reference","url":"api.html#tosvgcodeormatrix-options","text":"toSVG(codeOrMatrix, options?) Returns the standalone SVG string from the Node entry point."},{"title":"saveSVG(codeOrMatrix, filename, options?)","page":"API Reference","url":"api.html#savesvgcodeormatrix-filename-options","text":"saveSVG(codeOrMatrix, filename, options?) Writes SVG directly to disk. JS Copy await saveSVG(code, \"quadqr.svg\", { imageSize: 720, quietZone: 4 });"},{"title":"scanBuffer(buffer, options?)","page":"API Reference","url":"api.html#scanbufferbuffer-options","text":"scanBuffer(buffer, options?) Scans an image buffer. PNG is supported without dependencies. Other formats can use the host application's optional sharp installation."},{"title":"Node scanFile(filename, options?)","page":"API Reference","url":"api.html#node-scanfilefilename-options","text":"Node scanFile(filename, options?) Reads and scans an image file."},{"title":"decodePNG(buffer)","page":"API Reference","url":"api.html#decodepngbuffer","text":"decodePNG(buffer) Decodes supported PNG data into RGBA pixels."},{"title":"encodePNG(imageData, options?)","page":"API Reference","url":"api.html#encodepngimagedata-options","text":"encodePNG(imageData, options?) Encodes RGBA pixels into a PNG Buffer ."},{"title":"Security utilities","page":"API Reference","url":"api.html#security-utilities","text":"Security utilities generateRaw256Key() # Generates a cryptographically random 32-byte key. normalizeRaw256Key(key) # Normalizes a 32-byte key supplied as bytes, ArrayBuffer, or 64-character hexadecimal text. bytesToHex(bytes) # Converts bytes to lowercase hexadecimal text. Security constants include: SECURITY_MODES SECURITY_ALGORITHMS SECURE_PAYLOAD_VERSION DEFAULT_PBKDF2_ITERATIONS"},{"title":"generateRaw256Key()","page":"API Reference","url":"api.html#generateraw256key","text":"generateRaw256Key() Generates a cryptographically random 32-byte key."},{"title":"normalizeRaw256Key(key)","page":"API Reference","url":"api.html#normalizeraw256keykey","text":"normalizeRaw256Key(key) Normalizes a 32-byte key supplied as bytes, ArrayBuffer, or 64-character hexadecimal text."},{"title":"bytesToHex(bytes)","page":"API Reference","url":"api.html#bytestohexbytes","text":"bytesToHex(bytes) Converts bytes to lowercase hexadecimal text. Security constants include: SECURITY_MODES SECURITY_ALGORITHMS SECURE_PAYLOAD_VERSION DEFAULT_PBKDF2_ITERATIONS"},{"title":"WASM","page":"API Reference","url":"api.html#wasm","text":"WASM initWasm(options?) # Loads and activates the optional bundled WASM accelerator for CRC-32 and scanner pixel preprocessing: RGBA grayscale conversion, Otsu thresholding, and binary finder generation. JS Copy import { initWasm } from \"quadqr-js\"; await initWasm(); An explicit URL or byte buffer can also be supplied. Initialize WASM once before performance-sensitive synchronous image scans or camera scanning. getWasmState() # Returns the active WASM state or null . disableWasm() # Returns the codec to the JavaScript implementation."},{"title":"initWasm(options?)","page":"API Reference","url":"api.html#initwasmoptions","text":"initWasm(options?) Loads and activates the optional bundled WASM accelerator for CRC-32 and scanner pixel preprocessing: RGBA grayscale conversion, Otsu thresholding, and binary finder generation. JS Copy import { initWasm } from \"quadqr-js\"; await initWasm(); An explicit URL or byte buffer can also be supplied. Initialize WASM once before performance-sensitive synchronous image scans or camera scanning."},{"title":"getWasmState()","page":"API Reference","url":"api.html#getwasmstate","text":"getWasmState() Returns the active WASM state or null ."},{"title":"disableWasm()","page":"API Reference","url":"api.html#disablewasm","text":"disableWasm() Returns the codec to the JavaScript implementation."},{"title":"Version and capacity","page":"API Reference","url":"api.html#version-and-capacity","text":"Version and capacity getVersionInfo(version, options?) # Returns geometry and payload information for a specific QuadQR version/ECC profile."},{"title":"getVersionInfo(version, options?)","page":"API Reference","url":"api.html#getversioninfoversion-options","text":"getVersionInfo(version, options?) Returns geometry and payload information for a specific QuadQR version/ECC profile."},{"title":"CRC utilities","page":"API Reference","url":"api.html#crc-utilities","text":"CRC utilities crc32(bytes) # Calculates QuadQR's CRC-32 value for a byte array. installCrc32Accelerator(accelerator?) # Installs or removes a synchronous CRC-32 accelerator. Most applications should use initWasm() rather than calling this directly."},{"title":"crc32(bytes)","page":"API Reference","url":"api.html#crc32bytes","text":"crc32(bytes) Calculates QuadQR's CRC-32 value for a byte array."},{"title":"installCrc32Accelerator(accelerator?)","page":"API Reference","url":"api.html#installcrc32acceleratoraccelerator","text":"installCrc32Accelerator(accelerator?) Installs or removes a synchronous CRC-32 accelerator. Most applications should use initWasm() rather than calling this directly."},{"title":"Constants","page":"API Reference","url":"api.html#constants","text":"Constants Common exported constants include: FORMAT_VERSION MIN_VERSION MAX_VERSION DEFAULT_ECC_LEVEL CELL DEFAULT_PALETTE RENDER_STYLES RENDER_MODES COMPRESSION_MODES SIGNATURE_ALGORITHMS STRESS_PROFILES ECC_LEVELS"},{"title":"Benchmark entry","page":"API Reference","url":"api.html#benchmark-entry","text":"Benchmark entry Benchmark helpers are available from quadqr-js/benchmark : JS Copy import { buildCapacityComparison, benchmarkCodec, calculateCapacityPlan } from \"quadqr-js/benchmark\"; calculateCapacityPlan(options) # Estimates the smallest QuadQR version for a byte count or concrete payload and reports remaining capacity plus the standard QR byte-mode version at the same nominal ECC letter. When only a byte count is known, compression gain is intentionally not guessed."},{"title":"calculateCapacityPlan(options)","page":"API Reference","url":"api.html#calculatecapacityplanoptions","text":"calculateCapacityPlan(options) Estimates the smallest QuadQR version for a byte count or concrete payload and reports remaining capacity plus the standard QR byte-mode version at the same nominal ECC letter. When only a byte count is known, compression gain is intentionally not guessed."},{"title":"Compression","page":"API Reference","url":"api.html#compression","text":"Compression encodeText(text, options?) / encodeBytes(input, options?) # Both normal encoding APIs accept an optional compression mode: JS Copy const code = QuadQR.encodeText(\"hello hello hello\", { compression: \"auto\", ecc: \"M\" }); compression may be none , auto , smart , brotli , deflate , or lz . none stores the payload directly. auto is the fast balanced mode: one pass each of LZ level 6, DEFLATE level 6, and Brotli quality 6, followed by a complete stored-size comparison including envelope overhead. smart is the CPU-heavy mode. It starts with the Auto pass, checks the resulting QuadQR version, and only escalates to DEFLATE 8 / Brotli 9 and then DEFLATE 9 / Brotli 11 when a smaller physical version is realistically reachable. brotli always stores the payload using QuadQR's bundled synchronous Brotli codec. deflate always stores the payload using QuadQR's synchronous pure-JavaScript raw DEFLATE codec. lz always stores the payload through the original portable LZSS-style compressor for backward compatibility. Explicit LZ, DEFLATE, and Brotli support compressionLevel : JS Copy encodeText(text, { compression: \"lz\", compressionLevel: 9 }); // 1..9, default 6 encodeText(text, { compression: \"deflate\", compressionLevel: 9 }); // 1..9, default 6 encodeText(text, { compression: \"brotli\", compressionLevel: 11 }); // 0..11, default 11 lzLevel , deflateLevel , and brotliQuality are accepted as algorithm-specific aliases. Levels affect encoder effort only and are not serialized because the decoder does not need them. Generated code objects expose compressionLevel , compressionStrategy , and Smart-mode diagnostics; scanned symbols only need the stored compression algorithm ID. There is no public content-type registry. Text remains text and byte arrays remain byte arrays. compressPayload(input, options?) / decompressPayload(input, expectedLength?) # Legacy portable synchronous LZSS-style compression helpers. options.level accepts integers 1..9 and defaults to 6. Higher levels walk deeper candidate history and may use bounded lazy matching; level 6 preserves the historical QuadQR search depth. The LZ wire format and decoder are unchanged at every level. compressDeflatePayload(input, options?) / decompressDeflatePayload(input, expectedLength?) # Raw-DEFLATE helpers. options.level accepts integers 1..9 and defaults to 6. Higher levels spend more CPU on deeper LZ77 candidate search and lazy matching. The compressor emits standard RFC 1951 fixed-Huffman blocks with a 32 KiB match window and matches up to 258 bytes. The implementation is pure JavaScript and synchronous, so the same core works in browsers, Node.js servers, workers, and other JavaScript runtimes without node:zlib , CompressionStream , DOM APIs, or native dependencies. compressBrotliPayload(input, options?) / decompressBrotliPayload(input, expectedLength?) # Bundled synchronous Brotli helpers. compressBrotliPayload() accepts an optional { quality } setting from 0 through 11 for direct codec use. The generated stream is standard Brotli and the decoder accepts standard Brotli streams. QuadQR does not require node:zlib , browser CompressionStream , DOM APIs, a native addon, or a runtime dependency for this path."},{"title":"encodeText(text, options?) / encodeBytes(input, options?)","page":"API Reference","url":"api.html#encodetexttext-options-encodebytesinput-options","text":"encodeText(text, options?) / encodeBytes(input, options?) Both normal encoding APIs accept an optional compression mode: JS Copy const code = QuadQR.encodeText(\"hello hello hello\", { compression: \"auto\", ecc: \"M\" }); compression may be none , auto , smart , brotli , deflate , or lz . none stores the payload directly. auto is the fast balanced mode: one pass each of LZ level 6, DEFLATE level 6, and Brotli quality 6, followed by a complete stored-size comparison including envelope overhead. smart is the CPU-heavy mode. It starts with the Auto pass, checks the resulting QuadQR version, and only escalates to DEFLATE 8 / Brotli 9 and then DEFLATE 9 / Brotli 11 when a smaller physical version is realistically reachable. brotli always stores the payload using QuadQR's bundled synchronous Brotli codec. deflate always stores the payload using QuadQR's synchronous pure-JavaScript raw DEFLATE codec. lz always stores the payload through the original portable LZSS-style compressor for backward compatibility. Explicit LZ, DEFLATE, and Brotli support compressionLevel : JS Copy encodeText(text, { compression: \"lz\", compressionLevel: 9 }); // 1..9, default 6 encodeText(text, { compression: \"deflate\", compressionLevel: 9 }); // 1..9, default 6 encodeText(text, { compression: \"brotli\", compressionLevel: 11 }); // 0..11, default 11 lzLevel , deflateLevel , and brotliQuality are accepted as algorithm-specific aliases. Levels affect encoder effort only and are not serialized because the decoder does not need them. Generated code objects expose compressionLevel , compressionStrategy , and Smart-mode diagnostics; scanned symbols only need the stored compression algorithm ID. There is no public content-type registry. Text remains text and byte arrays remain byte arrays."},{"title":"compressPayload(input, options?) / decompressPayload(input, expectedLength?)","page":"API Reference","url":"api.html#compresspayloadinput-options-decompresspayloadinput-expectedlength","text":"compressPayload(input, options?) / decompressPayload(input, expectedLength?) Legacy portable synchronous LZSS-style compression helpers. options.level accepts integers 1..9 and defaults to 6. Higher levels walk deeper candidate history and may use bounded lazy matching; level 6 preserves the historical QuadQR search depth. The LZ wire format and decoder are unchanged at every level."},{"title":"compressDeflatePayload(input, options?) / decompressDeflatePayload(input, expectedLength?)","page":"API Reference","url":"api.html#compressdeflatepayloadinput-options-decompressdeflatepayloadinput-expectedlength","text":"compressDeflatePayload(input, options?) / decompressDeflatePayload(input, expectedLength?) Raw-DEFLATE helpers. options.level accepts integers 1..9 and defaults to 6. Higher levels spend more CPU on deeper LZ77 candidate search and lazy matching. The compressor emits standard RFC 1951 fixed-Huffman blocks with a 32 KiB match window and matches up to 258 bytes. The implementation is pure JavaScript and synchronous, so the same core works in browsers, Node.js servers, workers, and other JavaScript runtimes without node:zlib , CompressionStream , DOM APIs, or native dependencies."},{"title":"compressBrotliPayload(input, options?) / decompressBrotliPayload(input, expectedLength?)","page":"API Reference","url":"api.html#compressbrotlipayloadinput-options-decompressbrotlipayloadinput-expectedlength","text":"compressBrotliPayload(input, options?) / decompressBrotliPayload(input, expectedLength?) Bundled synchronous Brotli helpers. compressBrotliPayload() accepts an optional { quality } setting from 0 through 11 for direct codec use. The generated stream is standard Brotli and the decoder accepts standard Brotli streams. QuadQR does not require node:zlib , browser CompressionStream , DOM APIs, a native addon, or a runtime dependency for this path."},{"title":"Binary convenience APIs","page":"API Reference","url":"api.html#binary-convenience-apis","text":"Binary convenience APIs encodeUint8Array(input, options?) # Explicit byte-oriented alias of encodeBytes() . It supports the same optional compression setting. decodeUint8Array(matrix, options?) # Decodes a matrix and returns only the application payload bytes."},{"title":"encodeUint8Array(input, options?)","page":"API Reference","url":"api.html#encodeuint8arrayinput-options","text":"encodeUint8Array(input, options?) Explicit byte-oriented alias of encodeBytes() . It supports the same optional compression setting."},{"title":"decodeUint8Array(matrix, options?)","page":"API Reference","url":"api.html#decodeuint8arraymatrix-options","text":"decodeUint8Array(matrix, options?) Decodes a matrix and returns only the application payload bytes."},{"title":"Signed QuadQR","page":"API Reference","url":"api.html#signed-quadqr","text":"Signed QuadQR generateSigningKeyPair() # Generates an extractable Ed25519 key pair using Web Crypto and returns the CryptoKeys, raw public-key bytes, PKCS#8 private-key bytes, and a compact SHA-256-derived keyId . encodeSignedText(text, options) / encodeSignedBytes(input, options) # Signs the normal application payload with Ed25519. Any required signature and compression metadata is handled internally. JS Copy const pair = await QuadQR.generateSigningKeyPair(); const code = await QuadQR.encodeSignedText(\"certificate payload\", { ecc: \"Q\", compression: \"auto\", privateKey: pair.privateKey, keyId: pair.keyId }); Only the private key is required for signing. publicKey is accepted only for the explicit embedPublicKey: true compatibility mode. verifyDecodedSignature(result, options?) # Verifies a signed decode result against a trusted external Ed25519 public key. JS Copy const decoded = QuadQR.decodeMatrix(code.matrix); const verified = await QuadQR.verifyDecodedSignature(decoded, { publicKey: knownPublicKey }); For multiple issuers, pass a trustedKeys object or Map keyed by the embedded keyId : JS Copy const verified = await QuadQR.verifyDecodedSignature(decoded, { trustedKeys: { [decoded.signingKeyId]: knownPublicKey } }); signatureVerified: true means the signature is mathematically valid. signatureTrusted: true means it was checked using an external trusted key. allowEmbeddedKey: true may be used for compatibility/self-contained integrity checks, but such a result is not considered a trusted signer. Signing + encryption # encodeSecureText() and encodeSecureBytes() accept an optional signing object alongside security and compression . The internal pipeline is compression → signing → AES-256-GCM → Spectrum ECC. JS Copy const code = await QuadQR.encodeSecureText(\"private signed payload\", { compression: \"auto\", security: { mode: \"password\", password: \"secret\" }, signing: { privateKey: pair.privateKey, keyId: pair.keyId } });"},{"title":"generateSigningKeyPair()","page":"API Reference","url":"api.html#generatesigningkeypair","text":"generateSigningKeyPair() Generates an extractable Ed25519 key pair using Web Crypto and returns the CryptoKeys, raw public-key bytes, PKCS#8 private-key bytes, and a compact SHA-256-derived keyId ."},{"title":"encodeSignedText(text, options) / encodeSignedBytes(input, options)","page":"API Reference","url":"api.html#encodesignedtexttext-options-encodesignedbytesinput-options","text":"encodeSignedText(text, options) / encodeSignedBytes(input, options) Signs the normal application payload with Ed25519. Any required signature and compression metadata is handled internally. JS Copy const pair = await QuadQR.generateSigningKeyPair(); const code = await QuadQR.encodeSignedText(\"certificate payload\", { ecc: \"Q\", compression: \"auto\", privateKey: pair.privateKey, keyId: pair.keyId }); Only the private key is required for signing. publicKey is accepted only for the explicit embedPublicKey: true compatibility mode."},{"title":"verifyDecodedSignature(result, options?)","page":"API Reference","url":"api.html#verifydecodedsignatureresult-options","text":"verifyDecodedSignature(result, options?) Verifies a signed decode result against a trusted external Ed25519 public key. JS Copy const decoded = QuadQR.decodeMatrix(code.matrix); const verified = await QuadQR.verifyDecodedSignature(decoded, { publicKey: knownPublicKey }); For multiple issuers, pass a trustedKeys object or Map keyed by the embedded keyId : JS Copy const verified = await QuadQR.verifyDecodedSignature(decoded, { trustedKeys: { [decoded.signingKeyId]: knownPublicKey } }); signatureVerified: true means the signature is mathematically valid. signatureTrusted: true means it was checked using an external trusted key. allowEmbeddedKey: true may be used for compatibility/self-contained integrity checks, but such a result is not considered a trusted signer."},{"title":"Signing + encryption","page":"API Reference","url":"api.html#signing-encryption","text":"Signing + encryption encodeSecureText() and encodeSecureBytes() accept an optional signing object alongside security and compression . The internal pipeline is compression → signing → AES-256-GCM → Spectrum ECC. JS Copy const code = await QuadQR.encodeSecureText(\"private signed payload\", { compression: \"auto\", security: { mode: \"password\", password: \"secret\" }, signing: { privateKey: pair.privateKey, keyId: pair.keyId } });"},{"title":"Print rendering","page":"API Reference","url":"api.html#print-rendering","text":"Print rendering All render APIs accept: JS Copy { mode: \"screen\" | \"print\" } Print mode uses a darker print-safe palette and forces Classic modules by default. quietZone has the same behavior in print and screen modes, including support for values below the recommended four-module default. getPrintGuidance(codeOrMatrix, options?) # Returns physical module size, pixels/module at a DPI, recommended minimum physical size, and print recommendations."},{"title":"getPrintGuidance(codeOrMatrix, options?)","page":"API Reference","url":"api.html#getprintguidancecodeormatrix-options","text":"getPrintGuidance(codeOrMatrix, options?) Returns physical module size, pixels/module at a DPI, recommended minimum physical size, and print recommendations."},{"title":"Automatic logo safety","page":"API Reference","url":"api.html#automatic-logo-safety","text":"Automatic logo safety A logo can use: JS Copy logo: { source: logo, size: \"auto\", clearBackground: true } estimateSafeLogoSize() exposes the conservative ratio used by auto mode. findMaxSafeLogoSize() performs an empirical binary search when the logo source is ImageData-like."},{"title":"Scanner confidence and debug mode","page":"API Reference","url":"api.html#scanner-confidence-and-debug-mode","text":"Scanner confidence and debug mode Normal successful scans now include: confidence geometryConfidence calibrationConfidence structureConfidence eccUtilization correctedErrors diagnostics Use scanImageData(image, { debug: true }) for detailed geometry/sampling information. debugScanImageData() is a non-throwing helper that returns { ok, result, debug } or { ok: false, error, debug } ."},{"title":"Scanability and stress testing","page":"API Reference","url":"api.html#scanability-and-stress-testing","text":"Scanability and stress testing applyStressDistortion(imageData, type, severity?, options?) # Deterministically applies a selected synthetic distortion. In addition to the original blur/exposure/shadow/resampling profiles, perspective-3d accepts pitchDegrees , yawDegrees , and rollDegrees for camera-angle testing. runImageStressTest(imageData, expected?, options?) # Runs the standard torture profiles and returns a 0–100 score plus per-scenario decode results. assessScanability(code, renderOptions?, options?) # Renders a code and runs the standard test suite. It returns Excellent , Good , Risky , or Likely unscannable together with recommendations. runReliabilityLab(imageData, expected?, options?) # Runs the broader Reliability Lab suite. options.suite can be quick , full , or extreme . Results include overall score, per-scenario CRC verification, category scores, confidence, RS corrections, and timing. runPerspectiveSweep(imageData, expected?, options?) # Sweeps yaw , pitch , or roll /Z rotation across a list of angles and reports the largest tested angle that decoded successfully. Synthetic scores are regression aids and do not replace physical camera/print testing. See Reliability Lab ."},{"title":"applyStressDistortion(imageData, type, severity?, options?)","page":"API Reference","url":"api.html#applystressdistortionimagedata-type-severity-options","text":"applyStressDistortion(imageData, type, severity?, options?) Deterministically applies a selected synthetic distortion. In addition to the original blur/exposure/shadow/resampling profiles, perspective-3d accepts pitchDegrees , yawDegrees , and rollDegrees for camera-angle testing."},{"title":"runImageStressTest(imageData, expected?, options?)","page":"API Reference","url":"api.html#runimagestresstestimagedata-expected-options","text":"runImageStressTest(imageData, expected?, options?) Runs the standard torture profiles and returns a 0–100 score plus per-scenario decode results."},{"title":"assessScanability(code, renderOptions?, options?)","page":"API Reference","url":"api.html#assessscanabilitycode-renderoptions-options","text":"assessScanability(code, renderOptions?, options?) Renders a code and runs the standard test suite. It returns Excellent , Good , Risky , or Likely unscannable together with recommendations."},{"title":"runReliabilityLab(imageData, expected?, options?)","page":"API Reference","url":"api.html#runreliabilitylabimagedata-expected-options","text":"runReliabilityLab(imageData, expected?, options?) Runs the broader Reliability Lab suite. options.suite can be quick , full , or extreme . Results include overall score, per-scenario CRC verification, category scores, confidence, RS corrections, and timing."},{"title":"runPerspectiveSweep(imageData, expected?, options?)","page":"API Reference","url":"api.html#runperspectivesweepimagedata-expected-options","text":"runPerspectiveSweep(imageData, expected?, options?) Sweeps yaw , pitch , or roll /Z rotation across a list of angles and reports the largest tested angle that decoded successfully. Synthetic scores are regression aids and do not replace physical camera/print testing. See Reliability Lab ."},{"title":"Signing key identifiers","page":"API Reference","url":"api.html#signing-key-identifiers","text":"Signing key identifiers deriveSigningKeyId(publicKey, bytes?) # Derives a lowercase hexadecimal key ID from SHA-256 of the raw Ed25519 public key. bytes defaults to 8 and is clamped to 4..16 ; the returned string therefore contains 8..32 hex characters."},{"title":"deriveSigningKeyId(publicKey, bytes?)","page":"API Reference","url":"api.html#derivesigningkeyid","text":"deriveSigningKeyId(publicKey, bytes?) Derives a lowercase hexadecimal key ID from SHA-256 of the raw Ed25519 public key. bytes defaults to 8 and is clamped to 4..16 ; the returned string therefore contains 8..32 hex characters."},{"title":"Complete public export catalog","page":"API Reference","url":"api.html#public-export-catalog","text":"Complete public export catalog This catalog is intentionally exhaustive for the supported package entry points. Option objects referenced here are defined field-by-field in the Configuration Reference . Browser scanner tuning is also duplicated on Scanning & Camera . Core functions # Export / signature Returns Purpose compressPayload(input, options?) Uint8Array Legacy LZ compressor. Use options.level 1..9; default 6. decompressPayload(input, expectedLength?) Uint8Array Decompresses data produced by compressPayload . Optional expected length is an integrity/shape check. compressDeflatePayload(input, options?) Uint8Array Dependency-free raw fixed-Huffman DEFLATE compressor. options.level is 1..9, default 6. decompressDeflatePayload(input, expectedLength?) Uint8Array Inflates QuadQR raw-DEFLATE payload bytes. compressBrotliPayload(input, options?) Uint8Array Pure-JavaScript Brotli compressor. options.quality is 0..11, explicit default 11. decompressBrotliPayload(input, expectedLength?) Uint8Array Decompresses Brotli payload bytes created by QuadQR. deriveSigningKeyId(publicKey, bytes?) Promise<string> SHA-256-derived lowercase hex identifier for an Ed25519 public key; bytes defaults to 8 and is clamped 4..16. generateSigningKeyPair() Promise<CryptoKeyPair> Generates a WebCrypto Ed25519 signing/verification key pair. installCrc32Accelerator(accelerator?) previous accelerator or null Installs/removes an optional CRC32 accelerator. Passing null restores the built-in JavaScript implementation. crc32(bytes) unsigned 32-bit integer Computes the CRC-32 value used by QuadQR payload framing. getVersionInfo(version, options?) object Returns matrix size, capacity, data-cell count, ECC plan, and High Density information for one version. encodeText(text, options?) encoded code object Encodes UTF-8 text into a QuadQR matrix. Encoding, ECC, version, compression, and High Density options are documented in Configuration Reference. encodeBytes(input, options?) encoded code object Encodes arbitrary bytes into a QuadQR matrix. encodeUint8Array(input, options?) encoded code object Strict Uint8Array convenience alias around byte encoding. encodeSignedBytes(input, options) Promise<encoded code object> Creates an Ed25519-signed binary payload envelope, with optional internal compression. encodeSignedText(text, options) Promise<encoded code object> Text counterpart of encodeSignedBytes . encodeSecureText(text, options) Promise<encoded code object> Encrypts text using Secure Payload v1 password or raw-key mode, then encodes the protected bytes. encodeSecureBytes(input, options) Promise<encoded code object> Binary counterpart of encodeSecureText . decryptDecoded(result, security?) Promise<decode result> Decrypts a locked secure decode result and restores text/binary, compression, and signing metadata. decodeMatrix(matrix, options?) decode result Decodes an already sampled QuadQR cell matrix, including Spectrum ECC error/erasure and optional soft decoding. decodeUint8Array(matrix, options?) Uint8Array Convenience API returning only decoded application bytes. verifyDecodedSignature(result, options?) Promise<decode result> Verifies an Ed25519 signature using a trusted key, trusted-key map, or optionally an embedded public key. estimateSafeLogoSize(codeOrMatrix, options?) number ratio Fast ECC-budget estimate for a conservative centered logo size. findMaxSafeLogoSize(code, options?) object Renders/tests candidate clear-background logo sizes and binary-searches for the largest size that still self-decodes. getPrintGuidance(codeOrMatrix, options?) object Returns recommended quiet zone, physical dimensions, module size, and DPI guidance for printing. renderToCanvas(codeOrMatrix, canvas, options?) canvas Renders to an existing browser canvas and returns that canvas. renderToImageData(codeOrMatrix, options?) ImageData-like object Renders to RGBA pixel data, usable in browser or Node workflows. renderToSVG(codeOrMatrix, options?) string Returns standalone SVG markup. applyStressDistortion(imageData, type, severity?, options?) ImageData-like object Applies one deterministic Reliability Lab distortion such as blur, perspective, shadow, color cast, noise, glare, or downscale. runImageStressTest(imageData, expected?, options?) report object Runs the compact legacy stress profile set against one source image. runReliabilityLab(imageData, expected?, options?) report object Runs quick/full/extreme real-world distortion suites and returns per-case plus aggregate reliability results. runPerspectiveSweep(imageData, expected?, options?) report object Sweeps yaw, pitch, or roll angles and reports decode success/confidence by angle. assessScanability(code, renderOptions?, options?) report object Renders an encoded code and evaluates scanability across configured stress profiles. debugScanImageData(imageData, options?) diagnostic object Runs scanner analysis for debugging and returns finder/geometry/pass information rather than only throwing on failure. scanImageData(imageData, options?) decode result Main synchronous image scanner with geometry detection, calibration, recovery, Spectrum ECC, and optional diagnostics. scanFile(file, options?) Promise<decode result> Browser File / Blob image scanner. Node has a filename-based override in quadqr-js/node . scanVideoFrame(video, options?) decode result Captures one visible video frame/ROI and runs the image scanner. startCameraScanner(video, options?) Promise<{ stream, stop, scanNow, video }> Starts progressive live-camera scanning. Recovery cadence, multi-frame fusion, crop, and callback fields are fully defined on Scanning & Camera. rectifyDetectedCode(imageData, options?) { geometry, imageData } Finds the best symbol geometry and returns a perspective-rectified square image; moduleSize defaults to 8. rotateMatrix(matrix, quarterTurns?) 2D cell matrix Returns a copied matrix rotated clockwise by 90° increments; negative values are normalized modulo four. Core constants # Export Value / type Meaning FORMAT_VERSION 5 Current QuadQR matrix-format version written/read by this release. MIN_VERSION / MAX_VERSION 1 / 40 Supported matrix version bounds. DEFAULT_ECC_LEVEL \"M\" Default Spectrum ECC profile for library encoding. ECC_LEVELS frozen object Nominal L/M/Q/H definitions with IDs and Reed-Solomon parity/correctable-symbol counts. CELL frozen object Cell numeric values: BLACK=-1, RED=0, GREEN=1, BLUE=2, WHITE=3. CELL_ENCODINGS frozen object Physical encoding identifiers rgbw and experimental triangle16 . DEFAULT_PALETTE frozen object Screen RGBW/black hex colors used when no palette override is provided. PRINT_PALETTE frozen object Darker print-safe RGB primaries plus true black/white. RENDER_STYLES frozen object classic , depth , soft , and inset style names. RENDER_MODES frozen object screen and print renderer modes. COMPRESSION_MODES frozen object none , auto , smart , lz , deflate , and brotli identifiers. LZ_LEVEL_MIN / LZ_LEVEL_MAX / DEFAULT_LZ_LEVEL 1 / 9 / 6 Supported/default legacy LZ search level. COMPRESSION_LEVELS frozen object Programmatic level metadata for LZ, DEFLATE, Brotli, Auto, and Smart compression stages. SIGNATURE_ALGORITHMS frozen object Currently exposes ED25519: \"Ed25519\" . SECURITY_MODES frozen object Secure Payload credential mode identifiers: password and raw-key. SECURITY_ALGORITHMS frozen object Algorithm identifiers used by Secure Payload v1, including AES-256-GCM and PBKDF2-HMAC-SHA-256. SECURE_PAYLOAD_VERSION integer Current encrypted secure-envelope format version. DEFAULT_PBKDF2_ITERATIONS 600000 Default password KDF work factor. STRESS_PROFILES frozen array Compact predefined scanability stress cases used by runImageStressTest / assessScanability . RELIABILITY_PROFILES frozen array Full categorized quick/full/extreme Reliability Lab profile definitions. internals frozen object, unsupported Implementation/testing helpers. Exported for project diagnostics, not a stable application API; do not build production integrations against these members. Node entry point: quadqr-js/node # Export / signature Returns Purpose decodePNG(input) ImageData-like object Dependency-free decoder for supported non-interlaced PNG buffers. encodePNG(imageData, options?) Uint8Array Dependency-free PNG encoder; compressionLevel is 0..9, default 9, and affects PNG file size only. toPNG(codeOrMatrix, options?) Uint8Array Renders and PNG-encodes a code/matrix in memory. savePNG(codeOrMatrix, filename, options?) Promise<void> Writes rendered PNG bytes to disk. toSVG(codeOrMatrix, options?) string Node convenience wrapper returning SVG markup. saveSVG(codeOrMatrix, filename, options?) Promise<void> Writes SVG markup to disk. scanBuffer(input, options?) Promise<decode result> Scans encoded image bytes. PNG works dependency-free; other formats can use host-installed Sharp. scanFile(filename, options?) Promise<decode result> Reads and scans an image path in Node. initWasm(options?) Promise<state> Loads the optional CRC32 WebAssembly accelerator from a URL or supplied bytes. getWasmState() state object Reports whether WASM is loaded/active and any relevant state. disableWasm() state object Disables the installed WASM accelerator and restores JavaScript CRC32. Benchmark entry point: quadqr-js/benchmark # Export / signature Returns Purpose STANDARD_QR_BYTE_CAPACITY frozen object Reference ISO QR byte-mode capacities for versions 1..40 at L/M/Q/H, used only for same-size comparison. QuadQR ECC letters are not recovery-equivalent. getStandardQrByteCapacity(version, ecc?) integer bytes Returns the reference standard-QR byte capacity for one version and nominal ECC letter. compareCapacity(version, ecc?, options?) comparison object Compares one QuadQR version with same-size standard QR byte capacity, including bytes, ratio, gain percent, and matrix-cell efficiency. calculateCapacityPlan(options?) capacity-plan object Finds the smallest QuadQR/standard-QR versions for a concrete payload or byte count and can account for compression/signing overhead. buildCapacityComparison(options?) array of comparison rows Builds capacity-comparison rows across a requested version range. benchmarkCodec(options?) benchmark object Measures encode/decode/compression timing for configured payload sizes, iterations, ECC, and High Density settings. benchmarkReport(options?) Promise<report object> Runs capacity and codec benchmarks and returns one combined machine-readable report. Previous ← Reliability Lab Next Configuration Reference → QuadQR Experimental RGBW matrix symbology. AGPL-3.0 licensed"},{"title":"Core functions","page":"API Reference","url":"api.html#core-export-catalog","text":"Core functions Export / signature Returns Purpose compressPayload(input, options?) Uint8Array Legacy LZ compressor. Use options.level 1..9; default 6. decompressPayload(input, expectedLength?) Uint8Array Decompresses data produced by compressPayload . Optional expected length is an integrity/shape check. compressDeflatePayload(input, options?) Uint8Array Dependency-free raw fixed-Huffman DEFLATE compressor. options.level is 1..9, default 6. decompressDeflatePayload(input, expectedLength?) Uint8Array Inflates QuadQR raw-DEFLATE payload bytes. compressBrotliPayload(input, options?) Uint8Array Pure-JavaScript Brotli compressor. options.quality is 0..11, explicit default 11. decompressBrotliPayload(input, expectedLength?) Uint8Array Decompresses Brotli payload bytes created by QuadQR. deriveSigningKeyId(publicKey, bytes?) Promise<string> SHA-256-derived lowercase hex identifier for an Ed25519 public key; bytes defaults to 8 and is clamped 4..16. generateSigningKeyPair() Promise<CryptoKeyPair> Generates a WebCrypto Ed25519 signing/verification key pair. installCrc32Accelerator(accelerator?) previous accelerator or null Installs/removes an optional CRC32 accelerator. Passing null restores the built-in JavaScript implementation. crc32(bytes) unsigned 32-bit integer Computes the CRC-32 value used by QuadQR payload framing. getVersionInfo(version, options?) object Returns matrix size, capacity, data-cell count, ECC plan, and High Density information for one version. encodeText(text, options?) encoded code object Encodes UTF-8 text into a QuadQR matrix. Encoding, ECC, version, compression, and High Density options are documented in Configuration Reference. encodeBytes(input, options?) encoded code object Encodes arbitrary bytes into a QuadQR matrix. encodeUint8Array(input, options?) encoded code object Strict Uint8Array convenience alias around byte encoding. encodeSignedBytes(input, options) Promise<encoded code object> Creates an Ed25519-signed binary payload envelope, with optional internal compression. encodeSignedText(text, options) Promise<encoded code object> Text counterpart of encodeSignedBytes . encodeSecureText(text, options) Promise<encoded code object> Encrypts text using Secure Payload v1 password or raw-key mode, then encodes the protected bytes. encodeSecureBytes(input, options) Promise<encoded code object> Binary counterpart of encodeSecureText . decryptDecoded(result, security?) Promise<decode result> Decrypts a locked secure decode result and restores text/binary, compression, and signing metadata. decodeMatrix(matrix, options?) decode result Decodes an already sampled QuadQR cell matrix, including Spectrum ECC error/erasure and optional soft decoding. decodeUint8Array(matrix, options?) Uint8Array Convenience API returning only decoded application bytes. verifyDecodedSignature(result, options?) Promise<decode result> Verifies an Ed25519 signature using a trusted key, trusted-key map, or optionally an embedded public key. estimateSafeLogoSize(codeOrMatrix, options?) number ratio Fast ECC-budget estimate for a conservative centered logo size. findMaxSafeLogoSize(code, options?) object Renders/tests candidate clear-background logo sizes and binary-searches for the largest size that still self-decodes. getPrintGuidance(codeOrMatrix, options?) object Returns recommended quiet zone, physical dimensions, module size, and DPI guidance for printing. renderToCanvas(codeOrMatrix, canvas, options?) canvas Renders to an existing browser canvas and returns that canvas. renderToImageData(codeOrMatrix, options?) ImageData-like object Renders to RGBA pixel data, usable in browser or Node workflows. renderToSVG(codeOrMatrix, options?) string Returns standalone SVG markup. applyStressDistortion(imageData, type, severity?, options?) ImageData-like object Applies one deterministic Reliability Lab distortion such as blur, perspective, shadow, color cast, noise, glare, or downscale. runImageStressTest(imageData, expected?, options?) report object Runs the compact legacy stress profile set against one source image. runReliabilityLab(imageData, expected?, options?) report object Runs quick/full/extreme real-world distortion suites and returns per-case plus aggregate reliability results. runPerspectiveSweep(imageData, expected?, options?) report object Sweeps yaw, pitch, or roll angles and reports decode success/confidence by angle. assessScanability(code, renderOptions?, options?) report object Renders an encoded code and evaluates scanability across configured stress profiles. debugScanImageData(imageData, options?) diagnostic object Runs scanner analysis for debugging and returns finder/geometry/pass information rather than only throwing on failure. scanImageData(imageData, options?) decode result Main synchronous image scanner with geometry detection, calibration, recovery, Spectrum ECC, and optional diagnostics. scanFile(file, options?) Promise<decode result> Browser File / Blob image scanner. Node has a filename-based override in quadqr-js/node . scanVideoFrame(video, options?) decode result Captures one visible video frame/ROI and runs the image scanner. startCameraScanner(video, options?) Promise<{ stream, stop, scanNow, video }> Starts progressive live-camera scanning. Recovery cadence, multi-frame fusion, crop, and callback fields are fully defined on Scanning & Camera. rectifyDetectedCode(imageData, options?) { geometry, imageData } Finds the best symbol geometry and returns a perspective-rectified square image; moduleSize defaults to 8. rotateMatrix(matrix, quarterTurns?) 2D cell matrix Returns a copied matrix rotated clockwise by 90° increments; negative values are normalized modulo four."},{"title":"Core constants","page":"API Reference","url":"api.html#constant-export-catalog","text":"Core constants Export Value / type Meaning FORMAT_VERSION 5 Current QuadQR matrix-format version written/read by this release. MIN_VERSION / MAX_VERSION 1 / 40 Supported matrix version bounds. DEFAULT_ECC_LEVEL \"M\" Default Spectrum ECC profile for library encoding. ECC_LEVELS frozen object Nominal L/M/Q/H definitions with IDs and Reed-Solomon parity/correctable-symbol counts. CELL frozen object Cell numeric values: BLACK=-1, RED=0, GREEN=1, BLUE=2, WHITE=3. CELL_ENCODINGS frozen object Physical encoding identifiers rgbw and experimental triangle16 . DEFAULT_PALETTE frozen object Screen RGBW/black hex colors used when no palette override is provided. PRINT_PALETTE frozen object Darker print-safe RGB primaries plus true black/white. RENDER_STYLES frozen object classic , depth , soft , and inset style names. RENDER_MODES frozen object screen and print renderer modes. COMPRESSION_MODES frozen object none , auto , smart , lz , deflate , and brotli identifiers. LZ_LEVEL_MIN / LZ_LEVEL_MAX / DEFAULT_LZ_LEVEL 1 / 9 / 6 Supported/default legacy LZ search level. COMPRESSION_LEVELS frozen object Programmatic level metadata for LZ, DEFLATE, Brotli, Auto, and Smart compression stages. SIGNATURE_ALGORITHMS frozen object Currently exposes ED25519: \"Ed25519\" . SECURITY_MODES frozen object Secure Payload credential mode identifiers: password and raw-key. SECURITY_ALGORITHMS frozen object Algorithm identifiers used by Secure Payload v1, including AES-256-GCM and PBKDF2-HMAC-SHA-256. SECURE_PAYLOAD_VERSION integer Current encrypted secure-envelope format version. DEFAULT_PBKDF2_ITERATIONS 600000 Default password KDF work factor. STRESS_PROFILES frozen array Compact predefined scanability stress cases used by runImageStressTest / assessScanability . RELIABILITY_PROFILES frozen array Full categorized quick/full/extreme Reliability Lab profile definitions. internals frozen object, unsupported Implementation/testing helpers. Exported for project diagnostics, not a stable application API; do not build production integrations against these members."},{"title":"Node entry point: quadqr-js/node","page":"API Reference","url":"api.html#node-export-catalog","text":"Node entry point: quadqr-js/node Export / signature Returns Purpose decodePNG(input) ImageData-like object Dependency-free decoder for supported non-interlaced PNG buffers. encodePNG(imageData, options?) Uint8Array Dependency-free PNG encoder; compressionLevel is 0..9, default 9, and affects PNG file size only. toPNG(codeOrMatrix, options?) Uint8Array Renders and PNG-encodes a code/matrix in memory. savePNG(codeOrMatrix, filename, options?) Promise<void> Writes rendered PNG bytes to disk. toSVG(codeOrMatrix, options?) string Node convenience wrapper returning SVG markup. saveSVG(codeOrMatrix, filename, options?) Promise<void> Writes SVG markup to disk. scanBuffer(input, options?) Promise<decode result> Scans encoded image bytes. PNG works dependency-free; other formats can use host-installed Sharp. scanFile(filename, options?) Promise<decode result> Reads and scans an image path in Node. initWasm(options?) Promise<state> Loads the optional CRC32 WebAssembly accelerator from a URL or supplied bytes. getWasmState() state object Reports whether WASM is loaded/active and any relevant state. disableWasm() state object Disables the installed WASM accelerator and restores JavaScript CRC32."},{"title":"Benchmark entry point: quadqr-js/benchmark","page":"API Reference","url":"api.html#benchmark-export-catalog","text":"Benchmark entry point: quadqr-js/benchmark Export / signature Returns Purpose STANDARD_QR_BYTE_CAPACITY frozen object Reference ISO QR byte-mode capacities for versions 1..40 at L/M/Q/H, used only for same-size comparison. QuadQR ECC letters are not recovery-equivalent. getStandardQrByteCapacity(version, ecc?) integer bytes Returns the reference standard-QR byte capacity for one version and nominal ECC letter. compareCapacity(version, ecc?, options?) comparison object Compares one QuadQR version with same-size standard QR byte capacity, including bytes, ratio, gain percent, and matrix-cell efficiency. calculateCapacityPlan(options?) capacity-plan object Finds the smallest QuadQR/standard-QR versions for a concrete payload or byte count and can account for compression/signing overhead. buildCapacityComparison(options?) array of comparison rows Builds capacity-comparison rows across a requested version range. benchmarkCodec(options?) benchmark object Measures encode/decode/compression timing for configured payload sizes, iterations, ECC, and High Density settings. benchmarkReport(options?) Promise<report object> Runs capacity and codec benchmarks and returns one combined machine-readable report. Previous ← Reliability Lab Next Configuration Reference → QuadQR Experimental RGBW matrix symbology. AGPL-3.0 licensed"},{"title":"Browser & CDN","page":"Browser & CDN","url":"browser.html","text":"QUADQR DOCUMENTATION Browser & CDN Complete option definitions For every camera and image-scanner tuning field, including recovery cadence options such as cameraFinderRecoveryEvery , see Scanning & Camera or the Configuration Reference . Browser ESM With Vite, webpack, Rollup, Next.js client code, or another browser bundler: JS Copy import { encodeText, renderToCanvas, renderToSVG, scanFile, startCameraScanner } from \"quadqr-js/browser\"; The main quadqr-js entry also works in modern browser bundlers for runtime-neutral APIs. Render to canvas JS Copy const code = encodeText(\"Hello browser\"); renderToCanvas(code, document.querySelector(\"#qr\"), { imageSize: 720, quietZone: 4, style: \"classic\" }); const svg = renderToSVG(code, { imageSize: 720, quietZone: 4 }); imageSize controls the exact square output size. If neither imageSize nor moduleSize is supplied, the renderer defaults to 720 × 720 px. Available styles are classic , depth , soft , and inset . Compressed, signed, and print-safe output JS Copy import { assessScanability, decodeMatrix, encodeText, encodeSignedText, generateSigningKeyPair, renderToCanvas, verifyDecodedSignature } from \"quadqr-js/browser\"; const compressed = encodeText(\"repeat repeat repeat repeat\", { compression: \"auto\" }); const smallest = encodeText(\"structured payload \".repeat(500), { compression: \"smart\" }); const keys = await generateSigningKeyPair(); const signed = await encodeSignedText(\"ticket\", { compression: \"auto\", privateKey: keys.privateKey, keyId: keys.keyId }); const checked = await verifyDecodedSignature(decodeMatrix(signed.matrix), { publicKey: keys.publicKey }); console.log(checked.signatureVerified, checked.signatureTrusted); const canvas = document.querySelector(\"#qr\"); renderToCanvas(compressed, canvas, { mode: \"print\", imageSize: 720 }); const report = assessScanability(compressed, { imageSize: 480 }); console.log(report.score, report.rating); Compression uses bundled synchronous Brotli/DEFLATE/LZ implementations with explicit codec levels, fast Auto, and CPU-heavy Smart modes and signing uses internal metadata only when required. The repository demo keeps heavy encoding, verification, stress testing, perspective sweeps, image scanning, and benchmarks off the UI thread with module Web Workers, while the public library API itself remains synchronous where documented. There is no public content-type mode to configure. The private key signs, while the public verification key stays outside the QuadQR by default. Use the stored keyId to select an application-trusted public key. Scanability testing is deterministic synthetic regression testing and should be supplemented with real devices and print samples. Scan an uploaded image JS Copy const input = document.querySelector(\"#image\"); const result = await scanFile(input.files[0]); console.log(result.text); If the symbol uses Secure Payload, the scan result has secure: true and requiresDecryption: true until it is successfully unlocked. Live camera scanning JS Copy const video = document.querySelector(\"#camera\"); const scanner = await startCameraScanner(video, { scanInterval: 33, cameraFinderRecoveryEvery: 2, async onResult(result) { console.log(result); scanner.stop(); }, onScanMiss() { // Keep searching. }, onDiagnostic(event) { // Useful for live finder overlays and scanner debug logs. console.log(event.method, event.finderMethod, event.finderCount, event.geometry?.version); } }); Camera access requires HTTPS or localhost. QuadQR scans the part of an object-fit: cover video that is actually visible to the user. The fast finder pass uses the RGB value channel ( max(R,G,B) ) so saturated data colors are less likely to be mistaken for structural black. After misses, throttled recovery frames can try alternate finder thresholds before stronger color correction. onDiagnostic exposes the finder method and geometry work already performed by the scanner. Decrypt a secure scan JS Copy import { decryptDecoded } from \"quadqr-js/browser\"; const locked = await scanFile(file); if (locked.secure) { const unlocked = await decryptDecoded(locked, { password: \"my-password\" }); console.log(unlocked.text); } Raw-key mode uses { key } instead of { password } . Classic script tag The global browser build exposes window.QuadQR / globalThis.QuadQR . HTML Copy <canvas id=\"qr\"></canvas> <script src=\"https://cdn.jsdelivr.net/npm/quadqr-js@1.5.5/dist/quadqr.min.js\"></script> <script> const code = QuadQR.encodeText(\"Hello CDN\"); QuadQR.renderToCanvas(code, document.querySelector(\"#qr\"), { imageSize: 720, quietZone: 4 }); </script> unpkg HTML Copy <script src=\"https://unpkg.com/quadqr-js@1.5.5/dist/quadqr.min.js\"></script> Direct CDN ESM HTML Copy <script type=\"module\"> import { encodeText, renderToCanvas } from \"https://cdn.jsdelivr.net/npm/quadqr-js@1.5.5/dist/browser.js\"; const code = encodeText(\"ES module CDN\"); renderToCanvas(code, document.querySelector(\"#qr\")); </script> Optional WASM from a CDN The classic global build can resolve the bundled WASM asset relative to its own script URL: JS Copy await QuadQR.initWasm(); You can also provide a custom URL: JS Copy await QuadQR.initWasm({ url: \"https://example.com/assets/quadqr-core.wasm\" }); WASM is optional. The normal JavaScript implementation remains available if it cannot load. Production recommendation Pin a concrete package version such as @1.5.5 in CDN URLs so an existing site does not silently change when a newer package version becomes available. Previous ← Getting Started Next Node.js → QuadQR Experimental RGBW matrix symbology. AGPL-3.0 licensed"},{"title":"Browser ESM","page":"Browser & CDN","url":"browser.html#browser-esm","text":"Browser ESM With Vite, webpack, Rollup, Next.js client code, or another browser bundler: JS Copy import { encodeText, renderToCanvas, renderToSVG, scanFile, startCameraScanner } from \"quadqr-js/browser\"; The main quadqr-js entry also works in modern browser bundlers for runtime-neutral APIs."},{"title":"Render to canvas","page":"Browser & CDN","url":"browser.html#render-to-canvas","text":"Render to canvas JS Copy const code = encodeText(\"Hello browser\"); renderToCanvas(code, document.querySelector(\"#qr\"), { imageSize: 720, quietZone: 4, style: \"classic\" }); const svg = renderToSVG(code, { imageSize: 720, quietZone: 4 }); imageSize controls the exact square output size. If neither imageSize nor moduleSize is supplied, the renderer defaults to 720 × 720 px. Available styles are classic , depth , soft , and inset ."},{"title":"Compressed, signed, and print-safe output","page":"Browser & CDN","url":"browser.html#compressed-signed-and-print-safe-output","text":"Compressed, signed, and print-safe output JS Copy import { assessScanability, decodeMatrix, encodeText, encodeSignedText, generateSigningKeyPair, renderToCanvas, verifyDecodedSignature } from \"quadqr-js/browser\"; const compressed = encodeText(\"repeat repeat repeat repeat\", { compression: \"auto\" }); const smallest = encodeText(\"structured payload \".repeat(500), { compression: \"smart\" }); const keys = await generateSigningKeyPair(); const signed = await encodeSignedText(\"ticket\", { compression: \"auto\", privateKey: keys.privateKey, keyId: keys.keyId }); const checked = await verifyDecodedSignature(decodeMatrix(signed.matrix), { publicKey: keys.publicKey }); console.log(checked.signatureVerified, checked.signatureTrusted); const canvas = document.querySelector(\"#qr\"); renderToCanvas(compressed, canvas, { mode: \"print\", imageSize: 720 }); const report = assessScanability(compressed, { imageSize: 480 }); console.log(report.score, report.rating); Compression uses bundled synchronous Brotli/DEFLATE/LZ implementations with explicit codec levels, fast Auto, and CPU-heavy Smart modes and signing uses internal metadata only when required. The repository demo keeps heavy encoding, verification, stress testing, perspective sweeps, image scanning, and benchmarks off the UI thread with module Web Workers, while the public library API itself remains synchronous where documented. There is no public content-type mode to configure. The private key signs, while the public verification key stays outside the QuadQR by default. Use the stored keyId to select an application-trusted public key. Scanability testing is deterministic synthetic regression testing and should be supplemented with real devices and print samples."},{"title":"Scan an uploaded image","page":"Browser & CDN","url":"browser.html#scan-an-uploaded-image","text":"Scan an uploaded image JS Copy const input = document.querySelector(\"#image\"); const result = await scanFile(input.files[0]); console.log(result.text); If the symbol uses Secure Payload, the scan result has secure: true and requiresDecryption: true until it is successfully unlocked."},{"title":"Live camera scanning","page":"Browser & CDN","url":"browser.html#live-camera-scanning","text":"Live camera scanning JS Copy const video = document.querySelector(\"#camera\"); const scanner = await startCameraScanner(video, { scanInterval: 33, cameraFinderRecoveryEvery: 2, async onResult(result) { console.log(result); scanner.stop(); }, onScanMiss() { // Keep searching. }, onDiagnostic(event) { // Useful for live finder overlays and scanner debug logs. console.log(event.method, event.finderMethod, event.finderCount, event.geometry?.version); } }); Camera access requires HTTPS or localhost. QuadQR scans the part of an object-fit: cover video that is actually visible to the user. The fast finder pass uses the RGB value channel ( max(R,G,B) ) so saturated data colors are less likely to be mistaken for structural black. After misses, throttled recovery frames can try alternate finder thresholds before stronger color correction. onDiagnostic exposes the finder method and geometry work already performed by the scanner."},{"title":"Decrypt a secure scan","page":"Browser & CDN","url":"browser.html#decrypt-a-secure-scan","text":"Decrypt a secure scan JS Copy import { decryptDecoded } from \"quadqr-js/browser\"; const locked = await scanFile(file); if (locked.secure) { const unlocked = await decryptDecoded(locked, { password: \"my-password\" }); console.log(unlocked.text); } Raw-key mode uses { key } instead of { password } ."},{"title":"Classic script tag","page":"Browser & CDN","url":"browser.html#classic-script-tag","text":"Classic script tag The global browser build exposes window.QuadQR / globalThis.QuadQR . HTML Copy <canvas id=\"qr\"></canvas> <script src=\"https://cdn.jsdelivr.net/npm/quadqr-js@1.5.5/dist/quadqr.min.js\"></script> <script> const code = QuadQR.encodeText(\"Hello CDN\"); QuadQR.renderToCanvas(code, document.querySelector(\"#qr\"), { imageSize: 720, quietZone: 4 }); </script>"},{"title":"unpkg","page":"Browser & CDN","url":"browser.html#unpkg","text":"unpkg HTML Copy <script src=\"https://unpkg.com/quadqr-js@1.5.5/dist/quadqr.min.js\"></script>"},{"title":"Direct CDN ESM","page":"Browser & CDN","url":"browser.html#direct-cdn-esm","text":"Direct CDN ESM HTML Copy <script type=\"module\"> import { encodeText, renderToCanvas } from \"https://cdn.jsdelivr.net/npm/quadqr-js@1.5.5/dist/browser.js\"; const code = encodeText(\"ES module CDN\"); renderToCanvas(code, document.querySelector(\"#qr\")); </script>"},{"title":"Optional WASM from a CDN","page":"Browser & CDN","url":"browser.html#optional-wasm-from-a-cdn","text":"Optional WASM from a CDN The classic global build can resolve the bundled WASM asset relative to its own script URL: JS Copy await QuadQR.initWasm(); You can also provide a custom URL: JS Copy await QuadQR.initWasm({ url: \"https://example.com/assets/quadqr-core.wasm\" }); WASM is optional. The normal JavaScript implementation remains available if it cannot load."},{"title":"Production recommendation","page":"Browser & CDN","url":"browser.html#production-recommendation","text":"Production recommendation Pin a concrete package version such as @1.5.5 in CDN URLs so an existing site does not silently change when a newer package version becomes available. Previous ← Getting Started Next Node.js → QuadQR Experimental RGBW matrix symbology. AGPL-3.0 licensed"},{"title":"CLI Reference","page":"CLI Reference","url":"cli.html","text":"QUADQR DOCUMENTATION CLI Reference The npm package includes the quadqr executable and can be used directly through npx . Encode text BASH Copy npx quadqr-js encode \"Hello QuadQR\" -o hello.png npx quadqr-js encode \"Hello QuadQR\" -o hello.svg Optional encoding controls: BASH Copy npx quadqr-js encode \"Hello\" --ecc M --version auto --image-size 720 --quiet-zone 4 -o hello.png Use the print-safe rendering profile when the symbol is intended for physical output: BASH Copy npx quadqr-js encode \"Print me\" --print -o print.svg Print mode uses the print-safe palette and Classic rendering defaults. --quiet-zone is honored unchanged, just as it is in screen mode; four modules remains recommended. Compression Compression works directly with normal text payloads: BASH Copy npx quadqr-js encode \"repeat repeat repeat repeat\" \\ --compression auto \\ -o compressed.png Compression modes are none , auto , smart , brotli , deflate , and lz . auto performs one balanced comparison using LZ level 6, DEFLATE level 6, and Brotli quality 6. smart is CPU-heavy: it starts with the same pass and only escalates to stronger DEFLATE/Brotli levels when a smaller QuadQR version is realistically reachable. If envelope overhead would erase the gain, Auto/Smart leave the original payload untouched. No separate payload mode is required. Explicit codecs can select a level: BASH Copy npx quadqr-js encode \"structured payload\" --compression lz --compression-level 9 -o lz.png npx quadqr-js encode \"structured payload\" --compression deflate --compression-level 9 -o deflate.png npx quadqr-js encode \"structured payload\" --compression brotli --compression-level 11 -o brotli.png LZ and DEFLATE accept levels 1..9 and default to 6. Brotli accepts qualities 0..11 and defaults to 11. --compression-level is ignored by Auto/Smart because those modes manage their own staged levels. Signed QuadQR Generate an Ed25519 signing-key bundle: BASH Copy npx quadqr-js signkeygen -o quadqr-signing-key.json Keep this file secret because it contains the private signing key. Encode a signed payload with: BASH Copy npx quadqr-js encode \"Offline-verifiable ticket\" \\ --sign-key quadqr-signing-key.json \\ -o signed.png The generated key bundle contains both keys plus a compact keyId . Only the private key signs. The public key is not embedded in the QuadQR by default and should be distributed separately to trusted scanners or stored in a trusted-key registry. To override the identifier stored in the symbol: BASH Copy npx quadqr-js encode \"Offline-verifiable ticket\" \\ --sign-key quadqr-signing-key.json \\ --key-id event-main-2026 \\ -o signed.png Signing can be combined with password or raw-key encryption: BASH Copy npx quadqr-js encode \"Signed and private\" \\ --sign-key quadqr-signing-key.json \\ --password \"my-password\" \\ -o signed-secure.png Decode an image BASH Copy npx quadqr-js decode hello.png npx quadqr-js decode signed.png --verify-key quadqr-signing-key.json For an unencrypted text payload, the decoded text is printed to stdout. To verify a signed symbol against a trusted public key, pass the signing bundle with --verify-key . Add scanner diagnostics without changing the normal stdout payload: BASH Copy npx quadqr-js decode hello.png --debug Diagnostics are written to stderr and include confidence, geometry/color confidence, ECC utilization, signing state, and the scanner diagnostics object when available. Password-protected payloads Encode: BASH Copy npx quadqr-js encode \"Private data\" --password \"my-password\" -o secure.png Decode: BASH Copy npx quadqr-js decode secure.png --password \"my-password\" If an encrypted symbol is decoded without a credential, the CLI reports that decryption is required instead of exposing plaintext. Raw 256-bit key mode Generate a random 256-bit encryption key: BASH Copy npx quadqr-js keygen The output is a 64-character hexadecimal key. Store it securely and do not place it inside the same QuadQR symbol. Encode using the key: BASH Copy npx quadqr-js encode \"Application secret\" --key <64-hex-key> -o secure-key.png Decode using the key: BASH Copy npx quadqr-js decode secure-key.png --key <64-hex-key> Options Option Purpose Default -o, --output <file> PNG/SVG output path for encode , or signing-key JSON path for signkeygen . quadqr.png for encode --ecc <L|M|Q|H> Spectrum ECC profile. M --version <auto|1..40> Fixed symbol version or automatic smallest-fit version. auto --compression <mode> none|auto|smart|brotli|deflate|lz . auto in CLI encode --compression-level <n> Explicit LZ/DEFLATE 1..9 or Brotli 0..11 level. Auto/Smart manage their own levels. unset --high-density Enable experimental Triangle16 body cells. off --sign-key <file> Sign with a signkeygen key bundle. unset --key-id <id> Override the signing key ID stored in the symbol. bundle key ID --embed-public-key Embed the Ed25519 public key in the payload envelope. off --verify-key <file> Verify a signed decode using the public key in a trusted bundle. unset --password <text> Password-mode encryption/decryption. unset --key <hex> Raw 256-bit key encryption/decryption; exactly 64 hex characters. unset --print Use print-safe rendering. off --image-size <px> Exact square output size. 720 --module-size <px> Pixels per module when image size is omitted. unset --quiet-zone <modules> Blank border size. 4 --debug Write decode diagnostics to stderr. off -h, --help Show CLI help. — Password mode and raw-key mode are mutually exclusive for a single operation. High Density Mode (Experimental) Use --high-density to enable the experimental Triangle16 layout with 16 states and 4 raw bits per body cell. Decode is automatic; no matching decode flag is required. Previous ← Configuration Reference Next WebAssembly → QuadQR Experimental RGBW matrix symbology. AGPL-3.0 licensed"},{"title":"Encode text","page":"CLI Reference","url":"cli.html#encode-text","text":"Encode text BASH Copy npx quadqr-js encode \"Hello QuadQR\" -o hello.png npx quadqr-js encode \"Hello QuadQR\" -o hello.svg Optional encoding controls: BASH Copy npx quadqr-js encode \"Hello\" --ecc M --version auto --image-size 720 --quiet-zone 4 -o hello.png Use the print-safe rendering profile when the symbol is intended for physical output: BASH Copy npx quadqr-js encode \"Print me\" --print -o print.svg Print mode uses the print-safe palette and Classic rendering defaults. --quiet-zone is honored unchanged, just as it is in screen mode; four modules remains recommended."},{"title":"Compression","page":"CLI Reference","url":"cli.html#compression","text":"Compression Compression works directly with normal text payloads: BASH Copy npx quadqr-js encode \"repeat repeat repeat repeat\" \\ --compression auto \\ -o compressed.png Compression modes are none , auto , smart , brotli , deflate , and lz . auto performs one balanced comparison using LZ level 6, DEFLATE level 6, and Brotli quality 6. smart is CPU-heavy: it starts with the same pass and only escalates to stronger DEFLATE/Brotli levels when a smaller QuadQR version is realistically reachable. If envelope overhead would erase the gain, Auto/Smart leave the original payload untouched. No separate payload mode is required. Explicit codecs can select a level: BASH Copy npx quadqr-js encode \"structured payload\" --compression lz --compression-level 9 -o lz.png npx quadqr-js encode \"structured payload\" --compression deflate --compression-level 9 -o deflate.png npx quadqr-js encode \"structured payload\" --compression brotli --compression-level 11 -o brotli.png LZ and DEFLATE accept levels 1..9 and default to 6. Brotli accepts qualities 0..11 and defaults to 11. --compression-level is ignored by Auto/Smart because those modes manage their own staged levels."},{"title":"Signed QuadQR","page":"CLI Reference","url":"cli.html#signed-quadqr","text":"Signed QuadQR Generate an Ed25519 signing-key bundle: BASH Copy npx quadqr-js signkeygen -o quadqr-signing-key.json Keep this file secret because it contains the private signing key. Encode a signed payload with: BASH Copy npx quadqr-js encode \"Offline-verifiable ticket\" \\ --sign-key quadqr-signing-key.json \\ -o signed.png The generated key bundle contains both keys plus a compact keyId . Only the private key signs. The public key is not embedded in the QuadQR by default and should be distributed separately to trusted scanners or stored in a trusted-key registry. To override the identifier stored in the symbol: BASH Copy npx quadqr-js encode \"Offline-verifiable ticket\" \\ --sign-key quadqr-signing-key.json \\ --key-id event-main-2026 \\ -o signed.png Signing can be combined with password or raw-key encryption: BASH Copy npx quadqr-js encode \"Signed and private\" \\ --sign-key quadqr-signing-key.json \\ --password \"my-password\" \\ -o signed-secure.png"},{"title":"Decode an image","page":"CLI Reference","url":"cli.html#decode-an-image","text":"Decode an image BASH Copy npx quadqr-js decode hello.png npx quadqr-js decode signed.png --verify-key quadqr-signing-key.json For an unencrypted text payload, the decoded text is printed to stdout. To verify a signed symbol against a trusted public key, pass the signing bundle with --verify-key . Add scanner diagnostics without changing the normal stdout payload: BASH Copy npx quadqr-js decode hello.png --debug Diagnostics are written to stderr and include confidence, geometry/color confidence, ECC utilization, signing state, and the scanner diagnostics object when available."},{"title":"Password-protected payloads","page":"CLI Reference","url":"cli.html#password-protected-payloads","text":"Password-protected payloads Encode: BASH Copy npx quadqr-js encode \"Private data\" --password \"my-password\" -o secure.png Decode: BASH Copy npx quadqr-js decode secure.png --password \"my-password\" If an encrypted symbol is decoded without a credential, the CLI reports that decryption is required instead of exposing plaintext."},{"title":"Raw 256-bit key mode","page":"CLI Reference","url":"cli.html#raw-256-bit-key-mode","text":"Raw 256-bit key mode Generate a random 256-bit encryption key: BASH Copy npx quadqr-js keygen The output is a 64-character hexadecimal key. Store it securely and do not place it inside the same QuadQR symbol. Encode using the key: BASH Copy npx quadqr-js encode \"Application secret\" --key <64-hex-key> -o secure-key.png Decode using the key: BASH Copy npx quadqr-js decode secure-key.png --key <64-hex-key>"},{"title":"Options","page":"CLI Reference","url":"cli.html#options","text":"Options Option Purpose Default -o, --output <file> PNG/SVG output path for encode , or signing-key JSON path for signkeygen . quadqr.png for encode --ecc <L|M|Q|H> Spectrum ECC profile. M --version <auto|1..40> Fixed symbol version or automatic smallest-fit version. auto --compression <mode> none|auto|smart|brotli|deflate|lz . auto in CLI encode --compression-level <n> Explicit LZ/DEFLATE 1..9 or Brotli 0..11 level. Auto/Smart manage their own levels. unset --high-density Enable experimental Triangle16 body cells. off --sign-key <file> Sign with a signkeygen key bundle. unset --key-id <id> Override the signing key ID stored in the symbol. bundle key ID --embed-public-key Embed the Ed25519 public key in the payload envelope. off --verify-key <file> Verify a signed decode using the public key in a trusted bundle. unset --password <text> Password-mode encryption/decryption. unset --key <hex> Raw 256-bit key encryption/decryption; exactly 64 hex characters. unset --print Use print-safe rendering. off --image-size <px> Exact square output size. 720 --module-size <px> Pixels per module when image size is omitted. unset --quiet-zone <modules> Blank border size. 4 --debug Write decode diagnostics to stderr. off -h, --help Show CLI help. — Password mode and raw-key mode are mutually exclusive for a single operation."},{"title":"High Density Mode (Experimental)","page":"CLI Reference","url":"cli.html#high-density-mode-experimental","text":"High Density Mode (Experimental) Use --high-density to enable the experimental Triangle16 layout with 16 states and 4 raw bits per body cell. Decode is automatic; no matching decode flag is required. Previous ← Configuration Reference Next WebAssembly → QuadQR Experimental RGBW matrix symbology. AGPL-3.0 licensed"},{"title":"Compression","page":"Compression","url":"compression.html","text":"QUADQR DOCUMENTATION Compression Complete option definitions All compression mode and level fields are listed in the Configuration Reference . QuadQR keeps compression as an internal transport detail around normal text or byte payloads. It does not introduce a payload type system, and the decompressor does not need to know which compression level was used. Modes JS Copy encodeText(text, { compression: \"none\" }); encodeText(text, { compression: \"auto\" }); encodeText(text, { compression: \"smart\" }); encodeText(text, { compression: \"brotli\" }); encodeText(text, { compression: \"deflate\" }); encodeText(text, { compression: \"lz\" }); none stores the application payload directly. auto is the fast balanced mode. It performs one pass with LZ level 6, DEFLATE level 6, and Brotli quality 6, compares the complete stored sizes, and keeps the smallest result. smart is the opt-in CPU-heavy mode. It starts with the same balanced candidates as auto , checks the resulting QuadQR version, and only spends more CPU on stronger DEFLATE/Brotli passes when a smaller physical QuadQR is realistically reachable. brotli forces the bundled Brotli codec. deflate forces the portable raw-DEFLATE codec. lz forces the original QuadQR LZSS-style stream for compatibility and testing. For unsigned payloads, auto and smart include the 16-byte internal extension-envelope cost when deciding whether compression helps. If compression would not make the complete stored representation smaller, the original payload is kept with no compression envelope. Signed payloads already require the extension envelope, so their comparison includes the fixed signing metadata automatically. Secure payload planning also includes the fixed encryption-envelope overhead when Smart evaluates version boundaries. Auto versus Smart auto intentionally avoids expensive maximum-quality passes: TEXT Copy Raw LZ level 6 DEFLATE level 6 Brotli quality 6 ↓ smallest final representation smart begins the same way, then considers the next smaller QuadQR version. When that boundary is close enough to be plausible, it escalates in stages: TEXT Copy Balanced pass LZ 6 DEFLATE 6 Brotli 6 ↓ Is a smaller QuadQR version realistically reachable? ↓ yes Strong pass DEFLATE 8 Brotli 9 ↓ Still close to another/same smaller-version boundary? ↓ yes Maximum pass DEFLATE 9 Brotli 11 Smart is deliberately CPU-heavy and should be used when minimizing the physical matrix matters more than generation time. If an exact version is requested and the balanced pass does not fit, Smart may also escalate to stronger levels when that can plausibly make the requested version fit. The current plausibility gates are intentionally conservative: the strong stage is considered when the next boundary is within 30% of the current stored size or 192 bytes, and the maximum stage when it is within 16% or 96 bytes. The demo runs Smart in a Web Worker so the browser UI remains responsive. The generated code object includes generation-only diagnostics such as compressionLevel , compressionStrategy , and smartCompression . Compression level is not stored in the QuadQR because LZ, DEFLATE, and Brotli decoders do not need it. Explicit compression levels Use the generic compressionLevel option when you explicitly select LZ, DEFLATE, or Brotli: JS Copy const lz = encodeText(text, { compression: \"lz\", compressionLevel: 9 }); const deflated = encodeText(text, { compression: \"deflate\", compressionLevel: 9 }); const brotlied = encodeText(text, { compression: \"brotli\", compressionLevel: 11 }); Ranges and defaults: Algorithm Range Explicit default Legacy LZ 1..9 6 DEFLATE 1..9 6 Brotli 0..11 11 Algorithm-specific aliases are also accepted: lzLevel , deflateLevel , and brotliQuality . compressionLevel is the recommended public option. Levels only affect encoder CPU/search effort and output size. They do not change the compression ID or decoder behavior. Legacy LZ profile Compression ID 1 keeps the original QuadQR LZSS-style wire format. Levels 1..9 only control how much encoder work is spent walking candidate history and, at stronger levels, bounded lazy-match lookahead. Level 6 preserves the historical 32-candidate QuadQR search depth, so existing callers that do not pass a level keep the same default behavior. The direct helpers are: JS Copy compressPayload(bytes, { level: 9 }); decompressPayload(compressedBytes, originalLength); The LZ window remains 4095 bytes and each back-reference still represents a 3..18 byte match. The compressed stream format is identical at every level, so the level is never serialized and old/new LZ decoders remain compatible. Auto and Smart use LZ level 6; Smart does not escalate LZ because its additional staged CPU budget is reserved for the stronger DEFLATE/Brotli candidates. Brotli profile Compression ID 3 is a standard Brotli stream. QuadQR vendors the codec into the library so Brotli compression and decompression are synchronous and available in browser ESM, the classic browser bundle, Web Workers, and server-side Node.js without a runtime package or native binding. The direct helpers are: JS Copy compressBrotliPayload(bytes, { quality: 9 }); decompressBrotliPayload(compressedBytes, originalLength); Brotli quality accepts integers 0..11 . Explicit compression: \"brotli\" defaults to quality 11 for backward compatibility. auto starts at quality 6. smart starts at 6 and may test 9 and 11 when a smaller QuadQR version is plausible. Portable DEFLATE profile Compression ID 2 is a raw RFC 1951 DEFLATE stream. QuadQR's bundled encoder uses a fixed-Huffman block with level-dependent LZ77 match-search effort. It supports: a 32 KiB LZ77 window; match distances up to 32768 bytes; match lengths up to 258 bytes; DEFLATE levels 1..9 ; progressively deeper candidate search and lazy matching at stronger levels; deterministic synchronous output; no runtime dependency. The direct helpers are: JS Copy compressDeflatePayload(bytes, { level: 9 }); decompressDeflatePayload(compressedBytes, originalLength); The stream remains standard raw DEFLATE at every level. The level is not serialized. Node.js and browser portability All payload compression paths are bundled JavaScript. The library does not import node:zlib , use browser CompressionStream , depend on DOM APIs, or fetch a codec at runtime. JS Copy import { encodeText, decodeMatrix } from \"quadqr-js\"; const code = encodeText(\"hello \".repeat(1000), { compression: \"smart\" }); const decoded = decodeMatrix(code.matrix); console.log(code.compression, code.compressionLevel); console.log(decoded.compression); // decoder only needs the algorithm ID Compression IDs and compatibility The internal envelope keeps the stable IDs: TEXT Copy 0 = none 1 = legacy LZ 2 = raw DEFLATE 3 = Brotli Compression level is intentionally not part of the envelope. Existing LZ, DEFLATE, and Brotli decoders remain compatible with streams produced at any supported level. Previous ← Scanning & Camera Next Secure Payloads → QuadQR Experimental RGBW matrix symbology. AGPL-3.0 licensed"},{"title":"Modes","page":"Compression","url":"compression.html#modes","text":"Modes JS Copy encodeText(text, { compression: \"none\" }); encodeText(text, { compression: \"auto\" }); encodeText(text, { compression: \"smart\" }); encodeText(text, { compression: \"brotli\" }); encodeText(text, { compression: \"deflate\" }); encodeText(text, { compression: \"lz\" }); none stores the application payload directly. auto is the fast balanced mode. It performs one pass with LZ level 6, DEFLATE level 6, and Brotli quality 6, compares the complete stored sizes, and keeps the smallest result. smart is the opt-in CPU-heavy mode. It starts with the same balanced candidates as auto , checks the resulting QuadQR version, and only spends more CPU on stronger DEFLATE/Brotli passes when a smaller physical QuadQR is realistically reachable. brotli forces the bundled Brotli codec. deflate forces the portable raw-DEFLATE codec. lz forces the original QuadQR LZSS-style stream for compatibility and testing. For unsigned payloads, auto and smart include the 16-byte internal extension-envelope cost when deciding whether compression helps. If compression would not make the complete stored representation smaller, the original payload is kept with no compression envelope. Signed payloads already require the extension envelope, so their comparison includes the fixed signing metadata automatically. Secure payload planning also includes the fixed encryption-envelope overhead when Smart evaluates version boundaries."},{"title":"Auto versus Smart","page":"Compression","url":"compression.html#auto-versus-smart","text":"Auto versus Smart auto intentionally avoids expensive maximum-quality passes: TEXT Copy Raw LZ level 6 DEFLATE level 6 Brotli quality 6 ↓ smallest final representation smart begins the same way, then considers the next smaller QuadQR version. When that boundary is close enough to be plausible, it escalates in stages: TEXT Copy Balanced pass LZ 6 DEFLATE 6 Brotli 6 ↓ Is a smaller QuadQR version realistically reachable? ↓ yes Strong pass DEFLATE 8 Brotli 9 ↓ Still close to another/same smaller-version boundary? ↓ yes Maximum pass DEFLATE 9 Brotli 11 Smart is deliberately CPU-heavy and should be used when minimizing the physical matrix matters more than generation time. If an exact version is requested and the balanced pass does not fit, Smart may also escalate to stronger levels when that can plausibly make the requested version fit. The current plausibility gates are intentionally conservative: the strong stage is considered when the next boundary is within 30% of the current stored size or 192 bytes, and the maximum stage when it is within 16% or 96 bytes. The demo runs Smart in a Web Worker so the browser UI remains responsive. The generated code object includes generation-only diagnostics such as compressionLevel , compressionStrategy , and smartCompression . Compression level is not stored in the QuadQR because LZ, DEFLATE, and Brotli decoders do not need it."},{"title":"Explicit compression levels","page":"Compression","url":"compression.html#explicit-compression-levels","text":"Explicit compression levels Use the generic compressionLevel option when you explicitly select LZ, DEFLATE, or Brotli: JS Copy const lz = encodeText(text, { compression: \"lz\", compressionLevel: 9 }); const deflated = encodeText(text, { compression: \"deflate\", compressionLevel: 9 }); const brotlied = encodeText(text, { compression: \"brotli\", compressionLevel: 11 }); Ranges and defaults: Algorithm Range Explicit default Legacy LZ 1..9 6 DEFLATE 1..9 6 Brotli 0..11 11 Algorithm-specific aliases are also accepted: lzLevel , deflateLevel , and brotliQuality . compressionLevel is the recommended public option. Levels only affect encoder CPU/search effort and output size. They do not change the compression ID or decoder behavior."},{"title":"Legacy LZ profile","page":"Compression","url":"compression.html#legacy-lz-profile","text":"Legacy LZ profile Compression ID 1 keeps the original QuadQR LZSS-style wire format. Levels 1..9 only control how much encoder work is spent walking candidate history and, at stronger levels, bounded lazy-match lookahead. Level 6 preserves the historical 32-candidate QuadQR search depth, so existing callers that do not pass a level keep the same default behavior. The direct helpers are: JS Copy compressPayload(bytes, { level: 9 }); decompressPayload(compressedBytes, originalLength); The LZ window remains 4095 bytes and each back-reference still represents a 3..18 byte match. The compressed stream format is identical at every level, so the level is never serialized and old/new LZ decoders remain compatible. Auto and Smart use LZ level 6; Smart does not escalate LZ because its additional staged CPU budget is reserved for the stronger DEFLATE/Brotli candidates."},{"title":"Brotli profile","page":"Compression","url":"compression.html#brotli-profile","text":"Brotli profile Compression ID 3 is a standard Brotli stream. QuadQR vendors the codec into the library so Brotli compression and decompression are synchronous and available in browser ESM, the classic browser bundle, Web Workers, and server-side Node.js without a runtime package or native binding. The direct helpers are: JS Copy compressBrotliPayload(bytes, { quality: 9 }); decompressBrotliPayload(compressedBytes, originalLength); Brotli quality accepts integers 0..11 . Explicit compression: \"brotli\" defaults to quality 11 for backward compatibility. auto starts at quality 6. smart starts at 6 and may test 9 and 11 when a smaller QuadQR version is plausible."},{"title":"Portable DEFLATE profile","page":"Compression","url":"compression.html#portable-deflate-profile","text":"Portable DEFLATE profile Compression ID 2 is a raw RFC 1951 DEFLATE stream. QuadQR's bundled encoder uses a fixed-Huffman block with level-dependent LZ77 match-search effort. It supports: a 32 KiB LZ77 window; match distances up to 32768 bytes; match lengths up to 258 bytes; DEFLATE levels 1..9 ; progressively deeper candidate search and lazy matching at stronger levels; deterministic synchronous output; no runtime dependency. The direct helpers are: JS Copy compressDeflatePayload(bytes, { level: 9 }); decompressDeflatePayload(compressedBytes, originalLength); The stream remains standard raw DEFLATE at every level. The level is not serialized."},{"title":"Node.js and browser portability","page":"Compression","url":"compression.html#nodejs-and-browser-portability","text":"Node.js and browser portability All payload compression paths are bundled JavaScript. The library does not import node:zlib , use browser CompressionStream , depend on DOM APIs, or fetch a codec at runtime. JS Copy import { encodeText, decodeMatrix } from \"quadqr-js\"; const code = encodeText(\"hello \".repeat(1000), { compression: \"smart\" }); const decoded = decodeMatrix(code.matrix); console.log(code.compression, code.compressionLevel); console.log(decoded.compression); // decoder only needs the algorithm ID"},{"title":"Compression IDs and compatibility","page":"Compression","url":"compression.html#compression-ids-and-compatibility","text":"Compression IDs and compatibility The internal envelope keeps the stable IDs: TEXT Copy 0 = none 1 = legacy LZ 2 = raw DEFLATE 3 = Brotli Compression level is intentionally not part of the envelope. Existing LZ, DEFLATE, and Brotli decoders remain compatible with streams produced at any supported level. Previous ← Scanning & Camera Next Secure Payloads → QuadQR Experimental RGBW matrix symbology. AGPL-3.0 licensed"},{"title":"Format Reference","page":"Format Reference","url":"format.html","text":"QUADQR DOCUMENTATION Format Reference Status Experimental custom symbology. It is not ISO QR Code. Normal mode uses the default 4-state RGBW data cells. An optional experimental High Density Mode uses the 16-state Triangle16 physical layout for payload/body cells. Earlier RGB/ternary formats are intentionally not supported. Matrix sizes TEXT Copy size = 21 + 4 * (version - 1) Supported versions: 1 through 40. Cell alphabet Structural black: TEXT Copy BLACK = -1 Four data states: Cell 2-bit value Internal value Red 00 0 Green 01 1 Blue 10 2 White 11 3 Structural white and data white intentionally share the same visible/internal value. Reserved-position geometry distinguishes their roles. High Density Mode (Triangle16) High Density Mode is identified by header flag bit 6 and implemented with Triangle16 cells. Each non-reserved payload module is split by a fixed / diagonal into an upper-left triangle and a lower-right triangle. Each triangle independently uses the RGBW 2-bit alphabet. The packed internal value is: TEXT Copy cell = (upperLeftColor << 2) | lowerRightColor range = 0..15 Therefore Triangle16 has 16 visible states and carries 4 raw bits per body data cell. Same-color pairs such as R/R or B/B render as a visually solid module. Structural modules never use Triangle16 packing. Finder structures Three 7×7 black/white finder structures are placed at top-left, top-right, and bottom-left, with white separator cells where they fit inside the matrix. Alignment patterns QuadQR always keeps exactly three primary 7×7 finder patterns. Larger versions do not add more primary finders. Instead, versions 2 through 40 use distributed black/white alignment markers following the same center-position schedule used by standard QR Code versions. Format v6 uses a full 5×5 nested black/white/black alignment eye at every scheduled alignment position , giving the detector a stronger structural signature across large and perspective-distorted symbols. The three alignment positions that would overlap the primary finder corners are omitted. This produces progressively more alignment references as the matrix grows. Examples: TEXT Copy v2 -> 1 alignment pattern v7 -> 6 alignment patterns v14 -> 13 alignment patterns v28 -> 33 alignment patterns v40 -> 46 alignment patterns Version 1 is a QuadQR-specific exception. Standard QR v1 has no alignment pattern, but QuadQR keeps one legacy 5×5 bottom-right bootstrap alignment marker with a one-cell white separator so the camera scanner still has a fourth projective reference point. For versions 2 through 40, the scanner uses the bottom-right 5×5 member of the distributed alignment grid as the primary fourth homography reference, then scores and refines against the full 5×5 alignment grid. A fast nested-eye locator is attempted before broader template recovery so perspective geometry can be established as soon as the three primary finder eyes are visible. Timing structures Alternating black/white timing cells use row 6 and column 6 between the main finder regions. Color calibration Twelve reserved cells provide three 2×2 color patches: 4 red cells 4 green cells 4 blue cells Known finder/separator/alignment cells provide black and white references. Therefore white is calibrated without requiring a separate white swatch. Data placement Data positions use a two-column vertical zig-zag beginning at the bottom-right. Reserved finder, separator, timing, alignment, and calibration cells are skipped. Byte-to-cell mapping Each byte is serialized most-significant pair first: TEXT Copy bits 7..6 -> cell 0 bits 5..4 -> cell 1 bits 3..2 -> cell 2 bits 1..0 -> cell 3 Example: TEXT Copy 11001001 11 00 10 01 W R B G Therefore: TEXT Copy 1 byte = exactly 4 RGBW data cells 1 RGBW data cell = exactly 2 raw bits For Triangle16 ECC/body bytes: TEXT Copy bits 7..4 -> Triangle16 cell 0 bits 3..0 -> Triangle16 cell 1 1 byte = exactly 2 Triangle16 body cells 1 Triangle16 body cell = exactly 4 raw bits The protected header remains encoded as four solid-color RGBW-equivalent cells per byte even when Triangle16 is selected. Internally those solid header cells are represented as R/R, G/G, B/B, or W/W. This keeps the bootstrap/header substantially easier to recover from blur and perspective distortion. Masks Four masks are defined. Each returns a 2-bit value 0..3: TEXT Copy mask 0 = (row + col) mod 4 mask 1 = (2*row + col) mod 4 mask 2 = (row + 2*col) mod 4 mask 3 = (row*col + row + col) mod 4 Encoding and decoding use XOR: TEXT Copy visible = raw XOR mask raw = visible XOR mask The encoder evaluates all four masks using run-length and color-balance penalties. In Triangle16 body cells, two deterministic 2-bit masks are packed into one 4-bit XOR mask; protected solid-color header cells use the normal RGBW mask on both halves so they remain solid. The mask ID is not serialized. The decoder tries all four and accepts only a path whose protected header, ECC, and CRC validate. Header Versions 2 through 40 The normal logical header is 10 bytes. Offset Size Meaning 0 4 ASCII magic QQRW 4 1 format version 5 5 1 flags 6 4 payload byte length, big-endian It is protected with 8 RS parity bytes: TEXT Copy 10 data bytes + 8 parity bytes = 18 RS bytes 18 bytes * 4 cells/byte = 72 data cells The normal header can correct up to four damaged byte symbols. Version 1 compact header The 21×21 symbol uses a compact 4-byte header because the matrix size already identifies version 1. Offset Size Meaning 0 1 compact format marker 0xC3 1 1 flags 2 1 payload byte length 3 1 payload length XOR 0xFF The compact header is protected with 4 RS parity bytes: TEXT Copy 4 data bytes + 4 parity bytes = 8 RS bytes 8 bytes * 4 cells/byte = 32 data cells It corrects up to two damaged header byte symbols. The complemented length byte provides an additional structural validity check. Flags for both header forms: TEXT Copy bit 0 UTF-8 text flag bits 1..2 ECC profile id bit 3 Secure Payload envelope flag bit 4 internal payload-extension metadata present bit 5 signed-payload hint bit 6 High Density Mode flag bit 7 reserved ECC ids: TEXT Copy 0 = L 1 = M 2 = Q 3 = H Secure Payload v1 When header flag bit 3 is set, the body payload bytes contain a versioned encrypted envelope instead of plaintext application bytes. Spectrum ECC and CRC operate on the envelope exactly like any other byte payload. The envelope begins with this fixed 24-byte metadata block: Offset Size Field 0 4 ASCII magic QSEC 4 1 Secure Payload version ( 1 ) 5 1 Mode ( 1 password, 2 raw 256-bit key) 6 1 Algorithm ( 1 AES-256-GCM) 7 1 KDF ( 1 PBKDF2-HMAC-SHA-256, 0 none) 8 1 Security flags 9 1 Salt length 10 1 Nonce length 11 1 Authentication-tag length 12 1 Key-ID length 13 3 Reserved, zero 16 4 PBKDF2 iterations, big-endian ( 0 for raw-key mode) 20 4 Plaintext byte length, big-endian The fixed header is followed by: TEXT Copy keyId || salt || nonce || ciphertext || GCM tag Current required sizes: TEXT Copy AES-GCM nonce = 12 bytes AES-GCM tag = 16 bytes password salt = 16 bytes Password mode Password bytes are UTF-8 encoded and processed with: TEXT Copy PBKDF2-HMAC-SHA-256 output = 256-bit AES key default iterations = 600000 A fresh random 16-byte salt and 12-byte nonce are generated for every encryption operation. Raw 256-bit key mode The application supplies an exact 32-byte key. No password KDF or salt is used. Unless disabled or overridden, the encoder stores the first 8 bytes of SHA-256(rawKey) as a non-secret key fingerprint. This key ID is only a routing hint and is not sufficient to decrypt the payload. Authentication AES-256-GCM additional authenticated data (AAD) is: TEXT Copy fixed security header || keyId || salt || nonce Therefore the mode, KDF settings, key ID, salt, nonce, plaintext length, ciphertext, and authentication tag are cryptographically bound. Decryption must fail if the password/key is wrong or authenticated envelope data was changed. The QuadQR CRC still protects the decoded encrypted envelope against scanner/ECC corruption before any decryption is attempted. AES-GCM authentication then protects the secure payload cryptographically. Payload and CRC CRC-32 is calculated over: TEXT Copy header || payload Four CRC bytes are appended to the payload before body ECC. GF(256) Reed-Solomon Header and body ECC use: TEXT Copy GF(2^8) = GF(256) Primitive polynomial: TEXT Copy x^8 + x^4 + x^3 + x^2 + 1 0x11d A codeword is limited to 255 byte symbols. ECC profiles Versions 2 through 40 Profile Parity bytes per body block Correctable byte symbols per block L 12 6 M 24 12 Q 36 18 H 48 24 Version 1 compact body ECC Profile Parity bytes Correctable byte symbols L 4 2 M 8 4 Q 12 6 H 16 8 This scaling prevents fixed parity overhead from consuming the entire 21×21 symbol. With the unchanged geometry and CRC-32, v1-M now carries 24 user payload bytes. For each profile: TEXT Copy max data bytes per block = 255 - parityBytes Larger bodies are split into multiple RS blocks. Interleaving Reed-Solomon block interleaving Encoded RS blocks are interleaved column-wise: TEXT Copy block0[0], block1[0], block2[0], ..., block0[1], block1[1], block2[1], ... The decoder derives block lengths from payload length and ECC profile, reverses the interleaving, and corrects each block independently. Spectral-spatial cell interleaving After bytes are split into four 2-bit RGBW cells, the complete logical data-cell stream is mapped through a deterministic version/length-dependent permutation before being written to physical data positions. The permutation is a seeded Fisher-Yates shuffle and contains every physical data-position index exactly once. This layer has zero capacity overhead : no extra cells or parity symbols are added. Its purpose is to spread adjacent logical codeword cells across distant physical modules so localized damage is distributed over many RS symbols. Masking is applied using the final physical row/column position. Decoding therefore performs operations in this order: TEXT Copy physical sampled cells -> physical-position unmasking -> reverse spectral-spatial permutation -> byte reconstruction -> RS block deinterleaving The decoder also tries legacy physical order as a fallback for older QuadQR matrices. Padding Unused data positions are filled with deterministic pseudo-random values in the range 0..3. Padding is not semantically decoded. Spectrum ECC 2.0 confidence-aware and soft decoding For image/camera scans, classification retains more than the winning RGBW/Triangle16 state. Each sampled data cell receives a confidence score and a bounded second hypothesis derived from the calibrated palette classification. Triangle16 also incorporates the measured disagreement between its three interior samples per triangle so edge bleed or sub-module geometry instability lowers confidence instead of becoming false certainty. In normal RGBW mode, one GF(256) byte symbol corresponds to four 2-bit cells. In High Density Mode, a body byte corresponds to two 4-bit Triangle16 cells, while the protected header remains four solid RGBW-equivalent cells per byte. Symbol confidence is the minimum confidence of its constituent cells because an error in any constituent cell changes the reconstructed byte. Decoding first attempts normal hard-decision Reed-Solomon correction. If that fails, low-confidence byte positions are progressively promoted to known erasures and the decoder retries error/erasure RS correction. If that still fails and scanner second hypotheses are available, Spectrum ECC 2.0 performs a bounded Chase-style search over the least-confident cells, trying single alternate substitutions and then a small pair set. Every candidate is passed through the unchanged RS and CRC validation path. The RS budget follows the usual error/erasure relationship: TEXT Copy 2 * unknownErrors + knownErasures <= paritySymbols No additional parity is added for this feature, so payload capacity is unchanged. Scanner pipeline TEXT Copy RGB frame -> grayscale conversion -> global Otsu threshold for structural detection -> 1:1:3:1:1 finder candidate detection -> three-finder geometric ordering -> candidate version estimation -> primary bottom-right alignment search -> four-point homography -> distributed alignment-grid validation -> projective module sampling -> observed black/white/R/G/B calibration -> observed-palette / white-balance / affine calibration attempts -> RGBW or Triangle16 classification + confidence + second hypothesis -> four-state XOR unmasking -> reverse spectral-spatial permutation -> protected header GF(256) hard RS decode -> confidence-guided error/erasure retry when needed -> bounded second-hypothesis soft retry when needed -> body block deinterleaving -> body GF(256) error/erasure correction -> CRC-32 verification The scanner also has an axis-aligned fallback. Rotation Matrix decoding tries 0°, 90°, 180°, and 270° rotations. Compatibility Format v6 is intentionally incompatible with standard QR scanners and with the project's older ternary prototypes. The current encoder writes codeword cells using spectral-spatial placement and uses 5×5 nested alignment eyes at every scheduled alignment position. Format v5 used compact 3×3 secondary alignment markers while retaining the bottom-right primary marker at 5×5. The current decoder keeps a legacy Format v5 alignment-profile fallback, so existing v5 RGBW QuadQR symbols remain readable. The decoder also retains the pre-interleaver physical-order fallback for older RGBW matrices. Rendering profiles are not part of the wire format The canonical QuadQR matrix is independent of presentation style. Renderers may offer styles such as classic , depth , soft , or inset , provided structural finder/timing/alignment/calibration references remain sufficiently faithful for decoding. Style selection is not encoded in the payload/header and does not change matrix cell values. Compression and signature flags Format v5 keeps the physical matrix and ECC framing unchanged while reserving two protected-header flags for optional internal payload metadata: TEXT Copy bit 4 = internal payload-extension metadata present bit 5 = signed-payload hint These bits are not a public payload-type mode. Applications continue to encode normal UTF-8 text or arbitrary bytes. Compression 3.0 (fast Auto, CPU-heavy Smart, explicit LZ/DEFLATE/Brotli levels with legacy LZ wire compatibility) and Ed25519 signing use a compact internal extension envelope only when metadata is required. The envelope, print profile, diagnostics, and scanability test model are specified in technical specification . None of these features changes RGBW cell mapping or Spectrum ECC. Previous ← WebAssembly Next Technical Specification → QuadQR Experimental RGBW matrix symbology. AGPL-3.0 licensed"},{"title":"Status","page":"Format Reference","url":"format.html#status","text":"Status Experimental custom symbology. It is not ISO QR Code. Normal mode uses the default 4-state RGBW data cells. An optional experimental High Density Mode uses the 16-state Triangle16 physical layout for payload/body cells. Earlier RGB/ternary formats are intentionally not supported."},{"title":"Matrix sizes","page":"Format Reference","url":"format.html#matrix-sizes","text":"Matrix sizes TEXT Copy size = 21 + 4 * (version - 1) Supported versions: 1 through 40."},{"title":"Cell alphabet","page":"Format Reference","url":"format.html#cell-alphabet","text":"Cell alphabet Structural black: TEXT Copy BLACK = -1 Four data states: Cell 2-bit value Internal value Red 00 0 Green 01 1 Blue 10 2 White 11 3 Structural white and data white intentionally share the same visible/internal value. Reserved-position geometry distinguishes their roles. High Density Mode (Triangle16) # High Density Mode is identified by header flag bit 6 and implemented with Triangle16 cells. Each non-reserved payload module is split by a fixed / diagonal into an upper-left triangle and a lower-right triangle. Each triangle independently uses the RGBW 2-bit alphabet. The packed internal value is: TEXT Copy cell = (upperLeftColor << 2) | lowerRightColor range = 0..15 Therefore Triangle16 has 16 visible states and carries 4 raw bits per body data cell. Same-color pairs such as R/R or B/B render as a visually solid module. Structural modules never use Triangle16 packing."},{"title":"High Density Mode (Triangle16)","page":"Format Reference","url":"format.html#high-density-mode-triangle16","text":"High Density Mode (Triangle16) High Density Mode is identified by header flag bit 6 and implemented with Triangle16 cells. Each non-reserved payload module is split by a fixed / diagonal into an upper-left triangle and a lower-right triangle. Each triangle independently uses the RGBW 2-bit alphabet. The packed internal value is: TEXT Copy cell = (upperLeftColor << 2) | lowerRightColor range = 0..15 Therefore Triangle16 has 16 visible states and carries 4 raw bits per body data cell. Same-color pairs such as R/R or B/B render as a visually solid module. Structural modules never use Triangle16 packing."},{"title":"Finder structures","page":"Format Reference","url":"format.html#finder-structures","text":"Finder structures Three 7×7 black/white finder structures are placed at top-left, top-right, and bottom-left, with white separator cells where they fit inside the matrix."},{"title":"Alignment patterns","page":"Format Reference","url":"format.html#alignment-patterns","text":"Alignment patterns QuadQR always keeps exactly three primary 7×7 finder patterns. Larger versions do not add more primary finders. Instead, versions 2 through 40 use distributed black/white alignment markers following the same center-position schedule used by standard QR Code versions. Format v6 uses a full 5×5 nested black/white/black alignment eye at every scheduled alignment position , giving the detector a stronger structural signature across large and perspective-distorted symbols. The three alignment positions that would overlap the primary finder corners are omitted. This produces progressively more alignment references as the matrix grows. Examples: TEXT Copy v2 -> 1 alignment pattern v7 -> 6 alignment patterns v14 -> 13 alignment patterns v28 -> 33 alignment patterns v40 -> 46 alignment patterns Version 1 is a QuadQR-specific exception. Standard QR v1 has no alignment pattern, but QuadQR keeps one legacy 5×5 bottom-right bootstrap alignment marker with a one-cell white separator so the camera scanner still has a fourth projective reference point. For versions 2 through 40, the scanner uses the bottom-right 5×5 member of the distributed alignment grid as the primary fourth homography reference, then scores and refines against the full 5×5 alignment grid. A fast nested-eye locator is attempted before broader template recovery so perspective geometry can be established as soon as the three primary finder eyes are visible."},{"title":"Timing structures","page":"Format Reference","url":"format.html#timing-structures","text":"Timing structures Alternating black/white timing cells use row 6 and column 6 between the main finder regions."},{"title":"Color calibration","page":"Format Reference","url":"format.html#color-calibration","text":"Color calibration Twelve reserved cells provide three 2×2 color patches: 4 red cells 4 green cells 4 blue cells Known finder/separator/alignment cells provide black and white references. Therefore white is calibrated without requiring a separate white swatch."},{"title":"Data placement","page":"Format Reference","url":"format.html#data-placement","text":"Data placement Data positions use a two-column vertical zig-zag beginning at the bottom-right. Reserved finder, separator, timing, alignment, and calibration cells are skipped."},{"title":"Byte-to-cell mapping","page":"Format Reference","url":"format.html#byte-to-cell-mapping","text":"Byte-to-cell mapping Each byte is serialized most-significant pair first: TEXT Copy bits 7..6 -> cell 0 bits 5..4 -> cell 1 bits 3..2 -> cell 2 bits 1..0 -> cell 3 Example: TEXT Copy 11001001 11 00 10 01 W R B G Therefore: TEXT Copy 1 byte = exactly 4 RGBW data cells 1 RGBW data cell = exactly 2 raw bits For Triangle16 ECC/body bytes: TEXT Copy bits 7..4 -> Triangle16 cell 0 bits 3..0 -> Triangle16 cell 1 1 byte = exactly 2 Triangle16 body cells 1 Triangle16 body cell = exactly 4 raw bits The protected header remains encoded as four solid-color RGBW-equivalent cells per byte even when Triangle16 is selected. Internally those solid header cells are represented as R/R, G/G, B/B, or W/W. This keeps the bootstrap/header substantially easier to recover from blur and perspective distortion."},{"title":"Masks","page":"Format Reference","url":"format.html#masks","text":"Masks Four masks are defined. Each returns a 2-bit value 0..3: TEXT Copy mask 0 = (row + col) mod 4 mask 1 = (2*row + col) mod 4 mask 2 = (row + 2*col) mod 4 mask 3 = (row*col + row + col) mod 4 Encoding and decoding use XOR: TEXT Copy visible = raw XOR mask raw = visible XOR mask The encoder evaluates all four masks using run-length and color-balance penalties. In Triangle16 body cells, two deterministic 2-bit masks are packed into one 4-bit XOR mask; protected solid-color header cells use the normal RGBW mask on both halves so they remain solid. The mask ID is not serialized. The decoder tries all four and accepts only a path whose protected header, ECC, and CRC validate."},{"title":"Header","page":"Format Reference","url":"format.html#header","text":"Header Versions 2 through 40 # The normal logical header is 10 bytes. Offset Size Meaning 0 4 ASCII magic QQRW 4 1 format version 5 5 1 flags 6 4 payload byte length, big-endian It is protected with 8 RS parity bytes: TEXT Copy 10 data bytes + 8 parity bytes = 18 RS bytes 18 bytes * 4 cells/byte = 72 data cells The normal header can correct up to four damaged byte symbols. Version 1 compact header # The 21×21 symbol uses a compact 4-byte header because the matrix size already identifies version 1. Offset Size Meaning 0 1 compact format marker 0xC3 1 1 flags 2 1 payload byte length 3 1 payload length XOR 0xFF The compact header is protected with 4 RS parity bytes: TEXT Copy 4 data bytes + 4 parity bytes = 8 RS bytes 8 bytes * 4 cells/byte = 32 data cells It corrects up to two damaged header byte symbols. The complemented length byte provides an additional structural validity check. Flags for both header forms: TEXT Copy bit 0 UTF-8 text flag bits 1..2 ECC profile id bit 3 Secure Payload envelope flag bit 4 internal payload-extension metadata present bit 5 signed-payload hint bit 6 High Density Mode flag bit 7 reserved ECC ids: TEXT Copy 0 = L 1 = M 2 = Q 3 = H"},{"title":"Versions 2 through 40","page":"Format Reference","url":"format.html#versions-2-through-40","text":"Versions 2 through 40 The normal logical header is 10 bytes. Offset Size Meaning 0 4 ASCII magic QQRW 4 1 format version 5 5 1 flags 6 4 payload byte length, big-endian It is protected with 8 RS parity bytes: TEXT Copy 10 data bytes + 8 parity bytes = 18 RS bytes 18 bytes * 4 cells/byte = 72 data cells The normal header can correct up to four damaged byte symbols."},{"title":"Version 1 compact header","page":"Format Reference","url":"format.html#version-1-compact-header","text":"Version 1 compact header The 21×21 symbol uses a compact 4-byte header because the matrix size already identifies version 1. Offset Size Meaning 0 1 compact format marker 0xC3 1 1 flags 2 1 payload byte length 3 1 payload length XOR 0xFF The compact header is protected with 4 RS parity bytes: TEXT Copy 4 data bytes + 4 parity bytes = 8 RS bytes 8 bytes * 4 cells/byte = 32 data cells It corrects up to two damaged header byte symbols. The complemented length byte provides an additional structural validity check. Flags for both header forms: TEXT Copy bit 0 UTF-8 text flag bits 1..2 ECC profile id bit 3 Secure Payload envelope flag bit 4 internal payload-extension metadata present bit 5 signed-payload hint bit 6 High Density Mode flag bit 7 reserved ECC ids: TEXT Copy 0 = L 1 = M 2 = Q 3 = H"},{"title":"Secure Payload v1","page":"Format Reference","url":"format.html#secure-payload-v1","text":"Secure Payload v1 When header flag bit 3 is set, the body payload bytes contain a versioned encrypted envelope instead of plaintext application bytes. Spectrum ECC and CRC operate on the envelope exactly like any other byte payload. The envelope begins with this fixed 24-byte metadata block: Offset Size Field 0 4 ASCII magic QSEC 4 1 Secure Payload version ( 1 ) 5 1 Mode ( 1 password, 2 raw 256-bit key) 6 1 Algorithm ( 1 AES-256-GCM) 7 1 KDF ( 1 PBKDF2-HMAC-SHA-256, 0 none) 8 1 Security flags 9 1 Salt length 10 1 Nonce length 11 1 Authentication-tag length 12 1 Key-ID length 13 3 Reserved, zero 16 4 PBKDF2 iterations, big-endian ( 0 for raw-key mode) 20 4 Plaintext byte length, big-endian The fixed header is followed by: TEXT Copy keyId || salt || nonce || ciphertext || GCM tag Current required sizes: TEXT Copy AES-GCM nonce = 12 bytes AES-GCM tag = 16 bytes password salt = 16 bytes Password mode # Password bytes are UTF-8 encoded and processed with: TEXT Copy PBKDF2-HMAC-SHA-256 output = 256-bit AES key default iterations = 600000 A fresh random 16-byte salt and 12-byte nonce are generated for every encryption operation. Raw 256-bit key mode # The application supplies an exact 32-byte key. No password KDF or salt is used. Unless disabled or overridden, the encoder stores the first 8 bytes of SHA-256(rawKey) as a non-secret key fingerprint. This key ID is only a routing hint and is not sufficient to decrypt the payload. Authentication # AES-256-GCM additional authenticated data (AAD) is: TEXT Copy fixed security header || keyId || salt || nonce Therefore the mode, KDF settings, key ID, salt, nonce, plaintext length, ciphertext, and authentication tag are cryptographically bound. Decryption must fail if the password/key is wrong or authenticated envelope data was changed. The QuadQR CRC still protects the decoded encrypted envelope against scanner/ECC corruption before any decryption is attempted. AES-GCM authentication then protects the secure payload cryptographically."},{"title":"Password mode","page":"Format Reference","url":"format.html#password-mode","text":"Password mode Password bytes are UTF-8 encoded and processed with: TEXT Copy PBKDF2-HMAC-SHA-256 output = 256-bit AES key default iterations = 600000 A fresh random 16-byte salt and 12-byte nonce are generated for every encryption operation."},{"title":"Raw 256-bit key mode","page":"Format Reference","url":"format.html#raw-256-bit-key-mode","text":"Raw 256-bit key mode The application supplies an exact 32-byte key. No password KDF or salt is used. Unless disabled or overridden, the encoder stores the first 8 bytes of SHA-256(rawKey) as a non-secret key fingerprint. This key ID is only a routing hint and is not sufficient to decrypt the payload."},{"title":"Authentication","page":"Format Reference","url":"format.html#authentication","text":"Authentication AES-256-GCM additional authenticated data (AAD) is: TEXT Copy fixed security header || keyId || salt || nonce Therefore the mode, KDF settings, key ID, salt, nonce, plaintext length, ciphertext, and authentication tag are cryptographically bound. Decryption must fail if the password/key is wrong or authenticated envelope data was changed. The QuadQR CRC still protects the decoded encrypted envelope against scanner/ECC corruption before any decryption is attempted. AES-GCM authentication then protects the secure payload cryptographically."},{"title":"Payload and CRC","page":"Format Reference","url":"format.html#payload-and-crc","text":"Payload and CRC CRC-32 is calculated over: TEXT Copy header || payload Four CRC bytes are appended to the payload before body ECC."},{"title":"GF(256) Reed-Solomon","page":"Format Reference","url":"format.html#gf256-reed-solomon","text":"GF(256) Reed-Solomon Header and body ECC use: TEXT Copy GF(2^8) = GF(256) Primitive polynomial: TEXT Copy x^8 + x^4 + x^3 + x^2 + 1 0x11d A codeword is limited to 255 byte symbols."},{"title":"ECC profiles","page":"Format Reference","url":"format.html#ecc-profiles","text":"ECC profiles Versions 2 through 40 # Profile Parity bytes per body block Correctable byte symbols per block L 12 6 M 24 12 Q 36 18 H 48 24 Version 1 compact body ECC # Profile Parity bytes Correctable byte symbols L 4 2 M 8 4 Q 12 6 H 16 8 This scaling prevents fixed parity overhead from consuming the entire 21×21 symbol. With the unchanged geometry and CRC-32, v1-M now carries 24 user payload bytes. For each profile: TEXT Copy max data bytes per block = 255 - parityBytes Larger bodies are split into multiple RS blocks."},{"title":"Versions 2 through 40","page":"Format Reference","url":"format.html#versions-2-through-40-2","text":"Versions 2 through 40 Profile Parity bytes per body block Correctable byte symbols per block L 12 6 M 24 12 Q 36 18 H 48 24"},{"title":"Version 1 compact body ECC","page":"Format Reference","url":"format.html#version-1-compact-body-ecc","text":"Version 1 compact body ECC Profile Parity bytes Correctable byte symbols L 4 2 M 8 4 Q 12 6 H 16 8 This scaling prevents fixed parity overhead from consuming the entire 21×21 symbol. With the unchanged geometry and CRC-32, v1-M now carries 24 user payload bytes. For each profile: TEXT Copy max data bytes per block = 255 - parityBytes Larger bodies are split into multiple RS blocks."},{"title":"Interleaving","page":"Format Reference","url":"format.html#interleaving","text":"Interleaving Reed-Solomon block interleaving # Encoded RS blocks are interleaved column-wise: TEXT Copy block0[0], block1[0], block2[0], ..., block0[1], block1[1], block2[1], ... The decoder derives block lengths from payload length and ECC profile, reverses the interleaving, and corrects each block independently. Spectral-spatial cell interleaving # After bytes are split into four 2-bit RGBW cells, the complete logical data-cell stream is mapped through a deterministic version/length-dependent permutation before being written to physical data positions. The permutation is a seeded Fisher-Yates shuffle and contains every physical data-position index exactly once. This layer has zero capacity overhead : no extra cells or parity symbols are added. Its purpose is to spread adjacent logical codeword cells across distant physical modules so localized damage is distributed over many RS symbols. Masking is applied using the final physical row/column position. Decoding therefore performs operations in this order: TEXT Copy physical sampled cells -> physical-position unmasking -> reverse spectral-spatial permutation -> byte reconstruction -> RS block deinterleaving The decoder also tries legacy physical order as a fallback for older QuadQR matrices."},{"title":"Reed-Solomon block interleaving","page":"Format Reference","url":"format.html#reed-solomon-block-interleaving","text":"Reed-Solomon block interleaving Encoded RS blocks are interleaved column-wise: TEXT Copy block0[0], block1[0], block2[0], ..., block0[1], block1[1], block2[1], ... The decoder derives block lengths from payload length and ECC profile, reverses the interleaving, and corrects each block independently."},{"title":"Spectral-spatial cell interleaving","page":"Format Reference","url":"format.html#spectral-spatial-cell-interleaving","text":"Spectral-spatial cell interleaving After bytes are split into four 2-bit RGBW cells, the complete logical data-cell stream is mapped through a deterministic version/length-dependent permutation before being written to physical data positions. The permutation is a seeded Fisher-Yates shuffle and contains every physical data-position index exactly once. This layer has zero capacity overhead : no extra cells or parity symbols are added. Its purpose is to spread adjacent logical codeword cells across distant physical modules so localized damage is distributed over many RS symbols. Masking is applied using the final physical row/column position. Decoding therefore performs operations in this order: TEXT Copy physical sampled cells -> physical-position unmasking -> reverse spectral-spatial permutation -> byte reconstruction -> RS block deinterleaving The decoder also tries legacy physical order as a fallback for older QuadQR matrices."},{"title":"Padding","page":"Format Reference","url":"format.html#padding","text":"Padding Unused data positions are filled with deterministic pseudo-random values in the range 0..3. Padding is not semantically decoded."},{"title":"Spectrum ECC 2.0 confidence-aware and soft decoding","page":"Format Reference","url":"format.html#spectrum-ecc-20-confidence-aware-and-soft-decoding","text":"Spectrum ECC 2.0 confidence-aware and soft decoding For image/camera scans, classification retains more than the winning RGBW/Triangle16 state. Each sampled data cell receives a confidence score and a bounded second hypothesis derived from the calibrated palette classification. Triangle16 also incorporates the measured disagreement between its three interior samples per triangle so edge bleed or sub-module geometry instability lowers confidence instead of becoming false certainty. In normal RGBW mode, one GF(256) byte symbol corresponds to four 2-bit cells. In High Density Mode, a body byte corresponds to two 4-bit Triangle16 cells, while the protected header remains four solid RGBW-equivalent cells per byte. Symbol confidence is the minimum confidence of its constituent cells because an error in any constituent cell changes the reconstructed byte. Decoding first attempts normal hard-decision Reed-Solomon correction. If that fails, low-confidence byte positions are progressively promoted to known erasures and the decoder retries error/erasure RS correction. If that still fails and scanner second hypotheses are available, Spectrum ECC 2.0 performs a bounded Chase-style search over the least-confident cells, trying single alternate substitutions and then a small pair set. Every candidate is passed through the unchanged RS and CRC validation path. The RS budget follows the usual error/erasure relationship: TEXT Copy 2 * unknownErrors + knownErasures <= paritySymbols No additional parity is added for this feature, so payload capacity is unchanged."},{"title":"Scanner pipeline","page":"Format Reference","url":"format.html#scanner-pipeline","text":"Scanner pipeline TEXT Copy RGB frame -> grayscale conversion -> global Otsu threshold for structural detection -> 1:1:3:1:1 finder candidate detection -> three-finder geometric ordering -> candidate version estimation -> primary bottom-right alignment search -> four-point homography -> distributed alignment-grid validation -> projective module sampling -> observed black/white/R/G/B calibration -> observed-palette / white-balance / affine calibration attempts -> RGBW or Triangle16 classification + confidence + second hypothesis -> four-state XOR unmasking -> reverse spectral-spatial permutation -> protected header GF(256) hard RS decode -> confidence-guided error/erasure retry when needed -> bounded second-hypothesis soft retry when needed -> body block deinterleaving -> body GF(256) error/erasure correction -> CRC-32 verification The scanner also has an axis-aligned fallback."},{"title":"Rotation","page":"Format Reference","url":"format.html#rotation","text":"Rotation Matrix decoding tries 0°, 90°, 180°, and 270° rotations."},{"title":"Compatibility","page":"Format Reference","url":"format.html#compatibility","text":"Compatibility Format v6 is intentionally incompatible with standard QR scanners and with the project's older ternary prototypes. The current encoder writes codeword cells using spectral-spatial placement and uses 5×5 nested alignment eyes at every scheduled alignment position. Format v5 used compact 3×3 secondary alignment markers while retaining the bottom-right primary marker at 5×5. The current decoder keeps a legacy Format v5 alignment-profile fallback, so existing v5 RGBW QuadQR symbols remain readable. The decoder also retains the pre-interleaver physical-order fallback for older RGBW matrices."},{"title":"Rendering profiles are not part of the wire format","page":"Format Reference","url":"format.html#rendering-profiles-are-not-part-of-the-wire-format","text":"Rendering profiles are not part of the wire format The canonical QuadQR matrix is independent of presentation style. Renderers may offer styles such as classic , depth , soft , or inset , provided structural finder/timing/alignment/calibration references remain sufficiently faithful for decoding. Style selection is not encoded in the payload/header and does not change matrix cell values."},{"title":"Compression and signature flags","page":"Format Reference","url":"format.html#compression-and-signature-flags","text":"Compression and signature flags Format v5 keeps the physical matrix and ECC framing unchanged while reserving two protected-header flags for optional internal payload metadata: TEXT Copy bit 4 = internal payload-extension metadata present bit 5 = signed-payload hint These bits are not a public payload-type mode. Applications continue to encode normal UTF-8 text or arbitrary bytes. Compression 3.0 (fast Auto, CPU-heavy Smart, explicit LZ/DEFLATE/Brotli levels with legacy LZ wire compatibility) and Ed25519 signing use a compact internal extension envelope only when metadata is required. The envelope, print profile, diagnostics, and scanability test model are specified in technical specification . None of these features changes RGBW cell mapping or Spectrum ECC. Previous ← WebAssembly Next Technical Specification → QuadQR Experimental RGBW matrix symbology. AGPL-3.0 licensed"},{"title":"Getting Started","page":"Getting Started","url":"getting-started.html","text":"QUADQR DOCUMENTATION Getting Started Complete option definitions The quick examples use safe defaults. When you need to tune any encoder, renderer, scanner, camera, security, or compression field, use the Configuration Reference for accepted values and defaults. Install BASH Copy npm install quadqr-js Node.js usage targets Node.js 20.19 or newer. Browser use works through a bundler, direct ESM, or the prebuilt CDN/global bundle. Encode and decode text JS Copy import { encodeText, decodeMatrix } from \"quadqr-js\"; const code = encodeText(\"Hello from QuadQR\", { ecc: \"M\" }); const result = decodeMatrix(code.matrix); console.log(result.text); If no version is supplied, QuadQR automatically chooses the smallest version that fits the payload. Render in the browser JS Copy import { encodeText, renderToCanvas, renderToSVG } from \"quadqr-js/browser\"; const code = encodeText(\"Rendered in the browser\"); renderToCanvas(code, document.querySelector(\"#qr\"), { imageSize: 720, quietZone: 4, style: \"classic\" }); const svg = renderToSVG(code, { imageSize: 720, quietZone: 4 }); Available presentation styles are classic , depth , soft , and inset . Styling does not change the encoded matrix. imageSize is the exact square output dimension in pixels. If you omit both imageSize and moduleSize , the renderer defaults to 720 × 720 px. moduleSize is still available for low-level pixels-per-module sizing. To add a centered logo, load it as an Image and pass it as logo.source . Transparent logo pixels remain transparent. Use clearBackground: true when you want a padded white area behind the logo. quietZone is measured in modules and defaults to 4 . Scan an uploaded image JS Copy import { scanFile } from \"quadqr-js/browser\"; const file = document.querySelector(\"#image\").files[0]; const result = await scanFile(file); console.log(result.text); Scan from the camera JS Copy import { startCameraScanner } from \"quadqr-js/browser\"; let scanner; scanner = await startCameraScanner( document.querySelector(\"#video\"), { cameraFinderRecoveryEvery: 2, onResult(result) { console.log(result); scanner.stop(); } } ); Camera access requires HTTPS or localhost. Finder detection uses a QuadQR-specific RGB value pass first, then throttled threshold recovery when the fast pass cannot lock onto all three finder patterns. Secure password mode Secure payloads are optional. Password mode is the simplest choice when a person will provide the secret during decryption. JS Copy import { encodeSecureText, decodeMatrix, decryptDecoded } from \"quadqr-js\"; const code = await encodeSecureText(\"Private payload\", { ecc: \"M\", security: { mode: \"password\", password: \"correct horse battery staple\" } }); const locked = decodeMatrix(code.matrix); const unlocked = await decryptDecoded(locked, { password: \"correct horse battery staple\" }); console.log(unlocked.text); A secure image or camera scan follows the same pattern: scanning returns a locked result, then decryptDecoded() reveals plaintext only after successful authentication. Secure raw-key mode Use raw-key mode when an application already manages its own cryptographic keys. JS Copy import { encodeSecureText, generateRaw256Key } from \"quadqr-js\"; const key = generateRaw256Key(); const code = await encodeSecureText(\"Application-managed secret\", { security: { mode: \"raw-key\", key } }); The key must be exactly 32 bytes. Never embed the secret key inside the same QuadQR payload. Generate PNG and SVG files in Node.js JS Copy import { encodeText } from \"quadqr-js\"; import { savePNG, saveSVG, scanFile } from \"quadqr-js/node\"; const code = encodeText(\"Generated on Node.js\"); await savePNG(code, \"quadqr.png\", { imageSize: 720, quietZone: 4 }); await saveSVG(code, \"quadqr.svg\", { imageSize: 720, quietZone: 4 }); const result = await scanFile(\"quadqr.png\"); console.log(result.text); PNG generation/decoding and SVG generation are built into the Node.js adapter. Use a script tag HTML Copy <canvas id=\"qr\"></canvas> <script src=\"https://cdn.jsdelivr.net/npm/quadqr-js@1.5.5/dist/quadqr.min.js\"></script> <script> const code = QuadQR.encodeText(\"No build step\"); QuadQR.renderToCanvas(code, document.querySelector(\"#qr\")); </script> Pin an exact package version in production. Use the CLI BASH Copy npx quadqr-js encode \"Hello QuadQR\" -o hello.png npx quadqr-js decode hello.png Password-protected CLI workflow: BASH Copy npx quadqr-js encode \"Private data\" --password \"my-password\" -o secure.png npx quadqr-js decode secure.png --password \"my-password\" See CLI reference for the full command reference. Compression and signed payloads Compression is available directly on the normal encode APIs. You do not need to select a payload type or use a separate payload mode: JS Copy import { encodeText } from \"quadqr-js\"; const code = encodeText(\"repeat repeat repeat repeat\", { compression: \"auto\", ecc: \"Q\" }); auto is the fast default: it compares LZ level 6, DEFLATE level 6, and Brotli quality 6 once, then keeps the smallest complete representation. smart is the CPU-heavy option; it starts with the same pass and only tries DEFLATE 8/9 and Brotli 9/11 when stronger compression can realistically reduce the QuadQR version. When you explicitly choose a codec, compressionLevel controls encoder effort: JS Copy encodeText(text, { compression: \"lz\", compressionLevel: 9 }); // 1..9 encodeText(text, { compression: \"deflate\", compressionLevel: 9 }); // 1..9 encodeText(text, { compression: \"brotli\", compressionLevel: 11 }); // 0..11 LZ defaults to level 6, DEFLATE defaults to level 6, and Brotli defaults to quality 11. The level is not stored in the symbol because decoding does not depend on it. For offline integrity verification: JS Copy import { generateSigningKeyPair, encodeSignedText, decodeMatrix, verifyDecodedSignature } from \"quadqr-js\"; const keys = await generateSigningKeyPair(); const signed = await encodeSignedText(\"ticket data\", { compression: \"auto\", privateKey: keys.privateKey, keyId: keys.keyId }); const result = await verifyDecodedSignature(decodeMatrix(signed.matrix), { publicKey: keys.publicKey }); console.log(result.signatureVerified, result.signatureTrusted); Signing metadata is internal. The private key signs; the trusted public key verifies and remains outside the QuadQR by default. The optional keyId lets applications select the correct trusted public key without embedding it in every symbol. Print mode JS Copy renderToCanvas(code, canvas, { imageSize: 1200, mode: \"print\", quietZone: 4 }); Print mode uses safer defaults but should still be tested with the actual printer, paper, physical size, and target phones. Scanability testing JS Copy import { assessScanability } from \"quadqr-js\"; const report = assessScanability(code, { imageSize: 480 }); console.log(report.score, report.rating); The browser demo includes an interactive stress lab for testing one distortion at a time or running the full suite. Previous ← Overview Next Browser & CDN → QuadQR Experimental RGBW matrix symbology. AGPL-3.0 licensed"},{"title":"Install","page":"Getting Started","url":"getting-started.html#install","text":"Install BASH Copy npm install quadqr-js Node.js usage targets Node.js 20.19 or newer. Browser use works through a bundler, direct ESM, or the prebuilt CDN/global bundle."},{"title":"Encode and decode text","page":"Getting Started","url":"getting-started.html#encode-and-decode-text","text":"Encode and decode text JS Copy import { encodeText, decodeMatrix } from \"quadqr-js\"; const code = encodeText(\"Hello from QuadQR\", { ecc: \"M\" }); const result = decodeMatrix(code.matrix); console.log(result.text); If no version is supplied, QuadQR automatically chooses the smallest version that fits the payload."},{"title":"Render in the browser","page":"Getting Started","url":"getting-started.html#render-in-the-browser","text":"Render in the browser JS Copy import { encodeText, renderToCanvas, renderToSVG } from \"quadqr-js/browser\"; const code = encodeText(\"Rendered in the browser\"); renderToCanvas(code, document.querySelector(\"#qr\"), { imageSize: 720, quietZone: 4, style: \"classic\" }); const svg = renderToSVG(code, { imageSize: 720, quietZone: 4 }); Available presentation styles are classic , depth , soft , and inset . Styling does not change the encoded matrix. imageSize is the exact square output dimension in pixels. If you omit both imageSize and moduleSize , the renderer defaults to 720 × 720 px. moduleSize is still available for low-level pixels-per-module sizing. To add a centered logo, load it as an Image and pass it as logo.source . Transparent logo pixels remain transparent. Use clearBackground: true when you want a padded white area behind the logo. quietZone is measured in modules and defaults to 4 ."},{"title":"Scan an uploaded image","page":"Getting Started","url":"getting-started.html#scan-an-uploaded-image","text":"Scan an uploaded image JS Copy import { scanFile } from \"quadqr-js/browser\"; const file = document.querySelector(\"#image\").files[0]; const result = await scanFile(file); console.log(result.text);"},{"title":"Scan from the camera","page":"Getting Started","url":"getting-started.html#scan-from-the-camera","text":"Scan from the camera JS Copy import { startCameraScanner } from \"quadqr-js/browser\"; let scanner; scanner = await startCameraScanner( document.querySelector(\"#video\"), { cameraFinderRecoveryEvery: 2, onResult(result) { console.log(result); scanner.stop(); } } ); Camera access requires HTTPS or localhost. Finder detection uses a QuadQR-specific RGB value pass first, then throttled threshold recovery when the fast pass cannot lock onto all three finder patterns."},{"title":"Secure password mode","page":"Getting Started","url":"getting-started.html#secure-password-mode","text":"Secure password mode Secure payloads are optional. Password mode is the simplest choice when a person will provide the secret during decryption. JS Copy import { encodeSecureText, decodeMatrix, decryptDecoded } from \"quadqr-js\"; const code = await encodeSecureText(\"Private payload\", { ecc: \"M\", security: { mode: \"password\", password: \"correct horse battery staple\" } }); const locked = decodeMatrix(code.matrix); const unlocked = await decryptDecoded(locked, { password: \"correct horse battery staple\" }); console.log(unlocked.text); A secure image or camera scan follows the same pattern: scanning returns a locked result, then decryptDecoded() reveals plaintext only after successful authentication."},{"title":"Secure raw-key mode","page":"Getting Started","url":"getting-started.html#secure-raw-key-mode","text":"Secure raw-key mode Use raw-key mode when an application already manages its own cryptographic keys. JS Copy import { encodeSecureText, generateRaw256Key } from \"quadqr-js\"; const key = generateRaw256Key(); const code = await encodeSecureText(\"Application-managed secret\", { security: { mode: \"raw-key\", key } }); The key must be exactly 32 bytes. Never embed the secret key inside the same QuadQR payload."},{"title":"Generate PNG and SVG files in Node.js","page":"Getting Started","url":"getting-started.html#generate-png-and-svg-files-in-nodejs","text":"Generate PNG and SVG files in Node.js JS Copy import { encodeText } from \"quadqr-js\"; import { savePNG, saveSVG, scanFile } from \"quadqr-js/node\"; const code = encodeText(\"Generated on Node.js\"); await savePNG(code, \"quadqr.png\", { imageSize: 720, quietZone: 4 }); await saveSVG(code, \"quadqr.svg\", { imageSize: 720, quietZone: 4 }); const result = await scanFile(\"quadqr.png\"); console.log(result.text); PNG generation/decoding and SVG generation are built into the Node.js adapter."},{"title":"Use a script tag","page":"Getting Started","url":"getting-started.html#use-a-script-tag","text":"Use a script tag HTML Copy <canvas id=\"qr\"></canvas> <script src=\"https://cdn.jsdelivr.net/npm/quadqr-js@1.5.5/dist/quadqr.min.js\"></script> <script> const code = QuadQR.encodeText(\"No build step\"); QuadQR.renderToCanvas(code, document.querySelector(\"#qr\")); </script> Pin an exact package version in production."},{"title":"Use the CLI","page":"Getting Started","url":"getting-started.html#use-the-cli","text":"Use the CLI BASH Copy npx quadqr-js encode \"Hello QuadQR\" -o hello.png npx quadqr-js decode hello.png Password-protected CLI workflow: BASH Copy npx quadqr-js encode \"Private data\" --password \"my-password\" -o secure.png npx quadqr-js decode secure.png --password \"my-password\" See CLI reference for the full command reference."},{"title":"Compression and signed payloads","page":"Getting Started","url":"getting-started.html#compression-and-signed-payloads","text":"Compression and signed payloads Compression is available directly on the normal encode APIs. You do not need to select a payload type or use a separate payload mode: JS Copy import { encodeText } from \"quadqr-js\"; const code = encodeText(\"repeat repeat repeat repeat\", { compression: \"auto\", ecc: \"Q\" }); auto is the fast default: it compares LZ level 6, DEFLATE level 6, and Brotli quality 6 once, then keeps the smallest complete representation. smart is the CPU-heavy option; it starts with the same pass and only tries DEFLATE 8/9 and Brotli 9/11 when stronger compression can realistically reduce the QuadQR version. When you explicitly choose a codec, compressionLevel controls encoder effort: JS Copy encodeText(text, { compression: \"lz\", compressionLevel: 9 }); // 1..9 encodeText(text, { compression: \"deflate\", compressionLevel: 9 }); // 1..9 encodeText(text, { compression: \"brotli\", compressionLevel: 11 }); // 0..11 LZ defaults to level 6, DEFLATE defaults to level 6, and Brotli defaults to quality 11. The level is not stored in the symbol because decoding does not depend on it. For offline integrity verification: JS Copy import { generateSigningKeyPair, encodeSignedText, decodeMatrix, verifyDecodedSignature } from \"quadqr-js\"; const keys = await generateSigningKeyPair(); const signed = await encodeSignedText(\"ticket data\", { compression: \"auto\", privateKey: keys.privateKey, keyId: keys.keyId }); const result = await verifyDecodedSignature(decodeMatrix(signed.matrix), { publicKey: keys.publicKey }); console.log(result.signatureVerified, result.signatureTrusted); Signing metadata is internal. The private key signs; the trusted public key verifies and remains outside the QuadQR by default. The optional keyId lets applications select the correct trusted public key without embedding it in every symbol."},{"title":"Print mode","page":"Getting Started","url":"getting-started.html#print-mode","text":"Print mode JS Copy renderToCanvas(code, canvas, { imageSize: 1200, mode: \"print\", quietZone: 4 }); Print mode uses safer defaults but should still be tested with the actual printer, paper, physical size, and target phones."},{"title":"Scanability testing","page":"Getting Started","url":"getting-started.html#scanability-testing","text":"Scanability testing JS Copy import { assessScanability } from \"quadqr-js\"; const report = assessScanability(code, { imageSize: 480 }); console.log(report.score, report.rating); The browser demo includes an interactive stress lab for testing one distortion at a time or running the full suite. Previous ← Overview Next Browser & CDN → QuadQR Experimental RGBW matrix symbology. AGPL-3.0 licensed"},{"title":"High Density Mode","page":"High Density Mode","url":"high-density.html","text":"QUADQR DOCUMENTATION High Density Mode High Density Mode is an experimental QuadQR option implemented with the Triangle16 cell layout. It keeps the existing QuadQR geometry, finder patterns, timing pattern, alignment patterns, calibration, Spectrum ECC, CRC, security envelope, compression, and signing behavior, but changes how payload data cells are represented. Physical cell A Triangle16 payload cell is split by one fixed / diagonal: TEXT Copy +---------+ | AAAAAA /| | AAAAA /B| | AAAA /BB| | AAA /BBB| | AA /BBBB| | A /BBBBB| | /BBBBBBB| +---------+ A is the upper-left triangle and B is the lower-right triangle. Each triangle independently uses the existing RGBW alphabet: TEXT Copy R = 00 G = 01 B = 10 W = 11 The pair therefore has 16 states: TEXT Copy R/R R/G R/B R/W G/R G/G G/B G/W B/R B/G B/B B/W W/R W/G W/B W/W The first triangle carries the high 2 bits and the second triangle carries the low 2 bits. One Triangle16 body cell therefore carries 4 raw bits, so one byte occupies two body cells instead of four RGBW cells. Protected header stays solid The protected bootstrap/header intentionally does not use mixed-color triangles. Its normal RGBW header cells are represented as same-color pairs such as R/R , G/G , B/B , and W/W . This uses four cells per protected header byte, exactly like RGBW, but makes it much easier for the decoder to recover the mode flag when the image is blurred, skewed, resized, or color-shifted. Header flag bit 6 declares Triangle16 for the ECC-protected body. Scanner sampling The scanner does not sample the diagonal or the exact module center. Each triangle now uses a small three-anchor sampling cluster positioned safely inside its region. The cluster is robustly aggregated, and the variation between the three anchors is retained as a spatial-stability signal. This is less sensitive to blur, resampling, small homography offsets, and diagonal color bleed than relying on one point per triangle. Each triangle is classified independently against the calibrated RGBW palette. The cell confidence combines the weaker color-classification confidence with the measured within-triangle sample stability. The scanner also retains the most plausible alternate Triangle16 state by changing the less reliable of the two regions first. Those confidence and alternate-state signals feed Spectrum ECC 2.0 for erasure and bounded soft-decision recovery. The normal image and camera scanner automatically detects High Density Mode and attempts Triangle16 dual-region sampling when needed. No separate scan mode is required. If ordinary finder/alignment geometry is good enough to locate the symbol but not precise enough to decode the half-cell regions, the scanner performs one bounded precise-alignment recovery pass. That pass uses denser alignment-pattern probes and a finer sub-module search, then retries the same dual-triangle classifier. Rendering rules High Density Mode uses exact hard-edged Triangle16 payload cells. Decorative payload styles are intentionally bypassed in this experimental mode because rounded, inset, soft, or depth effects can reduce the usable sampling area or contaminate the diagonal boundary. Structural modules keep the normal QuadQR rendering behavior. PNG/ImageData, Canvas, SVG, browser, and Node rendering all support Triangle16. API JS Copy import { encodeText } from \"quadqr-js\"; const code = encodeText(\"High-density QuadQR\", { ecc: \"M\", highDensity: true }); High Density Mode is disabled by default. Existing callers remain on normal RGBW mode unless highDensity: true is explicitly supplied. Capacity Raw body density is: TEXT Copy RGBW 4 states 2 bits/body cell Triangle16 16 states 4 bits/body cell The protected header remains RGBW-equivalent, and ECC/CRC overhead is unchanged at the byte level, so usable payload capacity is close to but not exactly 2x for a fixed matrix version. Use getVersionInfo(version, { ecc, highDensity: true }) or the demo capacity calculator for the exact value. Reliability caveat Triangle16 doubles raw body bits per cell, but it also halves the spatial area available to each independently classified color. Its meaningful real-world metric is not only bits per matrix cell. It is reliable payload bytes at a fixed physical size, camera distance, angle, lighting condition, resize/compression pipeline, and print quality. The branch should therefore be stress-tested before treating High Density Mode as stable. Important cases include: perspective and partial finder degradation; defocus and motion blur; low camera pixel coverage per module; JPEG compression and repeated image resizing; shadows, glare, white-balance shifts, and saturation changes; cheap printing, ink spread, paper tint, and camera recapture. If full 16-state reliability becomes the limiting factor, a restricted triangle alphabet can be explored later without changing the basic physical-cell experiment. Previous ← Secure Payloads Next Reliability Lab → QuadQR Experimental RGBW matrix symbology. AGPL-3.0 licensed"},{"title":"Physical cell","page":"High Density Mode","url":"high-density.html#physical-cell","text":"Physical cell A Triangle16 payload cell is split by one fixed / diagonal: TEXT Copy +---------+ | AAAAAA /| | AAAAA /B| | AAAA /BB| | AAA /BBB| | AA /BBBB| | A /BBBBB| | /BBBBBBB| +---------+ A is the upper-left triangle and B is the lower-right triangle. Each triangle independently uses the existing RGBW alphabet: TEXT Copy R = 00 G = 01 B = 10 W = 11 The pair therefore has 16 states: TEXT Copy R/R R/G R/B R/W G/R G/G G/B G/W B/R B/G B/B B/W W/R W/G W/B W/W The first triangle carries the high 2 bits and the second triangle carries the low 2 bits. One Triangle16 body cell therefore carries 4 raw bits, so one byte occupies two body cells instead of four RGBW cells."},{"title":"Protected header stays solid","page":"High Density Mode","url":"high-density.html#protected-header-stays-solid","text":"Protected header stays solid The protected bootstrap/header intentionally does not use mixed-color triangles. Its normal RGBW header cells are represented as same-color pairs such as R/R , G/G , B/B , and W/W . This uses four cells per protected header byte, exactly like RGBW, but makes it much easier for the decoder to recover the mode flag when the image is blurred, skewed, resized, or color-shifted. Header flag bit 6 declares Triangle16 for the ECC-protected body."},{"title":"Scanner sampling","page":"High Density Mode","url":"high-density.html#scanner-sampling","text":"Scanner sampling The scanner does not sample the diagonal or the exact module center. Each triangle now uses a small three-anchor sampling cluster positioned safely inside its region. The cluster is robustly aggregated, and the variation between the three anchors is retained as a spatial-stability signal. This is less sensitive to blur, resampling, small homography offsets, and diagonal color bleed than relying on one point per triangle. Each triangle is classified independently against the calibrated RGBW palette. The cell confidence combines the weaker color-classification confidence with the measured within-triangle sample stability. The scanner also retains the most plausible alternate Triangle16 state by changing the less reliable of the two regions first. Those confidence and alternate-state signals feed Spectrum ECC 2.0 for erasure and bounded soft-decision recovery. The normal image and camera scanner automatically detects High Density Mode and attempts Triangle16 dual-region sampling when needed. No separate scan mode is required. If ordinary finder/alignment geometry is good enough to locate the symbol but not precise enough to decode the half-cell regions, the scanner performs one bounded precise-alignment recovery pass. That pass uses denser alignment-pattern probes and a finer sub-module search, then retries the same dual-triangle classifier."},{"title":"Rendering rules","page":"High Density Mode","url":"high-density.html#rendering-rules","text":"Rendering rules High Density Mode uses exact hard-edged Triangle16 payload cells. Decorative payload styles are intentionally bypassed in this experimental mode because rounded, inset, soft, or depth effects can reduce the usable sampling area or contaminate the diagonal boundary. Structural modules keep the normal QuadQR rendering behavior. PNG/ImageData, Canvas, SVG, browser, and Node rendering all support Triangle16."},{"title":"API","page":"High Density Mode","url":"high-density.html#api","text":"API JS Copy import { encodeText } from \"quadqr-js\"; const code = encodeText(\"High-density QuadQR\", { ecc: \"M\", highDensity: true }); High Density Mode is disabled by default. Existing callers remain on normal RGBW mode unless highDensity: true is explicitly supplied."},{"title":"Capacity","page":"High Density Mode","url":"high-density.html#capacity","text":"Capacity Raw body density is: TEXT Copy RGBW 4 states 2 bits/body cell Triangle16 16 states 4 bits/body cell The protected header remains RGBW-equivalent, and ECC/CRC overhead is unchanged at the byte level, so usable payload capacity is close to but not exactly 2x for a fixed matrix version. Use getVersionInfo(version, { ecc, highDensity: true }) or the demo capacity calculator for the exact value."},{"title":"Reliability caveat","page":"High Density Mode","url":"high-density.html#reliability-caveat","text":"Reliability caveat Triangle16 doubles raw body bits per cell, but it also halves the spatial area available to each independently classified color. Its meaningful real-world metric is not only bits per matrix cell. It is reliable payload bytes at a fixed physical size, camera distance, angle, lighting condition, resize/compression pipeline, and print quality. The branch should therefore be stress-tested before treating High Density Mode as stable. Important cases include: perspective and partial finder degradation; defocus and motion blur; low camera pixel coverage per module; JPEG compression and repeated image resizing; shadows, glare, white-balance shifts, and saturation changes; cheap printing, ink spread, paper tint, and camera recapture. If full 16-state reliability becomes the limiting factor, a restricted triangle alphabet can be explored later without changing the basic physical-cell experiment. Previous ← Secure Payloads Next Reliability Lab → QuadQR Experimental RGBW matrix symbology. AGPL-3.0 licensed"},{"title":"Documentation for the complete QuadQR toolkit.","page":"Documentation for the complete QuadQR toolkit.","url":"index.html","text":"QUADQR 1.5.5 Documentation for the complete QuadQR toolkit. Generate, render, scan, compress, sign, encrypt, diagnose, and stress-test QuadQR in browsers and Node.js with one JavaScript implementation. Start building Open live demo Live library output Runtime QuadQR Generating… START HERE Install once, use the runtime you need. SHELL Copy npm install quadqr-js Core Encode and decode Text, bytes, ECC profiles, auto-versioning, binary APIs, and shared format behavior. quadqr-js Browser Render and scan Canvas, SVG, uploaded images, video frames, camera scanning, and CDN usage. quadqr-js/browser Node.js Server-side workflows PNG/SVG output, file and buffer scanning, secure payloads, and portable compression. quadqr-js/node QUICK START Your first QuadQR. JAVASCRIPT Copy import { encodeText, decodeMatrix } from \"quadqr-js\"; const code = encodeText(\"Hello QuadQR\", { ecc: \"M\" }); const result = decodeMatrix(code.matrix); console.log(result.text); // Hello QuadQR Automatic sizing If you do not supply a version, QuadQR chooses the smallest version that fits the encoded payload and selected protection settings. CAPABILITIES Everything is documented here. Scanning Camera and image recovery Perspective recovery, color calibration, multi-frame confidence fusion, and scanner diagnostics. Payload Compression 3.0 Auto, Smart, LZ, DEFLATE, and Brotli with a tiny internal compression identifier. Security Encryption and signing AES-256-GCM secure payloads and Ed25519 signatures while trusted keys remain outside the symbol. Experimental High Density Mode Triangle16 split cells provide 16 RGBW pair states and 4 raw bits per payload body cell. Testing Reliability Lab Deterministic blur, glare, noise, color-cast, pitch, yaw, Z rotation, and perspective sweeps. Reference Complete API Function signatures, options, constants, Node helpers, diagnostics, and reliability APIs. FORMAT RGBW by default, Triangle16 when requested. Normal RGBW Four color states represent 2 raw bits per payload data cell. This is the default mode. Read format reference → High Density Each body cell is split diagonally into two RGBW triangles for 16 states and 4 raw bits. Read High Density guide → Custom symbology QuadQR is not an ISO QR Code. Standard QR scanner apps do not decode QuadQR symbols. Next Getting Started → QuadQR Experimental RGBW matrix symbology. AGPL-3.0 licensed"},{"title":"Install once, use the runtime you need.","page":"Documentation for the complete QuadQR toolkit.","url":"index.html#install","text":"Install once, use the runtime you need."},{"title":"Your first QuadQR.","page":"Documentation for the complete QuadQR toolkit.","url":"index.html#quick-start","text":"Your first QuadQR."},{"title":"Everything is documented here.","page":"Documentation for the complete QuadQR toolkit.","url":"index.html#capabilities","text":"Everything is documented here."},{"title":"RGBW by default, Triangle16 when requested.","page":"Documentation for the complete QuadQR toolkit.","url":"index.html#format-basics","text":"RGBW by default, Triangle16 when requested."},{"title":"Node.js","page":"Node.js","url":"node.html","text":"QUADQR DOCUMENTATION Node.js QuadQR uses the same matrix codec and scanner core in Node.js and the browser. The quadqr-js/node entry adds Node-specific PNG, file, and buffer helpers. Requirements Node.js 20.19 or newer. ESM JS Copy import { encodeText } from \"quadqr-js\"; import { savePNG, saveSVG, scanFile } from \"quadqr-js/node\"; CommonJS JS Copy const QuadQR = require(\"quadqr-js\"); const QuadQRNode = require(\"quadqr-js/node\"); Generate a PNG JS Copy const code = QuadQR.encodeText(\"Server generated\"); await QuadQRNode.savePNG(code, \"output.png\", { imageSize: 720, quietZone: 4 }); Generate an SVG JS Copy await QuadQRNode.saveSVG(code, \"output.svg\", { imageSize: 720, quietZone: 4, style: \"classic\" }); imageSize is the exact PNG/SVG output dimension in pixels. If you omit both imageSize and moduleSize , rendering defaults to 720 × 720 px. moduleSize remains available for legacy pixels-per-module sizing. Use toSVG() when you need the SVG string in memory. SVG logo sources can be URL/data URL strings. For PNG generation with a logo through renderToImageData() , pass decoded ImageData-like RGBA pixels as the logo source. Use toPNG() when you need an in-memory Buffer : JS Copy const png = QuadQRNode.toPNG(code); This works well for HTTP responses, object storage, attachments, and other buffer-based workflows. Scan a PNG file JS Copy const result = await QuadQRNode.scanFile(\"output.png\"); console.log(result.text); Or scan a buffer: JS Copy const result = await QuadQRNode.scanBuffer(pngBuffer); PNG generation and decoding do not require external native dependencies. Server-side compression Compression uses the same synchronous JavaScript codec on Node.js as it does in the browser. No DOM API, browser CompressionStream , or Node zlib call is required by the QuadQR payload codec. JS Copy import { encodeText, decodeMatrix } from \"quadqr-js\"; const code = encodeText(\"hello \".repeat(1000), { compression: \"auto\" }); const smallest = encodeText(\"structured payload \".repeat(500), { compression: \"smart\" }); const decoded = decodeMatrix(code.matrix); console.log(decoded.compression); // usually \"brotli\" for repetitive data console.log(decoded.text); compression: \"auto\" performs one balanced comparison using LZ level 6, DEFLATE level 6, and Brotli quality 6. compression: \"smart\" is the opt-in CPU-heavy mode and escalates to stronger levels only when a smaller QuadQR version is realistically reachable. Explicit lz and deflate accept compressionLevel: 1..9 (default 6), while explicit brotli accepts compressionLevel: 0..11 (default 11). lzLevel , deflateLevel , and brotliQuality are available as algorithm-specific aliases. Other image formats For JPEG, WebP, or AVIF input, the Node adapter can use sharp when the consuming application has it installed: BASH Copy npm install sharp If sharp is not installed, decode the image to RGBA in your own image pipeline and pass the pixels to scanImageData() . Secure password workflow JS Copy import { decryptDecoded, encodeSecureText } from \"quadqr-js\"; import { savePNG, scanFile } from \"quadqr-js/node\"; const code = await encodeSecureText(\"private\", { security: { mode: \"password\", password: process.env.QUADQR_PASSWORD } }); await savePNG(code, \"secure.png\"); const locked = await scanFile(\"secure.png\"); const unlocked = await decryptDecoded(locked, { password: process.env.QUADQR_PASSWORD }); console.log(unlocked.text); Secure raw-key workflow JS Copy import { decryptDecoded, encodeSecureText, generateRaw256Key } from \"quadqr-js\"; import { toPNG, scanBuffer } from \"quadqr-js/node\"; const key = generateRaw256Key(); const code = await encodeSecureText(\"machine secret\", { security: { mode: \"raw-key\", key } }); const png = toPNG(code); const locked = await scanBuffer(png); const unlocked = await decryptDecoded(locked, { key }); console.log(unlocked.text); Keep long-lived application keys in an appropriate secret-management or platform key-storage system. Previous ← Browser & CDN Next Scanning & Camera → QuadQR Experimental RGBW matrix symbology. AGPL-3.0 licensed"},{"title":"Requirements","page":"Node.js","url":"node.html#requirements","text":"Requirements Node.js 20.19 or newer."},{"title":"ESM","page":"Node.js","url":"node.html#esm","text":"ESM JS Copy import { encodeText } from \"quadqr-js\"; import { savePNG, saveSVG, scanFile } from \"quadqr-js/node\";"},{"title":"CommonJS","page":"Node.js","url":"node.html#commonjs","text":"CommonJS JS Copy const QuadQR = require(\"quadqr-js\"); const QuadQRNode = require(\"quadqr-js/node\");"},{"title":"Generate a PNG","page":"Node.js","url":"node.html#generate-a-png","text":"Generate a PNG JS Copy const code = QuadQR.encodeText(\"Server generated\"); await QuadQRNode.savePNG(code, \"output.png\", { imageSize: 720, quietZone: 4 });"},{"title":"Generate an SVG","page":"Node.js","url":"node.html#generate-an-svg","text":"Generate an SVG JS Copy await QuadQRNode.saveSVG(code, \"output.svg\", { imageSize: 720, quietZone: 4, style: \"classic\" }); imageSize is the exact PNG/SVG output dimension in pixels. If you omit both imageSize and moduleSize , rendering defaults to 720 × 720 px. moduleSize remains available for legacy pixels-per-module sizing. Use toSVG() when you need the SVG string in memory. SVG logo sources can be URL/data URL strings. For PNG generation with a logo through renderToImageData() , pass decoded ImageData-like RGBA pixels as the logo source. Use toPNG() when you need an in-memory Buffer : JS Copy const png = QuadQRNode.toPNG(code); This works well for HTTP responses, object storage, attachments, and other buffer-based workflows."},{"title":"Scan a PNG file","page":"Node.js","url":"node.html#scan-a-png-file","text":"Scan a PNG file JS Copy const result = await QuadQRNode.scanFile(\"output.png\"); console.log(result.text); Or scan a buffer: JS Copy const result = await QuadQRNode.scanBuffer(pngBuffer); PNG generation and decoding do not require external native dependencies."},{"title":"Server-side compression","page":"Node.js","url":"node.html#server-side-compression","text":"Server-side compression Compression uses the same synchronous JavaScript codec on Node.js as it does in the browser. No DOM API, browser CompressionStream , or Node zlib call is required by the QuadQR payload codec. JS Copy import { encodeText, decodeMatrix } from \"quadqr-js\"; const code = encodeText(\"hello \".repeat(1000), { compression: \"auto\" }); const smallest = encodeText(\"structured payload \".repeat(500), { compression: \"smart\" }); const decoded = decodeMatrix(code.matrix); console.log(decoded.compression); // usually \"brotli\" for repetitive data console.log(decoded.text); compression: \"auto\" performs one balanced comparison using LZ level 6, DEFLATE level 6, and Brotli quality 6. compression: \"smart\" is the opt-in CPU-heavy mode and escalates to stronger levels only when a smaller QuadQR version is realistically reachable. Explicit lz and deflate accept compressionLevel: 1..9 (default 6), while explicit brotli accepts compressionLevel: 0..11 (default 11). lzLevel , deflateLevel , and brotliQuality are available as algorithm-specific aliases."},{"title":"Other image formats","page":"Node.js","url":"node.html#other-image-formats","text":"Other image formats For JPEG, WebP, or AVIF input, the Node adapter can use sharp when the consuming application has it installed: BASH Copy npm install sharp If sharp is not installed, decode the image to RGBA in your own image pipeline and pass the pixels to scanImageData() ."},{"title":"Secure password workflow","page":"Node.js","url":"node.html#secure-password-workflow","text":"Secure password workflow JS Copy import { decryptDecoded, encodeSecureText } from \"quadqr-js\"; import { savePNG, scanFile } from \"quadqr-js/node\"; const code = await encodeSecureText(\"private\", { security: { mode: \"password\", password: process.env.QUADQR_PASSWORD } }); await savePNG(code, \"secure.png\"); const locked = await scanFile(\"secure.png\"); const unlocked = await decryptDecoded(locked, { password: process.env.QUADQR_PASSWORD }); console.log(unlocked.text);"},{"title":"Secure raw-key workflow","page":"Node.js","url":"node.html#secure-raw-key-workflow","text":"Secure raw-key workflow JS Copy import { decryptDecoded, encodeSecureText, generateRaw256Key } from \"quadqr-js\"; import { toPNG, scanBuffer } from \"quadqr-js/node\"; const key = generateRaw256Key(); const code = await encodeSecureText(\"machine secret\", { security: { mode: \"raw-key\", key } }); const png = toPNG(code); const locked = await scanBuffer(png); const unlocked = await decryptDecoded(locked, { key }); console.log(unlocked.text); Keep long-lived application keys in an appropriate secret-management or platform key-storage system. Previous ← Browser & CDN Next Scanning & Camera → QuadQR Experimental RGBW matrix symbology. AGPL-3.0 licensed"},{"title":"Configuration Reference","page":"Configuration Reference","url":"options.html","text":"QUADQR DOCUMENTATION Configuration Reference Every supported public option in QuadQR 1.5.5, with accepted values, defaults, behavior, and the context in which it matters. This page is generated from the runtime implementation rather than from UI labels. Public versus internal settings Options beginning with an underscore, such as _observationCollector , are private scanner plumbing and are intentionally not part of the supported API. Everything below is intended for application use or is accepted by a documented public function. Encoding, version and ECC Option Type / accepted values Default What it does ecc \"L\" | \"M\" | \"Q\" | \"H\" \"M\" Selects the Spectrum ECC profile. Higher letters add more Reed-Solomon parity and reduce payload capacity. The letters are QuadQR profiles and are not recovery-equivalent to ISO QR ECC letters. version \"auto\" or integer 1..40 \"auto\" Fixes the matrix version or lets QuadQR choose the smallest version that fits the final encoded payload. minVersion integer 1..40 1 Lower bound used by automatic version selection and scanner searches. maxVersion integer 1..40 40 Upper bound used by automatic version selection and scanner searches. Must be greater than or equal to minVersion . highDensity boolean false Convenience switch for experimental Triangle16. true is equivalent to cellEncoding: \"triangle16\" . cellEncoding \"rgbw\" | \"triangle16\" \"rgbw\" Selects the physical data-cell encoding. RGBW carries 2 raw bits per body cell; Triangle16 carries 4 raw bits by splitting each body module into two RGBW triangles. formatVersion integer 6 for new encoding Compatibility guard for encoding. New symbols use Format v6. The scanner retains backward decoding for legacy Format v5 symbols. Compression Option Type / accepted values Default What it does compression \"none\" | \"auto\" | \"smart\" | \"lz\" | \"deflate\" | \"brotli\" Normal encode: \"none\" Signed encode: \"auto\" Controls the internal payload-extension compression layer. Auto tries balanced LZ/DEFLATE/Brotli passes and keeps compression only when the final payload is smaller. Smart may spend more CPU near version boundaries. compressionLevel integer; codec-specific unset Generic level override for an explicit lz , deflate , or brotli mode. It is rejected for modes where a level does not apply. lzLevel integer 1..9 6 Legacy LZ search depth. Higher values spend more CPU looking for longer back-references. deflateLevel integer 1..9 6 Raw fixed-Huffman DEFLATE search level. Higher values search more candidate chains and use more lazy matching. brotliQuality integer 0..11 11 for explicit Brotli Brotli quality for explicit Brotli compression. Auto uses quality 6; Smart stages qualities 6, 9, then 11 only when useful. Signing Option Type / accepted values Default What it does privateKey Ed25519 private CryptoKey or PKCS#8 bytes required for signed APIs The signing key. Only the private key can create a signature. publicKey Ed25519 public CryptoKey or 32-byte raw key required only with embedPublicKey: true Public verification key. For normal trusted verification, distribute it separately instead of embedding it. keyId string/bytes, up to 64 bytes for signing empty Small identifier stored with a signature so a scanner can choose the correct trusted public key. embedPublicKey boolean false Embeds the 32-byte public key in the payload envelope. This increases symbol size and does not by itself make that key trusted. Signature verification Option Type / accepted values Default What it does publicKey / trustedPublicKey Ed25519 public key unset Trusted key used to verify the signature. publicKey takes precedence. trustedKeys Map or object keyed by stored keyId unset Trusted-key registry. Lets a signed result route itself to a known public key by key ID. allowEmbeddedKey boolean false for modern envelopes Allows verification using a public key embedded inside the symbol when no external trusted key is supplied. A successful cryptographic check from an embedded key is not marked externally trusted. Secure Payload Option Type / accepted values Default What it does security.mode \"password\" | \"raw-key\" required Selects Secure Payload v1 credential mode. security.password non-empty string required in password mode Password input to PBKDF2-HMAC-SHA-256, which derives the AES-256-GCM key. security.iterations integer 100000..2000000 600000 PBKDF2 iteration count stored in the encrypted envelope. Higher values cost more CPU for both encryption and decryption. security.key 32-byte key or 64-hex-character string required in raw-key mode Exact AES-256 key material. No password KDF is used. security.keyId false , string, or bytes up to 32 bytes Password: none Raw key: automatic 8-byte fingerprint Routing hint for selecting a decryption key. Set false to omit it. It is not secret and is authenticated by AES-GCM. verifyKeyId boolean true Raw-key decryption option. When an automatic key fingerprint exists, checks it before attempting AES-GCM. Set false only if you intentionally want to skip that fast mismatch check. Matrix decoding and Spectrum ECC 2.0 Option Type / accepted values Default What it does structureTolerance number, normally 0..1 0 in direct decodeMatrix() Fractional tolerance for structural finder/alignment mismatches when decoding an already sampled matrix. Image scanning supplies a more forgiving scanner-path value. cellConfidence square number matrix matching the code matrix unset Per-cell confidence values from 0 to 1 . Enables confidence-aware error/erasure decoding. cellAlternatives square matrix of alternative cell values unset Second-best cell hypotheses used by bounded Spectrum ECC 2.0 soft decoding after normal decoding fails. cellEncodingHint \"rgbw\" | \"triangle16\" auto-detect Restricts the first physical-cell interpretation attempt. Primarily useful when feeding scanner-produced sampled matrices. maxErasureConfidence number 0.68 Cells/symbols at or below this confidence can be promoted to Reed-Solomon erasures after hard decoding fails. Lower values are more conservative. softDecoding boolean true when confidence + alternatives exist Allows bounded alternate-cell substitution attempts after hard/error-erasure decoding fails. Set false to disable the soft stage. softDecodeConfidence number, clamped 0.05..0.95 0.72 Only cells at or below this confidence are considered for alternate-cell soft decoding. softDecodeMaxCells integer, clamped 2..16 10 Maximum number of low-confidence cells considered as soft-decoding candidates. softDecodePairCells integer, 2..softDecodeMaxCells 6 Limits the candidate subset used for two-cell substitution combinations, keeping the search bounded. Rendering Option Type / accepted values Default What it does imageSize positive number, pixels 720 when neither size option is set Exact square output dimension. The renderer distributes fractional module sizes across pixels so the final output stays exactly this size. moduleSize positive number, pixels/module unset Low-level sizing mode used only when imageSize is omitted. The actual output is (matrix + quiet zone) × moduleSize . quietZone non-negative integer modules 4 Blank border around the symbol. The requested value is honored in both screen and print modes; four modules remains recommended. mode / renderMode \"screen\" | \"print\" \"screen\" Selects screen or print rendering behavior. Print mode uses the darker print palette and conservative safety rules. style \"classic\" | \"depth\" | \"soft\" | \"inset\" \"classic\" Visual data-cell treatment. Structural cells remain exact. Triangle16 body cells are always rendered with hard-edged triangle geometry. palette object with black, white, red, green, blue hex colors screen or print built-in palette Overrides any subset of the active palette. allowStyledPrint boolean false Allows a non-classic style in print mode. Without this explicit opt-in, print mode forces classic for reliability. Logo rendering Option Type / accepted values Default What it does logo CanvasImageSource, ImageData-like object, URL/data URL for SVG, or nested logo object unset Adds a centered logo. The accepted source form depends on the renderer. logo.source renderer-compatible image source required for nested logo object Actual image source. Canvas needs a loaded image/canvas; ImageData rendering needs RGBA data; SVG accepts a URL/data URL. logo.size / logoSize ratio 0.05..0.30 or \"auto\" 0.18 Logo width as a fraction of the matrix width. \"auto\" uses estimateSafeLogoSize() . logo.clearBackground / logoClearBackground boolean false Paints a solid rounded background behind the logo before drawing it, clearing covered cells. logo.padding / logoPadding non-negative number, modules 0.65 Padding added around the logo when clear-background mode is enabled. logo.radius / logoRadius non-negative number, modules 0.8 Corner radius of the clear-background area. logo.backgroundColor / logoBackgroundColor CSS hex color active palette white Background color used by clear-background mode. Logo safety analysis Option Type / accepted values Default What it does ecc ECC letter code ECC or \"M\" Used by estimateSafeLogoSize() when a full encoded code object is not supplied. utilization ratio 0..1 code utilization or 0.65 Approximate occupied-data ratio used to reduce the recommended logo size for dense symbols. clearBackground / logoClearBackground boolean false Applies an additional safety penalty because clearing cells removes more encoded information. mode / renderMode \"screen\" | \"print\" \"screen\" Print mode uses a more conservative logo estimate. minSize ratio 0.05 Lower bound of the empirical findMaxSafeLogoSize() binary search. maxSize ratio, capped at 0.30 0.28 Upper bound of the empirical logo-size search. iterations integer, clamped 3..10 7 Binary-search iterations in findMaxSafeLogoSize() . Print guidance Option Type / accepted values Default What it does quietZone integer modules, minimum 4 in guidance 4 Quiet zone assumed for physical-size guidance. physicalSizeMm positive number, millimeters 35 Target total printed symbol width including quiet zone. dpi positive number 300 Printer resolution used to report pixels per module. minimumModuleMm positive number, millimeters 0.40 Recommended minimum physical module size used for pass/fail guidance. Image scanner: geometry and fallback Option Type / accepted values Default What it does minVersion integer 1..40 1 Lowest symbol version geometry detection will consider. maxVersion integer 1..40 40 Highest symbol version geometry detection will consider. perspective boolean true Enables finder/alignment detection and homography-based projective correction. Set false only for known axis-aligned input. axisAlignedFallback boolean true After perspective attempts fail, tries a rectangular active-bounds sampler suitable for already straight/cropped images. maxGeometryCandidates positive integer 8 Maximum perspective geometry candidates examined per finder pass. finderRecovery boolean true Allows additional finder passes after the fast RGB-value/Otsu pass, including two-finder recovery, QuadQR Auto Color-assisted finder detection, threshold bracketing, and luminance fallback. preciseAlignment boolean false Requests finer alignment localization on the current geometry pass. Normally the scanner uses this only in the bounded recovery path. preciseAlignmentRecovery boolean true If normal perspective geometry exists but does not decode, retries once with precise alignment enabled. whiteThreshold number 0..255 238 Axis-aligned fallback threshold used to find the non-white active image bounds. maxModuleAspectError non-negative ratio 0.16 Axis-aligned fallback rejects candidate grids whose estimated horizontal/vertical module sizes differ by more than this ratio. bounds {x,y,width,height} auto-detected Supplies explicit active bounds for axis-aligned scanning instead of finding them from near-white pixels. palette RGBW palette object built-in palette Fixed color reference used by the axis-aligned fallback. Perspective scanning normally calibrates colors from the symbol itself. Image scanner: sampling and calibration Option Type / accepted values Default What it does sampleMode \"cross\" | \"median\" \"cross\" Primary module sampler. Cross averages the center plus four nearby samples; median uses a compact 3×3 patch for dirt/blur rejection. sampleRadius number in module units Perspective: 0.16 Axis fallback: 0.18 Distance from module center for the primary sample pattern. Smaller values avoid edges; larger values average more of the module. adaptiveSampling boolean true Adds a robust median sampling pass after the fast sampling profile when needed. robustSampleRadius number in module units Perspective: 0.12 Axis fallback: 0.10 Radius used by the robust median sampling profile. spatialColorNormalization boolean true Uses distributed black/white calibration references to correct spatial lighting gradients before classification. structureTolerance number Perspective decode: 0.18 Axis fallback: 0.12 How much structural mismatch the scanner allows after sampled colors are classified. Increase cautiously because loose values can admit false geometry. includeRectified boolean false Adds a perspective-corrected QR-only ImageData image to a successful result. rectifiedModuleSize positive number 8 Pixels per module used for the optional rectified result image. QuadQR Finder Auto Color recovery Option Type / accepted values Default What it does finderAutoColorBlackClip number, effectively clamped 0..0.06 0.0001 Per-channel shadow percentile clipped during QuadQR Auto Color-assisted finder recovery. finderAutoColorWhiteClip number, clamped 0..0.06 0.004 Per-channel highlight percentile clipped when a direct output-highlight anchor is not used. finderAutoColorHighlightPercentile number 0.70..0.9999 0.95 Bright-reference percentile used to estimate a neutral highlight during QuadQR finder Auto Color recovery. finderAutoColorOutputHighlight number, clamped 96..250 190 Maps the observed highlight reference toward this output value, strengthening black and RGB separation instead of stretching paper white all the way to 255. finderAutoColorAnalysisInset fraction 0..0.30 recovery tries 0.10, 0.20, 0.04 Restricts histogram analysis toward the image center while applying the correction to the full frame. Set a single value to replace the built-in center-weighted sequence. finderAutoColorMinimumInputRange number, minimum 32 internally 72 Minimum per-channel input tonal span assumed while computing strong QuadQR Auto Color levels. finderAutoColorTargetSamples positive integer 90000 Approximate histogram sample budget used by QuadQR finder Auto Color analysis. Auto Tone / Contrast / Color recovery Option Type / accepted values Default What it does autoEnhanceRecovery boolean true Master switch for Auto Tone / Contrast / Color recovery after normal classification fails. fullFrameAutoEnhanceRecovery boolean Static image: true Camera fast path: false Allows a full-frame enhanced retry after geometry was found. Camera scanning normally avoids this expensive operation and prefers QR-region/cropped recovery. autoEnhanceWhenNoGeometry boolean Static image: true If even finder geometry is missing, allows one enhanced full-image retry. Camera scheduling enables it only on selected missed frames. autoEnhanceBlackClip number, clamped 0..0.08 0.006 Shadow clipping used by Auto Tone/Color recovery. autoEnhanceWhiteClip number, clamped 0..0.08 0.004 Highlight clipping used by Auto Tone/Color recovery. autoEnhanceSaturation number, clamped 1..1.5 1.12 Conservative saturation boost after white balance and tonal expansion. autoEnhanceTargetSamples positive integer, minimum 8000 160000 Approximate pixel sample budget for full-image enhancement statistics. rectifiedAutoEnhanceRecovery boolean true When geometry is known but decoding still fails, rectifies only the QR area, enhances it, and resamples it. This avoids unrelated scene pixels skewing histograms. rectifiedRecoveryModuleSize integer, clamped 4..10 6 Pixels per module used to build the temporary QR-only image for rectified recovery. rectifiedAutoEnhanceBlackClip number 0.008 Stronger shadow clipping for QR-only rectified enhancement. rectifiedAutoEnhanceWhiteClip number 0.006 Highlight clipping for QR-only rectified enhancement. rectifiedAutoEnhanceSaturation number 1.22 Saturation boost for QR-only rectified enhancement. rectifiedAutoEnhanceHighlightFraction number 0.16 Brightest fraction of samples used to estimate the neutral highlight during QR-only enhancement. rectifiedRecoveryRadiusRatio number in module units 0.16 Sampling radius used after rectified enhancement. Geometry micro-refinement Option Type / accepted values Default What it does geometryRefinement boolean true Enables a final bounded micro-refinement pass when structure looks strong but payload decoding still fails. refinementStructureThreshold number 0.90 Minimum structural accuracy required before spending CPU on micro-refinement. refinementOffset number, clamped 0.05..0.35 modules 0.20 Sampling-center offset used to probe a small neighborhood around the detected homography. refinementDecodeThreshold number 0.95 Only refined candidates whose known structural cells score at least this well are sent into the more expensive Reed-Solomon decode. refinementDecodeCandidates integer, clamped 1..8 4 Maximum number of best micro-refined candidates that receive a full decode attempt. Triangle16 / High Density sampling Option Type / accepted values Default What it does highDensitySampling boolean true Allows the scanner to sample both triangle regions of each body module so Triangle16 can be auto-detected. triangle16 boolean true Compatibility gate for Triangle16 sampling. Set false to skip Triangle16 attempts entirely. highDensitySampleInset / triangleSampleInset number, clamped 0.18..0.34 0.27 How far inside each triangle the protected sample anchors sit. Keeps samples away from the diagonal and module borders. highDensitySampleRadius / triangleSampleRadius number in module units Cross: 0.065 Median: 0.055 Local radius around each Triangle16 sample anchor. Scanner diagnostics Option Type / accepted values Default What it does debug boolean false Returns full scanner diagnostics on success and attaches diagnostics to thrown scan errors. Use debugScanImageData() when you want a non-throwing wrapper. debugMatrices boolean true while debug is enabled Controls whether sampled matrix and confidence matrix snapshots are included in full debug output. Set false to reduce debug payload size. Video framing and visible crop Option Type / accepted values Default What it does videoCropMode \"visible\" behavior by default; \"full\" disables cropping visible/automatic For object-fit: cover previews, maps the visible element box back to the matching camera-source rectangle so hidden sensor pixels do not shrink the QR during scanning. videoObjectFit CSS object-fit value computed style, otherwise \"cover\" Overrides the object-fit value used when mapping the visible video crop. Cropping is applied only for cover . videoObjectPosition CSS-like position string computed style, otherwise \"50% 50%\" Controls where the visible crop lies inside the camera source. Supports left/right/top/bottom/center and percentages. videoCropInset fraction, clamped 0..0.18 0 Optionally trims a small extra border from the already-visible crop, useful when preview edges are soft or obstructed. canvas HTMLCanvasElement new scratch canvas Direct scanVideoFrame() option for reusing a caller-owned canvas and reducing allocations. Camera scanner core Option Type / accepted values Default What it does constraints MediaStreamConstraints Environment camera, ideal 1280×720 @ 30 fps Passed directly to navigator.mediaDevices.getUserMedia() . Supply your own object to select a camera or alter resolution/frame-rate constraints. scanInterval number, milliseconds; minimum 24 Worker: 33 Main-thread fallback: 80 Minimum start-to-start cadence for live scan attempts. Worker mode can examine a 30 fps stream frame-for-frame when decoding is fast enough. If one scan takes longer, QuadQR drops stale frames rather than queueing work. Main-thread fallback uses a gentler default to preserve page responsiveness. cameraWorker boolean true Uses a dual-worker camera engine when Worker , createImageBitmap , and worker-side OffscreenCanvas are available. One worker stays dedicated to fresh normal scans while a second worker runs the complete high-resolution/color/perspective/damage recovery stack independently. Set false to force the main-thread engine. cameraWorkerRequired boolean false If true , worker initialization failure is thrown instead of automatically falling back to the full main-thread scanner. Useful when an application requires off-thread scanning. cameraWorkerUrl string or URL-compatible value bundled camera-scanner-worker.js Overrides the module-worker URL. Normally unnecessary; use it when a strict CSP, custom asset host, or bundler requires the worker file at a specific URL. useVideoFrameCallback boolean true when supported Uses requestVideoFrameCallback() to schedule work from real camera frames instead of blindly polling. QuadQR keeps only one scheduled attempt, so stale frames are not queued while a scan is still running. cameraGeometryReuse boolean true Reuses the most recent valid finder/perspective homography on following frames before running a new full finder search. If the code or camera moves, the same frame automatically falls back to normal detection. cameraGeometryReuseMaxMisses integer, minimum 1 5 Number of consecutive frames without a refreshed geometry solution before the cached camera homography is discarded. maxDimension positive pixels Camera scanner: minimum 480, default 640 Direct scanVideoFrame() : 960 Maximum width/height used for the normal fast camera scan. In worker mode the visible camera crop is resized to this dimension before transfer, reducing bitmap/memory overhead. stopOnResult boolean true Stops the camera tracks after the first successful decode. Set false for continuous scanning. multiFrame boolean true Enables confidence-weighted fusion of compatible observations from several missed frames before another Reed-Solomon attempt. multiFrameWindow integer, clamped 2..8 4 Maximum compatible frame observations retained for temporal fusion. multiFrameMinFrames integer, 2..multiFrameWindow 2 Minimum observations required before the scanner tries a fused multi-frame decode. multiFrameMinAgreement ratio 0.62 Minimum high-confidence data-cell agreement required to keep consecutive RGBW observations in the same fusion history. Lower agreement resets the history to avoid mixing two different codes. multiFrameMinAgreementHighDensity ratio 0.58 Triangle16 version of the observation-agreement threshold. It is slightly lower because half-cell sampling is naturally noisier. onResult function (result, frameMeta) => void unset Called on successful decode before optional camera stop. onDecode function (result, frameMeta) => void unset Alias callback invoked alongside onResult for successful decodes. onScanMiss function (error) => void unset Called after the progressive recovery sequence and multi-frame fusion fail for a scan cycle. onDiagnostic function (event) => void unset Receives live camera diagnostic events such as camera-ready , frame , method , and success . Exceptions thrown by this callback are ignored so UI diagnostics cannot break scanning. Camera progressive recovery scheduling Option Type / accepted values Default What it does cameraFinderRecoveryEvery integer, minimum 1 2 Controls how often the more expensive finder-recovery path is allowed after misses. 1 means every missed scan cycle; 2 means every other missed cycle. Recovery is first eligible on the first retry after a miss. cameraAutoEnhanceEvery integer, minimum 1 2 Cadence for progressive Auto Tone / Contrast / Color recovery after misses. Higher values reduce CPU at the cost of slower recovery on difficult frames. cameraAutoColorEvery integer, minimum 1 1 Cadence for camera-specific QuadQR-specific QuadQR Auto Color recovery. The default allows it on every missed cycle because this is a targeted recovery path. cameraAutoColorRecovery boolean true Master switch for camera-specific cropped QuadQR Auto Color recovery after a fast scan miss. cameraAutoColorCropInsets array of fractions, each clamped 0..0.30 [0.08, 0.16, 0.22, 0] Ordered centered crop profiles used by camera QuadQR Auto Color. The scanner tries tighter QR-centric histograms first and full frame last. cameraAutoColorAnalysisInsets array of fractions unset Per-profile histogram analysis insets matching cameraAutoColorCropInsets . Useful for explicitly tuning each crop profile. cameraAutoColorAnalysisInset fraction 0..0.30 profile defaults 0.10, 0.08, 0.04, 0.10 Single fallback analysis inset used when a per-profile value is not supplied. cameraAutoColorBlackClip number 0.0001 Shadow clipping for the camera-specific strong QuadQR Auto Color pass. cameraAutoColorWhiteClip number 0.004 Highlight clipping fallback for camera QuadQR Auto Color. cameraAutoColorHighlightPercentile number 0.70..0.9999 0.95 Bright-reference percentile used by camera QuadQR Auto Color. cameraAutoColorOutputHighlight number, clamped 96..250 190 Target highlight output used by the strong camera QuadQR Auto Color levels mapping. cameraAutoColorMinimumInputRange number 72 Minimum assumed input tonal range for camera QuadQR Auto Color. cameraAutoColorTargetSamples positive integer 90000 Histogram sample budget for camera QuadQR Auto Color. cameraHighResolutionRecovery boolean true Allows a bounded high-resolution retry when the fast pass already sees enough finder patterns but dense modules may have been lost by normal downscaling. cameraHighResolutionMinFinders integer 2 Minimum finder count needed before the high-resolution retry is considered. This prevents wasting the expensive pass on empty frames. cameraHighResolutionMaxDimension positive pixels, never below normal maxDimension 960 Maximum dimension used by the full recovery worker for high-detail geometry/color recovery. cameraHighResolutionEvery integer, minimum 1 2 Cadence for eligible high-resolution retries after misses. cameraRecoveryStrongFinderInterval milliseconds, minimum 80 120 Minimum time between parallel full-recovery dispatches when the fresh-frame worker already sees at least two finder patterns. cameraRecoveryWeakFinderInterval milliseconds 260 Minimum time between full-recovery dispatches after a persistent one-finder candidate has armed recovery. cameraRecoveryWeakFinderFrames integer, minimum 2 2 Consecutive fresh frames that must report exactly one finder before the full-recovery worker is armed. Zero-finder frames never dispatch recovery. Reliability and stress testing Option Type / accepted values Default What it does profiles array of profile objects STRESS_PROFILES or suite-derived reliability profiles Replaces the built-in distortion list. Each profile can define id , label , type , severity , weight , and distortion-specific fields. includeImages boolean false Includes each distorted ImageData image in the returned result rows. Useful for a visual lab UI, but memory-heavy. minVersion / maxVersion integer 1..40 1..40 unless expected.version is supplied Limits scanner version search during each stress case. suite \"quick\" | \"full\" | \"extreme\" \"full\" Reliability Lab only. Selects progressively larger built-in profile sets. axis \"yaw\" | \"pitch\" | \"roll\" | \"z\" \"yaw\" Perspective sweep only. Chooses which 3D angle is varied. angles array of numbers, degrees Yaw/pitch: [0,15,25,35,45,55] Roll: [0,25,45,60,75] Explicit sweep angles. pitchDegrees / yawDegrees / rollDegrees number, degrees 0 Fixed 3D angles for axes not currently being swept, or explicit distortion angles for perspective-3d . cameraDistance number, minimum 1.8 3 Virtual camera distance used by 3D perspective distortion. Lower values increase perspective exaggeration. fill fraction, clamped 0.45..0.94 0.84 How much of the output frame the projected code plane occupies. radius positive number severity-derived Motion-blur sample radius. slope number 0.35 Motion-blur line slope. testImageSize pixels, clamped to at least 240 and no larger than render size 480 assessScanability() raster size used for torture testing, independent of the final export size. Benchmark module Option Type / accepted values Default What it does ecc \"L\" | \"M\" | \"Q\" | \"H\" \"M\" ECC profile used for capacity/performance calculations. highDensity boolean false Benchmarks Triangle16 when enabled. cellEncoding \"rgbw\" | \"triangle16\" derived from highDensity Alternative way to select high-density capacity behavior. versions array of versions Function-specific Versions included in comparison/report output. payload string or Uint8Array unset Concrete payload used by calculateCapacityPlan() . Required if you want actual compression gain measured. payloadBytes non-negative integer 0 Size-only input for capacity planning when no concrete payload is available. Compression ratio cannot be predicted from only a byte count. compression compression mode \"none\" Compression mode included in capacity planning. compressionLevel / lzLevel / deflateLevel / brotliQuality codec-specific integers codec defaults Compression tuning used by capacity planning with concrete payload data. signed boolean false Includes signature-envelope overhead in capacity planning. keyId string unset Includes the signing key ID byte length in capacity planning. embedPublicKey boolean false Includes 32 embedded-public-key bytes in signed capacity planning. iterations positive integer 30 Measured encode/decode iterations per payload size in codec benchmarks. warmup non-negative integer up to 5 or iterations Warm-up encode/decode cycles discarded before timing. payloadSizes array of byte sizes [24,32,128,512,1024,2048] Payload sizes measured by benchmarkCodec() . Node.js-only options Option Type / accepted values Default What it does compressionLevel in encodePNG() number, clamped 0..9 9 Node PNG file compression level. This affects PNG file size only, not the data stored inside QuadQR and not QuadQR payload compression. WebAssembly Option Type / accepted values Default What it does url string or URL bundled quadqr-core.wasm Loads the WASM helper from a custom location. bytes Uint8Array / ArrayBuffer unset Instantiates directly from supplied WASM bytes instead of fetching/reading a URL. Camera callback payloads startCameraScanner() returns { stream, stop, scanNow, video } . stop() clears the loop and stops every media track. scanNow() performs one immediate normal-resolution frame scan using the same options. onResult(result, frameMeta) Field Type Meaning result normal scanner decode result The decoded QuadQR result, including recovery metadata such as recoveryMode , cameraHighResolutionRecovery , or multiFrameCombined when applicable. frameMeta.frame integer Live frame counter at the time the successful frame was captured. frameMeta.imageData ImageData-like The actual normal scan frame used for overlay/debug display. frameMeta.scanWidth / scanHeight pixels Dimensions of the scan copy. frameMeta.sourceRect object or null Camera-source rectangle that was mapped into the scan copy. frameMeta.enhancedImageData ImageData-like or null QuadQR Auto Color corrected crop when that recovery path produced the successful decode. frameMeta.enhancedRect object or null Where the enhanced crop came from inside the scan frame. frameMeta.enhancement object or null Recovery method plus crop/analysis inset metadata. frameMeta.diagnostic object or null Frame diagnostic snapshot associated with the successful result. onDiagnostic(event) Field Type Meaning timestamp number Epoch milliseconds added to every diagnostic event. frame integer Current camera frame number. type string Event family such as camera-ready , frame , method , or success . state string when present Typical values are trying , failed , miss , and decoded . method string Scanner/recovery path responsible for the event, for example fast-scan , camera-auto-color , or multi-frame-confidence-fusion . elapsedMs number when present Time spent by the reported attempt. missStreak integer when present Consecutive live scan cycles that have missed before this event. finderCount integer when present Number of finder candidates in the best vision pass. finders / geometry objects when present Geometry/debug details used by the demo overlay. message string when present Human-readable diagnostic summary. Decode result fields Field Type Meaning ok boolean Successful matrix decode marker. format / formatVersion string / integer \"QuadQR\" and current matrix format version. version / size integer Matrix version and side length in modules. rotation number Detected matrix rotation in degrees: 0, 90, 180, or 270. highDensity / cellEncoding boolean / string Whether Triangle16 was decoded and the physical cell encoding used. bitsPerDataCell / statesPerDataCell integer 2/4 states for RGBW, 4/16 states for Triangle16. eccLevel string Decoded Spectrum ECC profile. correctedSymbols integer Total Reed-Solomon symbols corrected across protected header and body. erasureSymbols integer Symbols recovered using confidence-directed erasure positions. confidenceAssisted boolean Whether erasure decoding was needed. softDecoded boolean Whether bounded alternate-cell substitutions were needed after normal decoding failed. payload Uint8Array Application bytes for normal payloads; encrypted envelope bytes while a secure result is still locked. text string or null UTF-8 text when the payload was encoded as text and is not locked. compressed / compression boolean / string Whether an internal compression envelope was used and which codec was selected. signed boolean Whether the payload carries an Ed25519 signature envelope. signingKeyId string or null Key-routing identifier stored with a signed payload. signatureVerified / signatureTrusted boolean/null Signature state after explicit verification. Trusted means the verifying key came from an external trusted source. secure / requiresDecryption boolean Whether the result contains Secure Payload v1 and still needs credentials. security object or null Inspectable non-secret secure-envelope metadata such as mode, algorithm, KDF, iterations, and key ID. crc32 unsigned integer CRC of the decoded logical payload framing, used for final integrity validation after ECC. confidence number 0..1 on image scans Overall scanner confidence synthesized from cell, structure, geometry, calibration, and ECC use. geometryConfidence / calibrationConfidence / structureConfidence number 0..1 Scanner sub-scores added by image/camera scanning. diagnostics object Compact diagnostics by default, or full diagnostics when debug: true . Encoded code object fields Field Type Meaning matrix 2D integer array Physical QuadQR cell matrix ready for rendering. version / size integer Selected version and matrix side length. maskId integer 0..3 Chosen data-cell mask with the lowest color distribution penalty. payloadBytes integer Bytes actually placed into the matrix after optional extension/encryption layers. sourcePayloadBytes integer Original application payload bytes before internal wrappers. capacityBytes integer Maximum payload bytes available for this exact version/ECC/cell encoding. utilization ratio Meaningful encoded cells divided by total data positions. compressed / compression / compressionLevel fields Compression decision and selected explicit/automatic codec level. compressionStrategy string/null none , explicit , auto , or smart when applicable. smartCompression object/null Smart-mode details such as versions before/after and stronger levels tried. signed / signingKeyId fields Signature-envelope state. secure / security fields Secure Payload state and non-secret metadata. eccParitySymbols / eccBlocks / correctableSymbolsPerBlock integers Physical Reed-Solomon plan for the selected payload/version. crc32 unsigned integer CRC written into the protected body framing. Terminology Term Meaning module One square position in the QuadQR matrix. Structural and RGBW data cells occupy modules; Triangle16 body modules contain two triangular color regions. cell The logical value stored/rendered at a module position. RGBW data cells are values 0..3; Triangle16 body cells pack two RGBW colors into states 0..15. finder pattern Large black/white locator structure used to find the symbol and establish orientation. alignment pattern Distributed smaller structural marker used to refine perspective geometry, especially on larger/dense symbols. quiet zone Blank margin around the matrix, measured in modules. homography Projective transform mapping an angled camera view back to ideal matrix coordinates. calibration Using known structural/color reference cells inside QuadQR to learn how black, white, red, green, and blue appear under current lighting/camera conditions. erasure A Reed-Solomon symbol the scanner explicitly marks as unreliable. Known erasure positions can be easier to recover than unknown errors. soft decoding A bounded second-stage decode that substitutes the scanner’s second-best color hypothesis for a few low-confidence cells when normal ECC still fails. Spectrum ECC 2.0 QuadQR’s confidence-aware decoding pipeline around GF(256) Reed-Solomon, including spectral/spatial interleaving, erasures, and bounded soft alternatives. QuadQR Auto Color Per-channel level correction used primarily to recover finder visibility and strong warm/cool color casts. Auto Tone / Contrast / Color Broader recovery transform that neutralizes highlights, expands per-channel tone/contrast, and applies a conservative saturation boost. multi-frame fusion Combines several compatible camera observations with confidence and recency weighting before another decode attempt. recovery cadence An *Every camera option controlling how many missed scan cycles occur between expensive recovery attempts. Triangle16 Experimental High Density physical layout with a fixed diagonal split and two independently colored RGBW triangle regions per body module. utilization How much of the available data-cell area is occupied by meaningful protected payload/header cells rather than deterministic padding. Previous ← API Reference Next CLI Reference → QuadQR Experimental RGBW matrix symbology. AGPL-3.0 licensed"},{"title":"Encoding, version and ECC","page":"Configuration Reference","url":"options.html#encoding","text":"Encoding, version and ECC Option Type / accepted values Default What it does ecc \"L\" | \"M\" | \"Q\" | \"H\" \"M\" Selects the Spectrum ECC profile. Higher letters add more Reed-Solomon parity and reduce payload capacity. The letters are QuadQR profiles and are not recovery-equivalent to ISO QR ECC letters. version \"auto\" or integer 1..40 \"auto\" Fixes the matrix version or lets QuadQR choose the smallest version that fits the final encoded payload. minVersion integer 1..40 1 Lower bound used by automatic version selection and scanner searches. maxVersion integer 1..40 40 Upper bound used by automatic version selection and scanner searches. Must be greater than or equal to minVersion . highDensity boolean false Convenience switch for experimental Triangle16. true is equivalent to cellEncoding: \"triangle16\" . cellEncoding \"rgbw\" | \"triangle16\" \"rgbw\" Selects the physical data-cell encoding. RGBW carries 2 raw bits per body cell; Triangle16 carries 4 raw bits by splitting each body module into two RGBW triangles. formatVersion integer 6 for new encoding Compatibility guard for encoding. New symbols use Format v6. The scanner retains backward decoding for legacy Format v5 symbols."},{"title":"Compression","page":"Configuration Reference","url":"options.html#compression","text":"Compression Option Type / accepted values Default What it does compression \"none\" | \"auto\" | \"smart\" | \"lz\" | \"deflate\" | \"brotli\" Normal encode: \"none\" Signed encode: \"auto\" Controls the internal payload-extension compression layer. Auto tries balanced LZ/DEFLATE/Brotli passes and keeps compression only when the final payload is smaller. Smart may spend more CPU near version boundaries. compressionLevel integer; codec-specific unset Generic level override for an explicit lz , deflate , or brotli mode. It is rejected for modes where a level does not apply. lzLevel integer 1..9 6 Legacy LZ search depth. Higher values spend more CPU looking for longer back-references. deflateLevel integer 1..9 6 Raw fixed-Huffman DEFLATE search level. Higher values search more candidate chains and use more lazy matching. brotliQuality integer 0..11 11 for explicit Brotli Brotli quality for explicit Brotli compression. Auto uses quality 6; Smart stages qualities 6, 9, then 11 only when useful."},{"title":"Signing","page":"Configuration Reference","url":"options.html#signing","text":"Signing Option Type / accepted values Default What it does privateKey Ed25519 private CryptoKey or PKCS#8 bytes required for signed APIs The signing key. Only the private key can create a signature. publicKey Ed25519 public CryptoKey or 32-byte raw key required only with embedPublicKey: true Public verification key. For normal trusted verification, distribute it separately instead of embedding it. keyId string/bytes, up to 64 bytes for signing empty Small identifier stored with a signature so a scanner can choose the correct trusted public key. embedPublicKey boolean false Embeds the 32-byte public key in the payload envelope. This increases symbol size and does not by itself make that key trusted."},{"title":"Signature verification","page":"Configuration Reference","url":"options.html#signature-verification","text":"Signature verification Option Type / accepted values Default What it does publicKey / trustedPublicKey Ed25519 public key unset Trusted key used to verify the signature. publicKey takes precedence. trustedKeys Map or object keyed by stored keyId unset Trusted-key registry. Lets a signed result route itself to a known public key by key ID. allowEmbeddedKey boolean false for modern envelopes Allows verification using a public key embedded inside the symbol when no external trusted key is supplied. A successful cryptographic check from an embedded key is not marked externally trusted."},{"title":"Secure Payload","page":"Configuration Reference","url":"options.html#secure-payload","text":"Secure Payload Option Type / accepted values Default What it does security.mode \"password\" | \"raw-key\" required Selects Secure Payload v1 credential mode. security.password non-empty string required in password mode Password input to PBKDF2-HMAC-SHA-256, which derives the AES-256-GCM key. security.iterations integer 100000..2000000 600000 PBKDF2 iteration count stored in the encrypted envelope. Higher values cost more CPU for both encryption and decryption. security.key 32-byte key or 64-hex-character string required in raw-key mode Exact AES-256 key material. No password KDF is used. security.keyId false , string, or bytes up to 32 bytes Password: none Raw key: automatic 8-byte fingerprint Routing hint for selecting a decryption key. Set false to omit it. It is not secret and is authenticated by AES-GCM. verifyKeyId boolean true Raw-key decryption option. When an automatic key fingerprint exists, checks it before attempting AES-GCM. Set false only if you intentionally want to skip that fast mismatch check."},{"title":"Matrix decoding and Spectrum ECC 2.0","page":"Configuration Reference","url":"options.html#matrix-decoding","text":"Matrix decoding and Spectrum ECC 2.0 Option Type / accepted values Default What it does structureTolerance number, normally 0..1 0 in direct decodeMatrix() Fractional tolerance for structural finder/alignment mismatches when decoding an already sampled matrix. Image scanning supplies a more forgiving scanner-path value. cellConfidence square number matrix matching the code matrix unset Per-cell confidence values from 0 to 1 . Enables confidence-aware error/erasure decoding. cellAlternatives square matrix of alternative cell values unset Second-best cell hypotheses used by bounded Spectrum ECC 2.0 soft decoding after normal decoding fails. cellEncodingHint \"rgbw\" | \"triangle16\" auto-detect Restricts the first physical-cell interpretation attempt. Primarily useful when feeding scanner-produced sampled matrices. maxErasureConfidence number 0.68 Cells/symbols at or below this confidence can be promoted to Reed-Solomon erasures after hard decoding fails. Lower values are more conservative. softDecoding boolean true when confidence + alternatives exist Allows bounded alternate-cell substitution attempts after hard/error-erasure decoding fails. Set false to disable the soft stage. softDecodeConfidence number, clamped 0.05..0.95 0.72 Only cells at or below this confidence are considered for alternate-cell soft decoding. softDecodeMaxCells integer, clamped 2..16 10 Maximum number of low-confidence cells considered as soft-decoding candidates. softDecodePairCells integer, 2..softDecodeMaxCells 6 Limits the candidate subset used for two-cell substitution combinations, keeping the search bounded."},{"title":"Rendering","page":"Configuration Reference","url":"options.html#rendering","text":"Rendering Option Type / accepted values Default What it does imageSize positive number, pixels 720 when neither size option is set Exact square output dimension. The renderer distributes fractional module sizes across pixels so the final output stays exactly this size. moduleSize positive number, pixels/module unset Low-level sizing mode used only when imageSize is omitted. The actual output is (matrix + quiet zone) × moduleSize . quietZone non-negative integer modules 4 Blank border around the symbol. The requested value is honored in both screen and print modes; four modules remains recommended. mode / renderMode \"screen\" | \"print\" \"screen\" Selects screen or print rendering behavior. Print mode uses the darker print palette and conservative safety rules. style \"classic\" | \"depth\" | \"soft\" | \"inset\" \"classic\" Visual data-cell treatment. Structural cells remain exact. Triangle16 body cells are always rendered with hard-edged triangle geometry. palette object with black, white, red, green, blue hex colors screen or print built-in palette Overrides any subset of the active palette. allowStyledPrint boolean false Allows a non-classic style in print mode. Without this explicit opt-in, print mode forces classic for reliability."},{"title":"Logo rendering","page":"Configuration Reference","url":"options.html#logo-rendering","text":"Logo rendering Option Type / accepted values Default What it does logo CanvasImageSource, ImageData-like object, URL/data URL for SVG, or nested logo object unset Adds a centered logo. The accepted source form depends on the renderer. logo.source renderer-compatible image source required for nested logo object Actual image source. Canvas needs a loaded image/canvas; ImageData rendering needs RGBA data; SVG accepts a URL/data URL. logo.size / logoSize ratio 0.05..0.30 or \"auto\" 0.18 Logo width as a fraction of the matrix width. \"auto\" uses estimateSafeLogoSize() . logo.clearBackground / logoClearBackground boolean false Paints a solid rounded background behind the logo before drawing it, clearing covered cells. logo.padding / logoPadding non-negative number, modules 0.65 Padding added around the logo when clear-background mode is enabled. logo.radius / logoRadius non-negative number, modules 0.8 Corner radius of the clear-background area. logo.backgroundColor / logoBackgroundColor CSS hex color active palette white Background color used by clear-background mode."},{"title":"Logo safety analysis","page":"Configuration Reference","url":"options.html#logo-safety-analysis","text":"Logo safety analysis Option Type / accepted values Default What it does ecc ECC letter code ECC or \"M\" Used by estimateSafeLogoSize() when a full encoded code object is not supplied. utilization ratio 0..1 code utilization or 0.65 Approximate occupied-data ratio used to reduce the recommended logo size for dense symbols. clearBackground / logoClearBackground boolean false Applies an additional safety penalty because clearing cells removes more encoded information. mode / renderMode \"screen\" | \"print\" \"screen\" Print mode uses a more conservative logo estimate. minSize ratio 0.05 Lower bound of the empirical findMaxSafeLogoSize() binary search. maxSize ratio, capped at 0.30 0.28 Upper bound of the empirical logo-size search. iterations integer, clamped 3..10 7 Binary-search iterations in findMaxSafeLogoSize() ."},{"title":"Print guidance","page":"Configuration Reference","url":"options.html#print-guidance","text":"Print guidance Option Type / accepted values Default What it does quietZone integer modules, minimum 4 in guidance 4 Quiet zone assumed for physical-size guidance. physicalSizeMm positive number, millimeters 35 Target total printed symbol width including quiet zone. dpi positive number 300 Printer resolution used to report pixels per module. minimumModuleMm positive number, millimeters 0.40 Recommended minimum physical module size used for pass/fail guidance."},{"title":"Image scanner: geometry and fallback","page":"Configuration Reference","url":"options.html#scanner-geometry","text":"Image scanner: geometry and fallback Option Type / accepted values Default What it does minVersion integer 1..40 1 Lowest symbol version geometry detection will consider. maxVersion integer 1..40 40 Highest symbol version geometry detection will consider. perspective boolean true Enables finder/alignment detection and homography-based projective correction. Set false only for known axis-aligned input. axisAlignedFallback boolean true After perspective attempts fail, tries a rectangular active-bounds sampler suitable for already straight/cropped images. maxGeometryCandidates positive integer 8 Maximum perspective geometry candidates examined per finder pass. finderRecovery boolean true Allows additional finder passes after the fast RGB-value/Otsu pass, including two-finder recovery, QuadQR Auto Color-assisted finder detection, threshold bracketing, and luminance fallback. preciseAlignment boolean false Requests finer alignment localization on the current geometry pass. Normally the scanner uses this only in the bounded recovery path. preciseAlignmentRecovery boolean true If normal perspective geometry exists but does not decode, retries once with precise alignment enabled. whiteThreshold number 0..255 238 Axis-aligned fallback threshold used to find the non-white active image bounds. maxModuleAspectError non-negative ratio 0.16 Axis-aligned fallback rejects candidate grids whose estimated horizontal/vertical module sizes differ by more than this ratio. bounds {x,y,width,height} auto-detected Supplies explicit active bounds for axis-aligned scanning instead of finding them from near-white pixels. palette RGBW palette object built-in palette Fixed color reference used by the axis-aligned fallback. Perspective scanning normally calibrates colors from the symbol itself."},{"title":"Image scanner: sampling and calibration","page":"Configuration Reference","url":"options.html#scanner-sampling","text":"Image scanner: sampling and calibration Option Type / accepted values Default What it does sampleMode \"cross\" | \"median\" \"cross\" Primary module sampler. Cross averages the center plus four nearby samples; median uses a compact 3×3 patch for dirt/blur rejection. sampleRadius number in module units Perspective: 0.16 Axis fallback: 0.18 Distance from module center for the primary sample pattern. Smaller values avoid edges; larger values average more of the module. adaptiveSampling boolean true Adds a robust median sampling pass after the fast sampling profile when needed. robustSampleRadius number in module units Perspective: 0.12 Axis fallback: 0.10 Radius used by the robust median sampling profile. spatialColorNormalization boolean true Uses distributed black/white calibration references to correct spatial lighting gradients before classification. structureTolerance number Perspective decode: 0.18 Axis fallback: 0.12 How much structural mismatch the scanner allows after sampled colors are classified. Increase cautiously because loose values can admit false geometry. includeRectified boolean false Adds a perspective-corrected QR-only ImageData image to a successful result. rectifiedModuleSize positive number 8 Pixels per module used for the optional rectified result image."},{"title":"QuadQR Finder Auto Color recovery","page":"Configuration Reference","url":"options.html#finder-auto-color","text":"QuadQR Finder Auto Color recovery Option Type / accepted values Default What it does finderAutoColorBlackClip number, effectively clamped 0..0.06 0.0001 Per-channel shadow percentile clipped during QuadQR Auto Color-assisted finder recovery. finderAutoColorWhiteClip number, clamped 0..0.06 0.004 Per-channel highlight percentile clipped when a direct output-highlight anchor is not used. finderAutoColorHighlightPercentile number 0.70..0.9999 0.95 Bright-reference percentile used to estimate a neutral highlight during QuadQR finder Auto Color recovery. finderAutoColorOutputHighlight number, clamped 96..250 190 Maps the observed highlight reference toward this output value, strengthening black and RGB separation instead of stretching paper white all the way to 255. finderAutoColorAnalysisInset fraction 0..0.30 recovery tries 0.10, 0.20, 0.04 Restricts histogram analysis toward the image center while applying the correction to the full frame. Set a single value to replace the built-in center-weighted sequence. finderAutoColorMinimumInputRange number, minimum 32 internally 72 Minimum per-channel input tonal span assumed while computing strong QuadQR Auto Color levels. finderAutoColorTargetSamples positive integer 90000 Approximate histogram sample budget used by QuadQR finder Auto Color analysis."},{"title":"Auto Tone / Contrast / Color recovery","page":"Configuration Reference","url":"options.html#auto-enhance","text":"Auto Tone / Contrast / Color recovery Option Type / accepted values Default What it does autoEnhanceRecovery boolean true Master switch for Auto Tone / Contrast / Color recovery after normal classification fails. fullFrameAutoEnhanceRecovery boolean Static image: true Camera fast path: false Allows a full-frame enhanced retry after geometry was found. Camera scanning normally avoids this expensive operation and prefers QR-region/cropped recovery. autoEnhanceWhenNoGeometry boolean Static image: true If even finder geometry is missing, allows one enhanced full-image retry. Camera scheduling enables it only on selected missed frames. autoEnhanceBlackClip number, clamped 0..0.08 0.006 Shadow clipping used by Auto Tone/Color recovery. autoEnhanceWhiteClip number, clamped 0..0.08 0.004 Highlight clipping used by Auto Tone/Color recovery. autoEnhanceSaturation number, clamped 1..1.5 1.12 Conservative saturation boost after white balance and tonal expansion. autoEnhanceTargetSamples positive integer, minimum 8000 160000 Approximate pixel sample budget for full-image enhancement statistics. rectifiedAutoEnhanceRecovery boolean true When geometry is known but decoding still fails, rectifies only the QR area, enhances it, and resamples it. This avoids unrelated scene pixels skewing histograms. rectifiedRecoveryModuleSize integer, clamped 4..10 6 Pixels per module used to build the temporary QR-only image for rectified recovery. rectifiedAutoEnhanceBlackClip number 0.008 Stronger shadow clipping for QR-only rectified enhancement. rectifiedAutoEnhanceWhiteClip number 0.006 Highlight clipping for QR-only rectified enhancement. rectifiedAutoEnhanceSaturation number 1.22 Saturation boost for QR-only rectified enhancement. rectifiedAutoEnhanceHighlightFraction number 0.16 Brightest fraction of samples used to estimate the neutral highlight during QR-only enhancement. rectifiedRecoveryRadiusRatio number in module units 0.16 Sampling radius used after rectified enhancement."},{"title":"Geometry micro-refinement","page":"Configuration Reference","url":"options.html#geometry-refinement","text":"Geometry micro-refinement Option Type / accepted values Default What it does geometryRefinement boolean true Enables a final bounded micro-refinement pass when structure looks strong but payload decoding still fails. refinementStructureThreshold number 0.90 Minimum structural accuracy required before spending CPU on micro-refinement. refinementOffset number, clamped 0.05..0.35 modules 0.20 Sampling-center offset used to probe a small neighborhood around the detected homography. refinementDecodeThreshold number 0.95 Only refined candidates whose known structural cells score at least this well are sent into the more expensive Reed-Solomon decode. refinementDecodeCandidates integer, clamped 1..8 4 Maximum number of best micro-refined candidates that receive a full decode attempt."},{"title":"Triangle16 / High Density sampling","page":"Configuration Reference","url":"options.html#high-density-sampling","text":"Triangle16 / High Density sampling Option Type / accepted values Default What it does highDensitySampling boolean true Allows the scanner to sample both triangle regions of each body module so Triangle16 can be auto-detected. triangle16 boolean true Compatibility gate for Triangle16 sampling. Set false to skip Triangle16 attempts entirely. highDensitySampleInset / triangleSampleInset number, clamped 0.18..0.34 0.27 How far inside each triangle the protected sample anchors sit. Keeps samples away from the diagonal and module borders. highDensitySampleRadius / triangleSampleRadius number in module units Cross: 0.065 Median: 0.055 Local radius around each Triangle16 sample anchor."},{"title":"Scanner diagnostics","page":"Configuration Reference","url":"options.html#scanner-diagnostics","text":"Scanner diagnostics Option Type / accepted values Default What it does debug boolean false Returns full scanner diagnostics on success and attaches diagnostics to thrown scan errors. Use debugScanImageData() when you want a non-throwing wrapper. debugMatrices boolean true while debug is enabled Controls whether sampled matrix and confidence matrix snapshots are included in full debug output. Set false to reduce debug payload size."},{"title":"Video framing and visible crop","page":"Configuration Reference","url":"options.html#video-framing","text":"Video framing and visible crop Option Type / accepted values Default What it does videoCropMode \"visible\" behavior by default; \"full\" disables cropping visible/automatic For object-fit: cover previews, maps the visible element box back to the matching camera-source rectangle so hidden sensor pixels do not shrink the QR during scanning. videoObjectFit CSS object-fit value computed style, otherwise \"cover\" Overrides the object-fit value used when mapping the visible video crop. Cropping is applied only for cover . videoObjectPosition CSS-like position string computed style, otherwise \"50% 50%\" Controls where the visible crop lies inside the camera source. Supports left/right/top/bottom/center and percentages. videoCropInset fraction, clamped 0..0.18 0 Optionally trims a small extra border from the already-visible crop, useful when preview edges are soft or obstructed. canvas HTMLCanvasElement new scratch canvas Direct scanVideoFrame() option for reusing a caller-owned canvas and reducing allocations."},{"title":"Camera scanner core","page":"Configuration Reference","url":"options.html#camera-core","text":"Camera scanner core Option Type / accepted values Default What it does constraints MediaStreamConstraints Environment camera, ideal 1280×720 @ 30 fps Passed directly to navigator.mediaDevices.getUserMedia() . Supply your own object to select a camera or alter resolution/frame-rate constraints. scanInterval number, milliseconds; minimum 24 Worker: 33 Main-thread fallback: 80 Minimum start-to-start cadence for live scan attempts. Worker mode can examine a 30 fps stream frame-for-frame when decoding is fast enough. If one scan takes longer, QuadQR drops stale frames rather than queueing work. Main-thread fallback uses a gentler default to preserve page responsiveness. cameraWorker boolean true Uses a dual-worker camera engine when Worker , createImageBitmap , and worker-side OffscreenCanvas are available. One worker stays dedicated to fresh normal scans while a second worker runs the complete high-resolution/color/perspective/damage recovery stack independently. Set false to force the main-thread engine. cameraWorkerRequired boolean false If true , worker initialization failure is thrown instead of automatically falling back to the full main-thread scanner. Useful when an application requires off-thread scanning. cameraWorkerUrl string or URL-compatible value bundled camera-scanner-worker.js Overrides the module-worker URL. Normally unnecessary; use it when a strict CSP, custom asset host, or bundler requires the worker file at a specific URL. useVideoFrameCallback boolean true when supported Uses requestVideoFrameCallback() to schedule work from real camera frames instead of blindly polling. QuadQR keeps only one scheduled attempt, so stale frames are not queued while a scan is still running. cameraGeometryReuse boolean true Reuses the most recent valid finder/perspective homography on following frames before running a new full finder search. If the code or camera moves, the same frame automatically falls back to normal detection. cameraGeometryReuseMaxMisses integer, minimum 1 5 Number of consecutive frames without a refreshed geometry solution before the cached camera homography is discarded. maxDimension positive pixels Camera scanner: minimum 480, default 640 Direct scanVideoFrame() : 960 Maximum width/height used for the normal fast camera scan. In worker mode the visible camera crop is resized to this dimension before transfer, reducing bitmap/memory overhead. stopOnResult boolean true Stops the camera tracks after the first successful decode. Set false for continuous scanning. multiFrame boolean true Enables confidence-weighted fusion of compatible observations from several missed frames before another Reed-Solomon attempt. multiFrameWindow integer, clamped 2..8 4 Maximum compatible frame observations retained for temporal fusion. multiFrameMinFrames integer, 2..multiFrameWindow 2 Minimum observations required before the scanner tries a fused multi-frame decode. multiFrameMinAgreement ratio 0.62 Minimum high-confidence data-cell agreement required to keep consecutive RGBW observations in the same fusion history. Lower agreement resets the history to avoid mixing two different codes. multiFrameMinAgreementHighDensity ratio 0.58 Triangle16 version of the observation-agreement threshold. It is slightly lower because half-cell sampling is naturally noisier. onResult function (result, frameMeta) => void unset Called on successful decode before optional camera stop. onDecode function (result, frameMeta) => void unset Alias callback invoked alongside onResult for successful decodes. onScanMiss function (error) => void unset Called after the progressive recovery sequence and multi-frame fusion fail for a scan cycle. onDiagnostic function (event) => void unset Receives live camera diagnostic events such as camera-ready , frame , method , and success . Exceptions thrown by this callback are ignored so UI diagnostics cannot break scanning. Continuous mode: continuous true keeps the camera active and resets scanner freshness between symbols. duplicateCooldown defaults to 1200 ms in continuous mode and suppresses the same CRC result without extra image work. pauseWhenHidden defaults to true. The returned scanner exposes pause() and resume(). signal accepts an AbortSignal for cancellation. onCameraState reports running, paused, ended, and stopped states. Main-thread fallback recovery is candidate-gated by finder or geometry evidence instead of miss count alone."},{"title":"Camera progressive recovery scheduling","page":"Configuration Reference","url":"options.html#camera-recovery","text":"Camera progressive recovery scheduling Option Type / accepted values Default What it does cameraFinderRecoveryEvery integer, minimum 1 2 Controls how often the more expensive finder-recovery path is allowed after misses. 1 means every missed scan cycle; 2 means every other missed cycle. Recovery is first eligible on the first retry after a miss. cameraAutoEnhanceEvery integer, minimum 1 2 Cadence for progressive Auto Tone / Contrast / Color recovery after misses. Higher values reduce CPU at the cost of slower recovery on difficult frames. cameraAutoColorEvery integer, minimum 1 1 Cadence for camera-specific QuadQR-specific QuadQR Auto Color recovery. The default allows it on every missed cycle because this is a targeted recovery path. cameraAutoColorRecovery boolean true Master switch for camera-specific cropped QuadQR Auto Color recovery after a fast scan miss. cameraAutoColorCropInsets array of fractions, each clamped 0..0.30 [0.08, 0.16, 0.22, 0] Ordered centered crop profiles used by camera QuadQR Auto Color. The scanner tries tighter QR-centric histograms first and full frame last. cameraAutoColorAnalysisInsets array of fractions unset Per-profile histogram analysis insets matching cameraAutoColorCropInsets . Useful for explicitly tuning each crop profile. cameraAutoColorAnalysisInset fraction 0..0.30 profile defaults 0.10, 0.08, 0.04, 0.10 Single fallback analysis inset used when a per-profile value is not supplied. cameraAutoColorBlackClip number 0.0001 Shadow clipping for the camera-specific strong QuadQR Auto Color pass. cameraAutoColorWhiteClip number 0.004 Highlight clipping fallback for camera QuadQR Auto Color. cameraAutoColorHighlightPercentile number 0.70..0.9999 0.95 Bright-reference percentile used by camera QuadQR Auto Color. cameraAutoColorOutputHighlight number, clamped 96..250 190 Target highlight output used by the strong camera QuadQR Auto Color levels mapping. cameraAutoColorMinimumInputRange number 72 Minimum assumed input tonal range for camera QuadQR Auto Color. cameraAutoColorTargetSamples positive integer 90000 Histogram sample budget for camera QuadQR Auto Color. cameraHighResolutionRecovery boolean true Allows a bounded high-resolution retry when the fast pass already sees enough finder patterns but dense modules may have been lost by normal downscaling. cameraHighResolutionMinFinders integer 2 Minimum finder count needed before the high-resolution retry is considered. This prevents wasting the expensive pass on empty frames. cameraHighResolutionMaxDimension positive pixels, never below normal maxDimension 960 Maximum dimension used by the full recovery worker for high-detail geometry/color recovery. cameraHighResolutionEvery integer, minimum 1 2 Cadence for eligible high-resolution retries after misses. cameraRecoveryStrongFinderInterval milliseconds, minimum 80 120 Minimum time between parallel full-recovery dispatches when the fresh-frame worker already sees at least two finder patterns. cameraRecoveryWeakFinderInterval milliseconds 260 Minimum time between full-recovery dispatches after a persistent one-finder candidate has armed recovery. cameraRecoveryWeakFinderFrames integer, minimum 2 2 Consecutive fresh frames that must report exactly one finder before the full-recovery worker is armed. Zero-finder frames never dispatch recovery."},{"title":"Reliability and stress testing","page":"Configuration Reference","url":"options.html#reliability-options","text":"Reliability and stress testing Option Type / accepted values Default What it does profiles array of profile objects STRESS_PROFILES or suite-derived reliability profiles Replaces the built-in distortion list. Each profile can define id , label , type , severity , weight , and distortion-specific fields. includeImages boolean false Includes each distorted ImageData image in the returned result rows. Useful for a visual lab UI, but memory-heavy. minVersion / maxVersion integer 1..40 1..40 unless expected.version is supplied Limits scanner version search during each stress case. suite \"quick\" | \"full\" | \"extreme\" \"full\" Reliability Lab only. Selects progressively larger built-in profile sets. axis \"yaw\" | \"pitch\" | \"roll\" | \"z\" \"yaw\" Perspective sweep only. Chooses which 3D angle is varied. angles array of numbers, degrees Yaw/pitch: [0,15,25,35,45,55] Roll: [0,25,45,60,75] Explicit sweep angles. pitchDegrees / yawDegrees / rollDegrees number, degrees 0 Fixed 3D angles for axes not currently being swept, or explicit distortion angles for perspective-3d . cameraDistance number, minimum 1.8 3 Virtual camera distance used by 3D perspective distortion. Lower values increase perspective exaggeration. fill fraction, clamped 0.45..0.94 0.84 How much of the output frame the projected code plane occupies. radius positive number severity-derived Motion-blur sample radius. slope number 0.35 Motion-blur line slope. testImageSize pixels, clamped to at least 240 and no larger than render size 480 assessScanability() raster size used for torture testing, independent of the final export size."},{"title":"Benchmark module","page":"Configuration Reference","url":"options.html#benchmark-options","text":"Benchmark module Option Type / accepted values Default What it does ecc \"L\" | \"M\" | \"Q\" | \"H\" \"M\" ECC profile used for capacity/performance calculations. highDensity boolean false Benchmarks Triangle16 when enabled. cellEncoding \"rgbw\" | \"triangle16\" derived from highDensity Alternative way to select high-density capacity behavior. versions array of versions Function-specific Versions included in comparison/report output. payload string or Uint8Array unset Concrete payload used by calculateCapacityPlan() . Required if you want actual compression gain measured. payloadBytes non-negative integer 0 Size-only input for capacity planning when no concrete payload is available. Compression ratio cannot be predicted from only a byte count. compression compression mode \"none\" Compression mode included in capacity planning. compressionLevel / lzLevel / deflateLevel / brotliQuality codec-specific integers codec defaults Compression tuning used by capacity planning with concrete payload data. signed boolean false Includes signature-envelope overhead in capacity planning. keyId string unset Includes the signing key ID byte length in capacity planning. embedPublicKey boolean false Includes 32 embedded-public-key bytes in signed capacity planning. iterations positive integer 30 Measured encode/decode iterations per payload size in codec benchmarks. warmup non-negative integer up to 5 or iterations Warm-up encode/decode cycles discarded before timing. payloadSizes array of byte sizes [24,32,128,512,1024,2048] Payload sizes measured by benchmarkCodec() ."},{"title":"Node.js-only options","page":"Configuration Reference","url":"options.html#node-options","text":"Node.js-only options Option Type / accepted values Default What it does compressionLevel in encodePNG() number, clamped 0..9 9 Node PNG file compression level. This affects PNG file size only, not the data stored inside QuadQR and not QuadQR payload compression."},{"title":"WebAssembly","page":"Configuration Reference","url":"options.html#wasm-options","text":"WebAssembly Option Type / accepted values Default What it does url string or URL bundled quadqr-core.wasm Loads the WASM helper from a custom location. bytes Uint8Array / ArrayBuffer unset Instantiates directly from supplied WASM bytes instead of fetching/reading a URL."},{"title":"Camera callback payloads","page":"Configuration Reference","url":"options.html#camera-callback-payloads","text":"Camera callback payloads startCameraScanner() returns { stream, stop, scanNow, video } . stop() clears the loop and stops every media track. scanNow() performs one immediate normal-resolution frame scan using the same options. onResult(result, frameMeta) # Field Type Meaning result normal scanner decode result The decoded QuadQR result, including recovery metadata such as recoveryMode , cameraHighResolutionRecovery , or multiFrameCombined when applicable. frameMeta.frame integer Live frame counter at the time the successful frame was captured. frameMeta.imageData ImageData-like The actual normal scan frame used for overlay/debug display. frameMeta.scanWidth / scanHeight pixels Dimensions of the scan copy. frameMeta.sourceRect object or null Camera-source rectangle that was mapped into the scan copy. frameMeta.enhancedImageData ImageData-like or null QuadQR Auto Color corrected crop when that recovery path produced the successful decode. frameMeta.enhancedRect object or null Where the enhanced crop came from inside the scan frame. frameMeta.enhancement object or null Recovery method plus crop/analysis inset metadata. frameMeta.diagnostic object or null Frame diagnostic snapshot associated with the successful result. onDiagnostic(event) # Field Type Meaning timestamp number Epoch milliseconds added to every diagnostic event. frame integer Current camera frame number. type string Event family such as camera-ready , frame , method , or success . state string when present Typical values are trying , failed , miss , and decoded . method string Scanner/recovery path responsible for the event, for example fast-scan , camera-auto-color , or multi-frame-confidence-fusion . elapsedMs number when present Time spent by the reported attempt. missStreak integer when present Consecutive live scan cycles that have missed before this event. finderCount integer when present Number of finder candidates in the best vision pass. finders / geometry objects when present Geometry/debug details used by the demo overlay. message string when present Human-readable diagnostic summary."},{"title":"onResult(result, frameMeta)","page":"Configuration Reference","url":"options.html#onresult","text":"onResult(result, frameMeta) Field Type Meaning result normal scanner decode result The decoded QuadQR result, including recovery metadata such as recoveryMode , cameraHighResolutionRecovery , or multiFrameCombined when applicable. frameMeta.frame integer Live frame counter at the time the successful frame was captured. frameMeta.imageData ImageData-like The actual normal scan frame used for overlay/debug display. frameMeta.scanWidth / scanHeight pixels Dimensions of the scan copy. frameMeta.sourceRect object or null Camera-source rectangle that was mapped into the scan copy. frameMeta.enhancedImageData ImageData-like or null QuadQR Auto Color corrected crop when that recovery path produced the successful decode. frameMeta.enhancedRect object or null Where the enhanced crop came from inside the scan frame. frameMeta.enhancement object or null Recovery method plus crop/analysis inset metadata. frameMeta.diagnostic object or null Frame diagnostic snapshot associated with the successful result."},{"title":"onDiagnostic(event)","page":"Configuration Reference","url":"options.html#ondiagnostic","text":"onDiagnostic(event) Field Type Meaning timestamp number Epoch milliseconds added to every diagnostic event. frame integer Current camera frame number. type string Event family such as camera-ready , frame , method , or success . state string when present Typical values are trying , failed , miss , and decoded . method string Scanner/recovery path responsible for the event, for example fast-scan , camera-auto-color , or multi-frame-confidence-fusion . elapsedMs number when present Time spent by the reported attempt. missStreak integer when present Consecutive live scan cycles that have missed before this event. finderCount integer when present Number of finder candidates in the best vision pass. finders / geometry objects when present Geometry/debug details used by the demo overlay. message string when present Human-readable diagnostic summary."},{"title":"Decode result fields","page":"Configuration Reference","url":"options.html#decode-result-fields","text":"Decode result fields Field Type Meaning ok boolean Successful matrix decode marker. format / formatVersion string / integer \"QuadQR\" and current matrix format version. version / size integer Matrix version and side length in modules. rotation number Detected matrix rotation in degrees: 0, 90, 180, or 270. highDensity / cellEncoding boolean / string Whether Triangle16 was decoded and the physical cell encoding used. bitsPerDataCell / statesPerDataCell integer 2/4 states for RGBW, 4/16 states for Triangle16. eccLevel string Decoded Spectrum ECC profile. correctedSymbols integer Total Reed-Solomon symbols corrected across protected header and body. erasureSymbols integer Symbols recovered using confidence-directed erasure positions. confidenceAssisted boolean Whether erasure decoding was needed. softDecoded boolean Whether bounded alternate-cell substitutions were needed after normal decoding failed. payload Uint8Array Application bytes for normal payloads; encrypted envelope bytes while a secure result is still locked. text string or null UTF-8 text when the payload was encoded as text and is not locked. compressed / compression boolean / string Whether an internal compression envelope was used and which codec was selected. signed boolean Whether the payload carries an Ed25519 signature envelope. signingKeyId string or null Key-routing identifier stored with a signed payload. signatureVerified / signatureTrusted boolean/null Signature state after explicit verification. Trusted means the verifying key came from an external trusted source. secure / requiresDecryption boolean Whether the result contains Secure Payload v1 and still needs credentials. security object or null Inspectable non-secret secure-envelope metadata such as mode, algorithm, KDF, iterations, and key ID. crc32 unsigned integer CRC of the decoded logical payload framing, used for final integrity validation after ECC. confidence number 0..1 on image scans Overall scanner confidence synthesized from cell, structure, geometry, calibration, and ECC use. geometryConfidence / calibrationConfidence / structureConfidence number 0..1 Scanner sub-scores added by image/camera scanning. diagnostics object Compact diagnostics by default, or full diagnostics when debug: true ."},{"title":"Encoded code object fields","page":"Configuration Reference","url":"options.html#encoded-code-fields","text":"Encoded code object fields Field Type Meaning matrix 2D integer array Physical QuadQR cell matrix ready for rendering. version / size integer Selected version and matrix side length. maskId integer 0..3 Chosen data-cell mask with the lowest color distribution penalty. payloadBytes integer Bytes actually placed into the matrix after optional extension/encryption layers. sourcePayloadBytes integer Original application payload bytes before internal wrappers. capacityBytes integer Maximum payload bytes available for this exact version/ECC/cell encoding. utilization ratio Meaningful encoded cells divided by total data positions. compressed / compression / compressionLevel fields Compression decision and selected explicit/automatic codec level. compressionStrategy string/null none , explicit , auto , or smart when applicable. smartCompression object/null Smart-mode details such as versions before/after and stronger levels tried. signed / signingKeyId fields Signature-envelope state. secure / security fields Secure Payload state and non-secret metadata. eccParitySymbols / eccBlocks / correctableSymbolsPerBlock integers Physical Reed-Solomon plan for the selected payload/version. crc32 unsigned integer CRC written into the protected body framing."},{"title":"Terminology","page":"Configuration Reference","url":"options.html#terminology","text":"Terminology Term Meaning module One square position in the QuadQR matrix. Structural and RGBW data cells occupy modules; Triangle16 body modules contain two triangular color regions. cell The logical value stored/rendered at a module position. RGBW data cells are values 0..3; Triangle16 body cells pack two RGBW colors into states 0..15. finder pattern Large black/white locator structure used to find the symbol and establish orientation. alignment pattern Distributed smaller structural marker used to refine perspective geometry, especially on larger/dense symbols. quiet zone Blank margin around the matrix, measured in modules. homography Projective transform mapping an angled camera view back to ideal matrix coordinates. calibration Using known structural/color reference cells inside QuadQR to learn how black, white, red, green, and blue appear under current lighting/camera conditions. erasure A Reed-Solomon symbol the scanner explicitly marks as unreliable. Known erasure positions can be easier to recover than unknown errors. soft decoding A bounded second-stage decode that substitutes the scanner’s second-best color hypothesis for a few low-confidence cells when normal ECC still fails. Spectrum ECC 2.0 QuadQR’s confidence-aware decoding pipeline around GF(256) Reed-Solomon, including spectral/spatial interleaving, erasures, and bounded soft alternatives. QuadQR Auto Color Per-channel level correction used primarily to recover finder visibility and strong warm/cool color casts. Auto Tone / Contrast / Color Broader recovery transform that neutralizes highlights, expands per-channel tone/contrast, and applies a conservative saturation boost. multi-frame fusion Combines several compatible camera observations with confidence and recency weighting before another decode attempt. recovery cadence An *Every camera option controlling how many missed scan cycles occur between expensive recovery attempts. Triangle16 Experimental High Density physical layout with a fixed diagonal split and two independently colored RGBW triangle regions per body module. utilization How much of the available data-cell area is occupied by meaningful protected payload/header cells rather than deterministic padding. Previous ← API Reference Next CLI Reference → QuadQR Experimental RGBW matrix symbology. AGPL-3.0 licensed"},{"title":"Reliability Lab","page":"Reliability Lab","url":"reliability.html","text":"QUADQR DOCUMENTATION Reliability Lab Complete option definitions All Reliability Lab, stress-distortion, and perspective-sweep parameters are listed in the Configuration Reference . QuadQR includes a deterministic Reliability Lab for regression testing generated symbols against camera-style damage. It is intended for comparing scanner changes, render settings, ECC profiles, output size, and the experimental High Density Mode under repeatable conditions. The lab verifies the decoded payload CRC. Finding three locators is not counted as success unless the final payload is recovered correctly. Browser demo Open the Reliability Lab tab after generating a QuadQR. The lab provides: Quick, Full, and Extreme suites; per-scenario pass/fail, confidence, Reed-Solomon correction count, and decode time; category-level scoring for optics, lighting, color, resampling, sensor damage, and perspective; a 3D perspective playground with X pitch, Y yaw, and Z rotation controls; angle sweeps to find the largest tested pitch, yaw, or Z rotation that still decodes. X pitch and Y yaw simulate a code plane tilting in depth. Z rotation is an in-plane rotation. The controls can be combined. Public API JS Copy import { applyStressDistortion, runReliabilityLab, runPerspectiveSweep } from \"quadqr-js\"; const transformed = applyStressDistortion(imageData, \"perspective-3d\", 0.5, { pitchDegrees: 20, yawDegrees: 45, rollDegrees: 15 }); const report = runReliabilityLab(imageData, { version: encoded.version, crc32: encoded.crc32 }, { suite: \"full\" }); const sweep = runPerspectiveSweep(imageData, { version: encoded.version, crc32: encoded.crc32 }, { axis: \"yaw\", angles: [0, 15, 25, 35, 45, 55] }); Suites The Reliability Lab extends the smaller scanability suite with deterministic cases for lens blur, motion blur, low/high exposure, gradient shadow, glare, warm/cool color casts, contrast loss, sensor noise, JPEG-like damage, downscaling, projective skew, 3D pitch/yaw, Z rotation, and combined 3D tilt. The Extreme suite adds stronger perspective cases. These are deliberately demanding and may fail at low pixels-per-module even when the same angle succeeds at a larger rendered size. Perspective scanner recovery The scanner keeps its normal fast path for ordinary frames. When locator scale differences indicate projective foreshortening, the geometry stage can perform a bounded coarse-to-fine search for the primary alignment reference. The fine stage uses denser sub-cell scoring so payload cells are less likely to impersonate the alignment marker. This improves steep-angle recovery without weakening normal structure checks globally. Distributed secondary alignment references are still used to validate the resulting homography. Interpreting results Synthetic results are regression aids, not a substitute for real phone-camera and print testing. A useful practical metric is the largest reliable angle at the intended physical size and scanning distance, not only the theoretical bits per cell. Previous ← High Density Mode Next API Reference → QuadQR Experimental RGBW matrix symbology. AGPL-3.0 licensed"},{"title":"Browser demo","page":"Reliability Lab","url":"reliability.html#browser-demo","text":"Browser demo Open the Reliability Lab tab after generating a QuadQR. The lab provides: Quick, Full, and Extreme suites; per-scenario pass/fail, confidence, Reed-Solomon correction count, and decode time; category-level scoring for optics, lighting, color, resampling, sensor damage, and perspective; a 3D perspective playground with X pitch, Y yaw, and Z rotation controls; angle sweeps to find the largest tested pitch, yaw, or Z rotation that still decodes. X pitch and Y yaw simulate a code plane tilting in depth. Z rotation is an in-plane rotation. The controls can be combined."},{"title":"Public API","page":"Reliability Lab","url":"reliability.html#public-api","text":"Public API JS Copy import { applyStressDistortion, runReliabilityLab, runPerspectiveSweep } from \"quadqr-js\"; const transformed = applyStressDistortion(imageData, \"perspective-3d\", 0.5, { pitchDegrees: 20, yawDegrees: 45, rollDegrees: 15 }); const report = runReliabilityLab(imageData, { version: encoded.version, crc32: encoded.crc32 }, { suite: \"full\" }); const sweep = runPerspectiveSweep(imageData, { version: encoded.version, crc32: encoded.crc32 }, { axis: \"yaw\", angles: [0, 15, 25, 35, 45, 55] });"},{"title":"Suites","page":"Reliability Lab","url":"reliability.html#suites","text":"Suites The Reliability Lab extends the smaller scanability suite with deterministic cases for lens blur, motion blur, low/high exposure, gradient shadow, glare, warm/cool color casts, contrast loss, sensor noise, JPEG-like damage, downscaling, projective skew, 3D pitch/yaw, Z rotation, and combined 3D tilt. The Extreme suite adds stronger perspective cases. These are deliberately demanding and may fail at low pixels-per-module even when the same angle succeeds at a larger rendered size."},{"title":"Perspective scanner recovery","page":"Reliability Lab","url":"reliability.html#perspective-scanner-recovery","text":"Perspective scanner recovery The scanner keeps its normal fast path for ordinary frames. When locator scale differences indicate projective foreshortening, the geometry stage can perform a bounded coarse-to-fine search for the primary alignment reference. The fine stage uses denser sub-cell scoring so payload cells are less likely to impersonate the alignment marker. This improves steep-angle recovery without weakening normal structure checks globally. Distributed secondary alignment references are still used to validate the resulting homography."},{"title":"Interpreting results","page":"Reliability Lab","url":"reliability.html#interpreting-results","text":"Interpreting results Synthetic results are regression aids, not a substitute for real phone-camera and print testing. A useful practical metric is the largest reliable angle at the intended physical size and scanning distance, not only the theoretical bits per cell. Previous ← High Density Mode Next API Reference → QuadQR Experimental RGBW matrix symbology. AGPL-3.0 licensed"},{"title":"Scanning & Camera","page":"Scanning & Camera","url":"scanning.html","text":"QUADQR DOCUMENTATION Scanning & Camera The complete browser/image/camera scanner guide, including every supported scanner tuning option and every progressive recovery control. Need a specific option? Every scanner option is defined on this page. The Configuration Reference also collects scanner, encoder, renderer, security, benchmark, and Node.js options in one searchable index. Scanner pipeline finder detection → perspective geometry → RGBW / Triangle16 sampling → color calibration → Spectrum ECC 2.0 → CRC → payload The normal path is deliberately cheap. Recovery stages only activate after a normal decode misses. That distinction is important when tuning camera cadence options: a lower cadence number spends more CPU on difficult frames, not on frames that already decode cleanly. Scan an uploaded browser image JS Copy import { scanFile } from \"quadqr-js/browser\"; const result = await scanFile(file, { debug: true, includeRectified: true }); console.log(result.text); scanFile() decodes the browser File into RGBA pixels and then uses exactly the same scanImageData() pipeline described below. Image scanner options Geometry and fallback Option Type / accepted values Default What it does minVersion integer 1..40 1 Lowest symbol version geometry detection will consider. maxVersion integer 1..40 40 Highest symbol version geometry detection will consider. perspective boolean true Enables finder/alignment detection and homography-based projective correction. Set false only for known axis-aligned input. axisAlignedFallback boolean true After perspective attempts fail, tries a rectangular active-bounds sampler suitable for already straight/cropped images. maxGeometryCandidates positive integer 8 Maximum perspective geometry candidates examined per finder pass. finderRecovery boolean true Allows additional finder passes after the fast RGB-value/Otsu pass, including two-finder recovery, QuadQR Auto Color-assisted finder detection, threshold bracketing, and luminance fallback. preciseAlignment boolean false Requests finer alignment localization on the current geometry pass. Normally the scanner uses this only in the bounded recovery path. preciseAlignmentRecovery boolean true If normal perspective geometry exists but does not decode, retries once with precise alignment enabled. whiteThreshold number 0..255 238 Axis-aligned fallback threshold used to find the non-white active image bounds. maxModuleAspectError non-negative ratio 0.16 Axis-aligned fallback rejects candidate grids whose estimated horizontal/vertical module sizes differ by more than this ratio. bounds {x,y,width,height} auto-detected Supplies explicit active bounds for axis-aligned scanning instead of finding them from near-white pixels. palette RGBW palette object built-in palette Fixed color reference used by the axis-aligned fallback. Perspective scanning normally calibrates colors from the symbol itself. Sampling and calibration Option Type / accepted values Default What it does sampleMode \"cross\" | \"median\" \"cross\" Primary module sampler. Cross averages the center plus four nearby samples; median uses a compact 3×3 patch for dirt/blur rejection. sampleRadius number in module units Perspective: 0.16 Axis fallback: 0.18 Distance from module center for the primary sample pattern. Smaller values avoid edges; larger values average more of the module. adaptiveSampling boolean true Adds a robust median sampling pass after the fast sampling profile when needed. robustSampleRadius number in module units Perspective: 0.12 Axis fallback: 0.10 Radius used by the robust median sampling profile. spatialColorNormalization boolean true Uses distributed black/white calibration references to correct spatial lighting gradients before classification. structureTolerance number Perspective decode: 0.18 Axis fallback: 0.12 How much structural mismatch the scanner allows after sampled colors are classified. Increase cautiously because loose values can admit false geometry. includeRectified boolean false Adds a perspective-corrected QR-only ImageData image to a successful result. rectifiedModuleSize positive number 8 Pixels per module used for the optional rectified result image. QuadQR Finder Auto Color recovery These values affect the strong per-channel correction used only when the clean finder pass cannot establish valid geometry. Option Type / accepted values Default What it does finderAutoColorBlackClip number, effectively clamped 0..0.06 0.0001 Per-channel shadow percentile clipped during QuadQR Auto Color-assisted finder recovery. finderAutoColorWhiteClip number, clamped 0..0.06 0.004 Per-channel highlight percentile clipped when a direct output-highlight anchor is not used. finderAutoColorHighlightPercentile number 0.70..0.9999 0.95 Bright-reference percentile used to estimate a neutral highlight during QuadQR finder Auto Color recovery. finderAutoColorOutputHighlight number, clamped 96..250 190 Maps the observed highlight reference toward this output value, strengthening black and RGB separation instead of stretching paper white all the way to 255. finderAutoColorAnalysisInset fraction 0..0.30 recovery tries 0.10, 0.20, 0.04 Restricts histogram analysis toward the image center while applying the correction to the full frame. Set a single value to replace the built-in center-weighted sequence. finderAutoColorMinimumInputRange number, minimum 32 internally 72 Minimum per-channel input tonal span assumed while computing strong QuadQR Auto Color levels. finderAutoColorTargetSamples positive integer 90000 Approximate histogram sample budget used by QuadQR finder Auto Color analysis. Auto Tone / Contrast / Color recovery Option Type / accepted values Default What it does autoEnhanceRecovery boolean true Master switch for Auto Tone / Contrast / Color recovery after normal classification fails. fullFrameAutoEnhanceRecovery boolean Static image: true Camera fast path: false Allows a full-frame enhanced retry after geometry was found. Camera scanning normally avoids this expensive operation and prefers QR-region/cropped recovery. autoEnhanceWhenNoGeometry boolean Static image: true If even finder geometry is missing, allows one enhanced full-image retry. Camera scheduling enables it only on selected missed frames. autoEnhanceBlackClip number, clamped 0..0.08 0.006 Shadow clipping used by Auto Tone/Color recovery. autoEnhanceWhiteClip number, clamped 0..0.08 0.004 Highlight clipping used by Auto Tone/Color recovery. autoEnhanceSaturation number, clamped 1..1.5 1.12 Conservative saturation boost after white balance and tonal expansion. autoEnhanceTargetSamples positive integer, minimum 8000 160000 Approximate pixel sample budget for full-image enhancement statistics. rectifiedAutoEnhanceRecovery boolean true When geometry is known but decoding still fails, rectifies only the QR area, enhances it, and resamples it. This avoids unrelated scene pixels skewing histograms. rectifiedRecoveryModuleSize integer, clamped 4..10 6 Pixels per module used to build the temporary QR-only image for rectified recovery. rectifiedAutoEnhanceBlackClip number 0.008 Stronger shadow clipping for QR-only rectified enhancement. rectifiedAutoEnhanceWhiteClip number 0.006 Highlight clipping for QR-only rectified enhancement. rectifiedAutoEnhanceSaturation number 1.22 Saturation boost for QR-only rectified enhancement. rectifiedAutoEnhanceHighlightFraction number 0.16 Brightest fraction of samples used to estimate the neutral highlight during QR-only enhancement. rectifiedRecoveryRadiusRatio number in module units 0.16 Sampling radius used after rectified enhancement. Geometry micro-refinement Option Type / accepted values Default What it does geometryRefinement boolean true Enables a final bounded micro-refinement pass when structure looks strong but payload decoding still fails. refinementStructureThreshold number 0.90 Minimum structural accuracy required before spending CPU on micro-refinement. refinementOffset number, clamped 0.05..0.35 modules 0.20 Sampling-center offset used to probe a small neighborhood around the detected homography. refinementDecodeThreshold number 0.95 Only refined candidates whose known structural cells score at least this well are sent into the more expensive Reed-Solomon decode. refinementDecodeCandidates integer, clamped 1..8 4 Maximum number of best micro-refined candidates that receive a full decode attempt. High Density / Triangle16 sampling Option Type / accepted values Default What it does highDensitySampling boolean true Allows the scanner to sample both triangle regions of each body module so Triangle16 can be auto-detected. triangle16 boolean true Compatibility gate for Triangle16 sampling. Set false to skip Triangle16 attempts entirely. highDensitySampleInset / triangleSampleInset number, clamped 0.18..0.34 0.27 How far inside each triangle the protected sample anchors sit. Keeps samples away from the diagonal and module borders. highDensitySampleRadius / triangleSampleRadius number in module units Cross: 0.065 Median: 0.055 Local radius around each Triangle16 sample anchor. Spectrum ECC 2.0 controls Option Type / accepted values Default What it does maxErasureConfidence number 0.68 Cells/symbols at or below this confidence can be promoted to Reed-Solomon erasures after hard decoding fails. Lower values are more conservative. softDecoding boolean true when confidence + alternatives exist Allows bounded alternate-cell substitution attempts after hard/error-erasure decoding fails. Set false to disable the soft stage. softDecodeConfidence number, clamped 0.05..0.95 0.72 Only cells at or below this confidence are considered for alternate-cell soft decoding. softDecodeMaxCells integer, clamped 2..16 10 Maximum number of low-confidence cells considered as soft-decoding candidates. softDecodePairCells integer, 2..softDecodeMaxCells 6 Limits the candidate subset used for two-cell substitution combinations, keeping the search bounded. Diagnostics Option Type / accepted values Default What it does debug boolean false Returns full scanner diagnostics on success and attaches diagnostics to thrown scan errors. Use debugScanImageData() when you want a non-throwing wrapper. debugMatrices boolean true while debug is enabled Controls whether sampled matrix and confidence matrix snapshots are included in full debug output. Set false to reduce debug payload size. Live camera scanning JS Copy import { startCameraScanner } from \"quadqr-js/browser\"; const scanner = await startCameraScanner(video, { scanInterval: 33, multiFrame: true, cameraFinderRecoveryEvery: 2, cameraAutoColorEvery: 1, cameraHighResolutionRecovery: true, onResult(result, frameMeta) { console.log(result.text, frameMeta); } }); // Later: scanner.stop(); The returned controller contains stream , video , stop() , and scanNow() . By default the scanner stops on the first successful decode. Core live-camera controls The live scanner is frame-driven on browsers that support requestVideoFrameCallback() and does not queue stale fast-path frames. Supported browsers use a dual-worker engine: a fast worker continuously handles fresh normal finder/geometry/decode attempts while a second worker runs the complete perspective, precise-alignment, QuadQR Auto Color, QR-region enhancement, Spectrum ECC, multi-frame, and damaged-code recovery stack independently. A difficult recovery frame therefore cannot hold the camera scheduler hostage. Finder detection itself remains JavaScript. WASM accelerates grayscale/Otsu/binary preprocessing and CRC beneath the same detector. The visible camera crop is resized to the normal scan dimension before worker transfer, reducing mobile bitmap and memory pressure. Unsupported or CSP-restricted environments automatically use the same full scanner on the main thread. Option Type / accepted values Default What it does constraints MediaStreamConstraints Environment camera, ideal 1280×720 @ 30 fps Passed directly to navigator.mediaDevices.getUserMedia() . Supply your own object to select a camera or alter resolution/frame-rate constraints. scanInterval number, milliseconds; minimum 24 Worker: 33 Main-thread fallback: 80 Minimum start-to-start cadence for live scan attempts. Worker mode can examine a 30 fps stream frame-for-frame when decoding is fast enough. If one scan takes longer, QuadQR drops stale frames rather than queueing work. Main-thread fallback uses a gentler default to preserve page responsiveness. cameraWorker boolean true Uses a dual-worker camera engine when Worker , createImageBitmap , and worker-side OffscreenCanvas are available. The fast worker stays responsive to fresh frames while the second worker runs the full recovery stack. Set false to force the main-thread engine. cameraWorkerRequired boolean false If true , worker initialization failure is thrown instead of automatically falling back to the full main-thread scanner. Useful when an application requires off-thread scanning. cameraWorkerUrl string or URL-compatible value bundled camera-scanner-worker.js Overrides the module-worker URL. Normally unnecessary; use it when a strict CSP, custom asset host, or bundler requires the worker file at a specific URL. useVideoFrameCallback boolean true when supported Uses requestVideoFrameCallback() to schedule work from real camera frames instead of blindly polling. QuadQR keeps only one scheduled attempt, so stale frames are not queued while a scan is still running. cameraGeometryReuse boolean true Reuses the most recent valid finder/perspective homography on following frames before running a new full finder search. If the code or camera moves, the same frame automatically falls back to normal detection. cameraGeometryReuseMaxMisses integer, minimum 1 5 Number of consecutive frames without a refreshed geometry solution before the cached camera homography is discarded. maxDimension positive pixels Camera scanner: minimum 480, default 640 Direct scanVideoFrame() : 960 Maximum width/height used for the normal fast camera scan. In worker mode the visible preview crop is resized before transfer. stopOnResult boolean true Stops the camera tracks after the first successful decode. Set false for continuous scanning. multiFrame boolean true Enables confidence-weighted fusion of compatible observations from several missed frames before another Reed-Solomon attempt. multiFrameWindow integer, clamped 2..8 4 Maximum compatible frame observations retained for temporal fusion. multiFrameMinFrames integer, 2..multiFrameWindow 2 Minimum observations required before the scanner tries a fused multi-frame decode. multiFrameMinAgreement ratio 0.62 Minimum high-confidence data-cell agreement required to keep consecutive RGBW observations in the same fusion history. Lower agreement resets the history to avoid mixing two different codes. multiFrameMinAgreementHighDensity ratio 0.58 Triangle16 version of the observation-agreement threshold. It is slightly lower because half-cell sampling is naturally noisier. onResult function (result, frameMeta) => void unset Called on successful decode before optional camera stop. onDecode function (result, frameMeta) => void unset Alias callback invoked alongside onResult for successful decodes. onScanMiss function (error) => void unset Called after the progressive recovery sequence and multi-frame fusion fail for a scan cycle. onDiagnostic function (event) => void unset Receives live camera diagnostic events such as camera-ready , frame , method , and success . Exceptions thrown by this callback are ignored so UI diagnostics cannot break scanning. Visible camera crop On phones, the camera sensor is often 16:9 while the preview element is portrait with object-fit: cover . QuadQR scans the source pixels corresponding to what the user can actually see instead of silently scanning the hidden full sensor frame. Option Type / accepted values Default What it does videoCropMode \"visible\" behavior by default; \"full\" disables cropping visible/automatic For object-fit: cover previews, maps the visible element box back to the matching camera-source rectangle so hidden sensor pixels do not shrink the QR during scanning. videoObjectFit CSS object-fit value computed style, otherwise \"cover\" Overrides the object-fit value used when mapping the visible video crop. Cropping is applied only for cover . videoObjectPosition CSS-like position string computed style, otherwise \"50% 50%\" Controls where the visible crop lies inside the camera source. Supports left/right/top/bottom/center and percentages. videoCropInset fraction, clamped 0..0.18 0 Optionally trims a small extra border from the already-visible crop, useful when preview edges are soft or obstructed. canvas HTMLCanvasElement new scratch canvas Direct scanVideoFrame() option for reusing a caller-owned canvas and reducing allocations. Progressive camera recovery After a miss, the scanner increases effort in bounded stages. The *Every options are cadence controls measured in missed scan cycles. They are performance knobs, not millisecond timers. cameraFinderRecoveryEvery example Default 2 means finder recovery is eligible on the first retry after a miss, then every other missed cycle. 1 runs it on every missed cycle. 4 uses it much less often, reducing CPU but potentially delaying recovery when lighting or perspective is difficult. Option Type / accepted values Default What it does cameraFinderRecoveryEvery integer, minimum 1 2 Controls how often the more expensive finder-recovery path is allowed after misses. 1 means every missed scan cycle; 2 means every other missed cycle. Recovery is first eligible on the first retry after a miss. cameraAutoEnhanceEvery integer, minimum 1 2 Cadence for progressive Auto Tone / Contrast / Color recovery after misses. Higher values reduce CPU at the cost of slower recovery on difficult frames. cameraAutoColorEvery integer, minimum 1 1 Cadence for camera-specific QuadQR-specific QuadQR Auto Color recovery. The default allows it on every missed cycle because this is a targeted recovery path. cameraAutoColorRecovery boolean true Master switch for camera-specific cropped QuadQR Auto Color recovery after a fast scan miss. cameraAutoColorCropInsets array of fractions, each clamped 0..0.30 [0.08, 0.16, 0.22, 0] Ordered centered crop profiles used by camera QuadQR Auto Color. The scanner tries tighter QR-centric histograms first and full frame last. cameraAutoColorAnalysisInsets array of fractions unset Per-profile histogram analysis insets matching cameraAutoColorCropInsets . Useful for explicitly tuning each crop profile. cameraAutoColorAnalysisInset fraction 0..0.30 profile defaults 0.10, 0.08, 0.04, 0.10 Single fallback analysis inset used when a per-profile value is not supplied. cameraAutoColorBlackClip number 0.0001 Shadow clipping for the camera-specific strong QuadQR Auto Color pass. cameraAutoColorWhiteClip number 0.004 Highlight clipping fallback for camera QuadQR Auto Color. cameraAutoColorHighlightPercentile number 0.70..0.9999 0.95 Bright-reference percentile used by camera QuadQR Auto Color. cameraAutoColorOutputHighlight number, clamped 96..250 190 Target highlight output used by the strong camera QuadQR Auto Color levels mapping. cameraAutoColorMinimumInputRange number 72 Minimum assumed input tonal range for camera QuadQR Auto Color. cameraAutoColorTargetSamples positive integer 90000 Histogram sample budget for camera QuadQR Auto Color. cameraHighResolutionRecovery boolean true Allows a bounded high-resolution retry when the fast pass already sees enough finder patterns but dense modules may have been lost by normal downscaling. cameraHighResolutionMinFinders integer 2 Minimum finder count needed before the high-resolution retry is considered. This prevents wasting the expensive pass on empty frames. cameraHighResolutionMaxDimension positive pixels, never below normal maxDimension 960 Maximum dimension used by the full recovery worker for high-detail geometry/color recovery. cameraHighResolutionEvery integer, minimum 1 2 Cadence for eligible high-resolution retries after misses. cameraRecoveryStrongFinderInterval milliseconds, minimum 80 120 Minimum interval for parallel full recovery when at least two finders are already visible. cameraRecoveryWeakFinderInterval milliseconds 260 Minimum interval after a persistent one-finder candidate is allowed to trigger recovery. cameraRecoveryWeakFinderFrames integer, minimum 2 2 Consecutive fresh frames that must report exactly one finder before the full recovery worker is armed. Zero-finder frames never trigger recovery. How the recovery sequence behaves Fresh fast scan: a 640 px visible ROI is immediately checked for normal finder geometry, calibrated sampling, ECC, and CRC. Parallel recovery dispatch: finder evidence controls whether the independent full-recovery worker receives a fresh frame. Two or more finders trigger immediately; one finder must persist; zero-finder scenes stay fast-only. High-resolution geometry retry: the recovery worker can use up to 960 px when dense modules need more pixels. QuadQR Camera Auto Color crops: progressively QR-centric histogram windows remain available for strong yellow/warm casts. QR-region enhancement: if geometry is strong, enhance only the rectified code area. Multi-frame fusion: combine compatible high-confidence observations and retry Spectrum ECC 2.0. Multi-frame confidence fusion Fusion is not simple majority voting. Each cell vote is weighted by source confidence, frame quality, and recency. Second-best cell hypotheses contribute weak evidence. Before frames are fused, high-confidence data-cell agreement is checked so two different QuadQR symbols of the same version do not contaminate each other. Camera callbacks and frame metadata onResult and onDecode receive the same successful decode plus frame metadata. onDiagnostic receives live attempt events. Full field definitions are in Configuration Reference → Camera callback payloads . Understanding scanner confidence Successful image scans include an overall confidence plus geometry, calibration, structure, and ECC-related diagnostics. Confidence is a ranking/debug aid, not a cryptographic guarantee. Payload acceptance still requires Reed-Solomon validation and the final CRC. Secure and signed scans A secure result can be geometrically and cryptographically intact while still returning text: null . Use decryptDecoded() with the password or raw key. Signed payload verification is separate: call verifyDecodedSignature() with a trusted Ed25519 public key or trusted-key registry. Reliability testing Use runReliabilityLab() , runPerspectiveSweep() , and assessScanability() to test scanner behavior under blur, lighting shifts, downscaling, and 3D perspective before shipping a physical workflow. Previous ← Node.js Next Compression → QuadQR Experimental RGBW matrix symbology. AGPL-3.0 licensed"},{"title":"Scanner pipeline","page":"Scanning & Camera","url":"scanning.html#scanner-pipeline","text":"Scanner pipeline finder detection → perspective geometry → RGBW / Triangle16 sampling → color calibration → Spectrum ECC 2.0 → CRC → payload The normal path is deliberately cheap. Recovery stages only activate after a normal decode misses. That distinction is important when tuning camera cadence options: a lower cadence number spends more CPU on difficult frames, not on frames that already decode cleanly."},{"title":"Scan an uploaded browser image","page":"Scanning & Camera","url":"scanning.html#scan-upload","text":"Scan an uploaded browser image JS Copy import { scanFile } from \"quadqr-js/browser\"; const result = await scanFile(file, { debug: true, includeRectified: true }); console.log(result.text); scanFile() decodes the browser File into RGBA pixels and then uses exactly the same scanImageData() pipeline described below."},{"title":"Image scanner options","page":"Scanning & Camera","url":"scanning.html#image-scanner-options","text":"Image scanner options Geometry and fallback # Option Type / accepted values Default What it does minVersion integer 1..40 1 Lowest symbol version geometry detection will consider. maxVersion integer 1..40 40 Highest symbol version geometry detection will consider. perspective boolean true Enables finder/alignment detection and homography-based projective correction. Set false only for known axis-aligned input. axisAlignedFallback boolean true After perspective attempts fail, tries a rectangular active-bounds sampler suitable for already straight/cropped images. maxGeometryCandidates positive integer 8 Maximum perspective geometry candidates examined per finder pass. finderRecovery boolean true Allows additional finder passes after the fast RGB-value/Otsu pass, including two-finder recovery, QuadQR Auto Color-assisted finder detection, threshold bracketing, and luminance fallback. preciseAlignment boolean false Requests finer alignment localization on the current geometry pass. Normally the scanner uses this only in the bounded recovery path. preciseAlignmentRecovery boolean true If normal perspective geometry exists but does not decode, retries once with precise alignment enabled. whiteThreshold number 0..255 238 Axis-aligned fallback threshold used to find the non-white active image bounds. maxModuleAspectError non-negative ratio 0.16 Axis-aligned fallback rejects candidate grids whose estimated horizontal/vertical module sizes differ by more than this ratio. bounds {x,y,width,height} auto-detected Supplies explicit active bounds for axis-aligned scanning instead of finding them from near-white pixels. palette RGBW palette object built-in palette Fixed color reference used by the axis-aligned fallback. Perspective scanning normally calibrates colors from the symbol itself. Sampling and calibration # Option Type / accepted values Default What it does sampleMode \"cross\" | \"median\" \"cross\" Primary module sampler. Cross averages the center plus four nearby samples; median uses a compact 3×3 patch for dirt/blur rejection. sampleRadius number in module units Perspective: 0.16 Axis fallback: 0.18 Distance from module center for the primary sample pattern. Smaller values avoid edges; larger values average more of the module. adaptiveSampling boolean true Adds a robust median sampling pass after the fast sampling profile when needed. robustSampleRadius number in module units Perspective: 0.12 Axis fallback: 0.10 Radius used by the robust median sampling profile. spatialColorNormalization boolean true Uses distributed black/white calibration references to correct spatial lighting gradients before classification. structureTolerance number Perspective decode: 0.18 Axis fallback: 0.12 How much structural mismatch the scanner allows after sampled colors are classified. Increase cautiously because loose values can admit false geometry. includeRectified boolean false Adds a perspective-corrected QR-only ImageData image to a successful result. rectifiedModuleSize positive number 8 Pixels per module used for the optional rectified result image. QuadQR Finder Auto Color recovery # These values affect the strong per-channel correction used only when the clean finder pass cannot establish valid geometry. Option Type / accepted values Default What it does finderAutoColorBlackClip number, effectively clamped 0..0.06 0.0001 Per-channel shadow percentile clipped during QuadQR Auto Color-assisted finder recovery. finderAutoColorWhiteClip number, clamped 0..0.06 0.004 Per-channel highlight percentile clipped when a direct output-highlight anchor is not used. finderAutoColorHighlightPercentile number 0.70..0.9999 0.95 Bright-reference percentile used to estimate a neutral highlight during QuadQR finder Auto Color recovery. finderAutoColorOutputHighlight number, clamped 96..250 190 Maps the observed highlight reference toward this output value, strengthening black and RGB separation instead of stretching paper white all the way to 255. finderAutoColorAnalysisInset fraction 0..0.30 recovery tries 0.10, 0.20, 0.04 Restricts histogram analysis toward the image center while applying the correction to the full frame. Set a single value to replace the built-in center-weighted sequence. finderAutoColorMinimumInputRange number, minimum 32 internally 72 Minimum per-channel input tonal span assumed while computing strong QuadQR Auto Color levels. finderAutoColorTargetSamples positive integer 90000 Approximate histogram sample budget used by QuadQR finder Auto Color analysis. Auto Tone / Contrast / Color recovery # Option Type / accepted values Default What it does autoEnhanceRecovery boolean true Master switch for Auto Tone / Contrast / Color recovery after normal classification fails. fullFrameAutoEnhanceRecovery boolean Static image: true Camera fast path: false Allows a full-frame enhanced retry after geometry was found. Camera scanning normally avoids this expensive operation and prefers QR-region/cropped recovery. autoEnhanceWhenNoGeometry boolean Static image: true If even finder geometry is missing, allows one enhanced full-image retry. Camera scheduling enables it only on selected missed frames. autoEnhanceBlackClip number, clamped 0..0.08 0.006 Shadow clipping used by Auto Tone/Color recovery. autoEnhanceWhiteClip number, clamped 0..0.08 0.004 Highlight clipping used by Auto Tone/Color recovery. autoEnhanceSaturation number, clamped 1..1.5 1.12 Conservative saturation boost after white balance and tonal expansion. autoEnhanceTargetSamples positive integer, minimum 8000 160000 Approximate pixel sample budget for full-image enhancement statistics. rectifiedAutoEnhanceRecovery boolean true When geometry is known but decoding still fails, rectifies only the QR area, enhances it, and resamples it. This avoids unrelated scene pixels skewing histograms. rectifiedRecoveryModuleSize integer, clamped 4..10 6 Pixels per module used to build the temporary QR-only image for rectified recovery. rectifiedAutoEnhanceBlackClip number 0.008 Stronger shadow clipping for QR-only rectified enhancement. rectifiedAutoEnhanceWhiteClip number 0.006 Highlight clipping for QR-only rectified enhancement. rectifiedAutoEnhanceSaturation number 1.22 Saturation boost for QR-only rectified enhancement. rectifiedAutoEnhanceHighlightFraction number 0.16 Brightest fraction of samples used to estimate the neutral highlight during QR-only enhancement. rectifiedRecoveryRadiusRatio number in module units 0.16 Sampling radius used after rectified enhancement. Geometry micro-refinement # Option Type / accepted values Default What it does geometryRefinement boolean true Enables a final bounded micro-refinement pass when structure looks strong but payload decoding still fails. refinementStructureThreshold number 0.90 Minimum structural accuracy required before spending CPU on micro-refinement. refinementOffset number, clamped 0.05..0.35 modules 0.20 Sampling-center offset used to probe a small neighborhood around the detected homography. refinementDecodeThreshold number 0.95 Only refined candidates whose known structural cells score at least this well are sent into the more expensive Reed-Solomon decode. refinementDecodeCandidates integer, clamped 1..8 4 Maximum number of best micro-refined candidates that receive a full decode attempt. High Density / Triangle16 sampling # Option Type / accepted values Default What it does highDensitySampling boolean true Allows the scanner to sample both triangle regions of each body module so Triangle16 can be auto-detected. triangle16 boolean true Compatibility gate for Triangle16 sampling. Set false to skip Triangle16 attempts entirely. highDensitySampleInset / triangleSampleInset number, clamped 0.18..0.34 0.27 How far inside each triangle the protected sample anchors sit. Keeps samples away from the diagonal and module borders. highDensitySampleRadius / triangleSampleRadius number in module units Cross: 0.065 Median: 0.055 Local radius around each Triangle16 sample anchor. Spectrum ECC 2.0 controls # Option Type / accepted values Default What it does maxErasureConfidence number 0.68 Cells/symbols at or below this confidence can be promoted to Reed-Solomon erasures after hard decoding fails. Lower values are more conservative. softDecoding boolean true when confidence + alternatives exist Allows bounded alternate-cell substitution attempts after hard/error-erasure decoding fails. Set false to disable the soft stage. softDecodeConfidence number, clamped 0.05..0.95 0.72 Only cells at or below this confidence are considered for alternate-cell soft decoding. softDecodeMaxCells integer, clamped 2..16 10 Maximum number of low-confidence cells considered as soft-decoding candidates. softDecodePairCells integer, 2..softDecodeMaxCells 6 Limits the candidate subset used for two-cell substitution combinations, keeping the search bounded. Diagnostics # Option Type / accepted values Default What it does debug boolean false Returns full scanner diagnostics on success and attaches diagnostics to thrown scan errors. Use debugScanImageData() when you want a non-throwing wrapper. debugMatrices boolean true while debug is enabled Controls whether sampled matrix and confidence matrix snapshots are included in full debug output. Set false to reduce debug payload size."},{"title":"Geometry and fallback","page":"Scanning & Camera","url":"scanning.html#scanner-geometry","text":"Geometry and fallback Option Type / accepted values Default What it does minVersion integer 1..40 1 Lowest symbol version geometry detection will consider. maxVersion integer 1..40 40 Highest symbol version geometry detection will consider. perspective boolean true Enables finder/alignment detection and homography-based projective correction. Set false only for known axis-aligned input. axisAlignedFallback boolean true After perspective attempts fail, tries a rectangular active-bounds sampler suitable for already straight/cropped images. maxGeometryCandidates positive integer 8 Maximum perspective geometry candidates examined per finder pass. finderRecovery boolean true Allows additional finder passes after the fast RGB-value/Otsu pass, including two-finder recovery, QuadQR Auto Color-assisted finder detection, threshold bracketing, and luminance fallback. preciseAlignment boolean false Requests finer alignment localization on the current geometry pass. Normally the scanner uses this only in the bounded recovery path. preciseAlignmentRecovery boolean true If normal perspective geometry exists but does not decode, retries once with precise alignment enabled. whiteThreshold number 0..255 238 Axis-aligned fallback threshold used to find the non-white active image bounds. maxModuleAspectError non-negative ratio 0.16 Axis-aligned fallback rejects candidate grids whose estimated horizontal/vertical module sizes differ by more than this ratio. bounds {x,y,width,height} auto-detected Supplies explicit active bounds for axis-aligned scanning instead of finding them from near-white pixels. palette RGBW palette object built-in palette Fixed color reference used by the axis-aligned fallback. Perspective scanning normally calibrates colors from the symbol itself."},{"title":"Sampling and calibration","page":"Scanning & Camera","url":"scanning.html#scanner-sampling","text":"Sampling and calibration Option Type / accepted values Default What it does sampleMode \"cross\" | \"median\" \"cross\" Primary module sampler. Cross averages the center plus four nearby samples; median uses a compact 3×3 patch for dirt/blur rejection. sampleRadius number in module units Perspective: 0.16 Axis fallback: 0.18 Distance from module center for the primary sample pattern. Smaller values avoid edges; larger values average more of the module. adaptiveSampling boolean true Adds a robust median sampling pass after the fast sampling profile when needed. robustSampleRadius number in module units Perspective: 0.12 Axis fallback: 0.10 Radius used by the robust median sampling profile. spatialColorNormalization boolean true Uses distributed black/white calibration references to correct spatial lighting gradients before classification. structureTolerance number Perspective decode: 0.18 Axis fallback: 0.12 How much structural mismatch the scanner allows after sampled colors are classified. Increase cautiously because loose values can admit false geometry. includeRectified boolean false Adds a perspective-corrected QR-only ImageData image to a successful result. rectifiedModuleSize positive number 8 Pixels per module used for the optional rectified result image."},{"title":"QuadQR Finder Auto Color recovery","page":"Scanning & Camera","url":"scanning.html#finder-auto-color","text":"QuadQR Finder Auto Color recovery These values affect the strong per-channel correction used only when the clean finder pass cannot establish valid geometry. Option Type / accepted values Default What it does finderAutoColorBlackClip number, effectively clamped 0..0.06 0.0001 Per-channel shadow percentile clipped during QuadQR Auto Color-assisted finder recovery. finderAutoColorWhiteClip number, clamped 0..0.06 0.004 Per-channel highlight percentile clipped when a direct output-highlight anchor is not used. finderAutoColorHighlightPercentile number 0.70..0.9999 0.95 Bright-reference percentile used to estimate a neutral highlight during QuadQR finder Auto Color recovery. finderAutoColorOutputHighlight number, clamped 96..250 190 Maps the observed highlight reference toward this output value, strengthening black and RGB separation instead of stretching paper white all the way to 255. finderAutoColorAnalysisInset fraction 0..0.30 recovery tries 0.10, 0.20, 0.04 Restricts histogram analysis toward the image center while applying the correction to the full frame. Set a single value to replace the built-in center-weighted sequence. finderAutoColorMinimumInputRange number, minimum 32 internally 72 Minimum per-channel input tonal span assumed while computing strong QuadQR Auto Color levels. finderAutoColorTargetSamples positive integer 90000 Approximate histogram sample budget used by QuadQR finder Auto Color analysis."},{"title":"Auto Tone / Contrast / Color recovery","page":"Scanning & Camera","url":"scanning.html#auto-enhance","text":"Auto Tone / Contrast / Color recovery Option Type / accepted values Default What it does autoEnhanceRecovery boolean true Master switch for Auto Tone / Contrast / Color recovery after normal classification fails. fullFrameAutoEnhanceRecovery boolean Static image: true Camera fast path: false Allows a full-frame enhanced retry after geometry was found. Camera scanning normally avoids this expensive operation and prefers QR-region/cropped recovery. autoEnhanceWhenNoGeometry boolean Static image: true If even finder geometry is missing, allows one enhanced full-image retry. Camera scheduling enables it only on selected missed frames. autoEnhanceBlackClip number, clamped 0..0.08 0.006 Shadow clipping used by Auto Tone/Color recovery. autoEnhanceWhiteClip number, clamped 0..0.08 0.004 Highlight clipping used by Auto Tone/Color recovery. autoEnhanceSaturation number, clamped 1..1.5 1.12 Conservative saturation boost after white balance and tonal expansion. autoEnhanceTargetSamples positive integer, minimum 8000 160000 Approximate pixel sample budget for full-image enhancement statistics. rectifiedAutoEnhanceRecovery boolean true When geometry is known but decoding still fails, rectifies only the QR area, enhances it, and resamples it. This avoids unrelated scene pixels skewing histograms. rectifiedRecoveryModuleSize integer, clamped 4..10 6 Pixels per module used to build the temporary QR-only image for rectified recovery. rectifiedAutoEnhanceBlackClip number 0.008 Stronger shadow clipping for QR-only rectified enhancement. rectifiedAutoEnhanceWhiteClip number 0.006 Highlight clipping for QR-only rectified enhancement. rectifiedAutoEnhanceSaturation number 1.22 Saturation boost for QR-only rectified enhancement. rectifiedAutoEnhanceHighlightFraction number 0.16 Brightest fraction of samples used to estimate the neutral highlight during QR-only enhancement. rectifiedRecoveryRadiusRatio number in module units 0.16 Sampling radius used after rectified enhancement."},{"title":"Geometry micro-refinement","page":"Scanning & Camera","url":"scanning.html#geometry-refinement","text":"Geometry micro-refinement Option Type / accepted values Default What it does geometryRefinement boolean true Enables a final bounded micro-refinement pass when structure looks strong but payload decoding still fails. refinementStructureThreshold number 0.90 Minimum structural accuracy required before spending CPU on micro-refinement. refinementOffset number, clamped 0.05..0.35 modules 0.20 Sampling-center offset used to probe a small neighborhood around the detected homography. refinementDecodeThreshold number 0.95 Only refined candidates whose known structural cells score at least this well are sent into the more expensive Reed-Solomon decode. refinementDecodeCandidates integer, clamped 1..8 4 Maximum number of best micro-refined candidates that receive a full decode attempt."},{"title":"High Density / Triangle16 sampling","page":"Scanning & Camera","url":"scanning.html#high-density-sampling","text":"High Density / Triangle16 sampling Option Type / accepted values Default What it does highDensitySampling boolean true Allows the scanner to sample both triangle regions of each body module so Triangle16 can be auto-detected. triangle16 boolean true Compatibility gate for Triangle16 sampling. Set false to skip Triangle16 attempts entirely. highDensitySampleInset / triangleSampleInset number, clamped 0.18..0.34 0.27 How far inside each triangle the protected sample anchors sit. Keeps samples away from the diagonal and module borders. highDensitySampleRadius / triangleSampleRadius number in module units Cross: 0.065 Median: 0.055 Local radius around each Triangle16 sample anchor."},{"title":"Spectrum ECC 2.0 controls","page":"Scanning & Camera","url":"scanning.html#spectrum-ecc-controls","text":"Spectrum ECC 2.0 controls Option Type / accepted values Default What it does maxErasureConfidence number 0.68 Cells/symbols at or below this confidence can be promoted to Reed-Solomon erasures after hard decoding fails. Lower values are more conservative. softDecoding boolean true when confidence + alternatives exist Allows bounded alternate-cell substitution attempts after hard/error-erasure decoding fails. Set false to disable the soft stage. softDecodeConfidence number, clamped 0.05..0.95 0.72 Only cells at or below this confidence are considered for alternate-cell soft decoding. softDecodeMaxCells integer, clamped 2..16 10 Maximum number of low-confidence cells considered as soft-decoding candidates. softDecodePairCells integer, 2..softDecodeMaxCells 6 Limits the candidate subset used for two-cell substitution combinations, keeping the search bounded."},{"title":"Diagnostics","page":"Scanning & Camera","url":"scanning.html#diagnostics","text":"Diagnostics Option Type / accepted values Default What it does debug boolean false Returns full scanner diagnostics on success and attaches diagnostics to thrown scan errors. Use debugScanImageData() when you want a non-throwing wrapper. debugMatrices boolean true while debug is enabled Controls whether sampled matrix and confidence matrix snapshots are included in full debug output. Set false to reduce debug payload size."},{"title":"Live camera scanning","page":"Scanning & Camera","url":"scanning.html#live-camera-scanning","text":"Live camera scanning JS Copy import { startCameraScanner } from \"quadqr-js/browser\"; const scanner = await startCameraScanner(video, { scanInterval: 33, multiFrame: true, cameraFinderRecoveryEvery: 2, cameraAutoColorEvery: 1, cameraHighResolutionRecovery: true, onResult(result, frameMeta) { console.log(result.text, frameMeta); } }); // Later: scanner.stop(); The returned controller contains stream , video , stop() , and scanNow() . By default the scanner stops on the first successful decode. Core live-camera controls # The live scanner is frame-driven on browsers that support requestVideoFrameCallback() and does not queue stale fast-path frames. Supported browsers use a dual-worker engine: a fast worker continuously handles fresh normal finder/geometry/decode attempts while a second worker runs the complete perspective, precise-alignment, QuadQR Auto Color, QR-region enhancement, Spectrum ECC, multi-frame, and damaged-code recovery stack independently. A difficult recovery frame therefore cannot hold the camera scheduler hostage. Finder detection itself remains JavaScript. WASM accelerates grayscale/Otsu/binary preprocessing and CRC beneath the same detector. The visible camera crop is resized to the normal scan dimension before worker transfer, reducing mobile bitmap and memory pressure. Unsupported or CSP-restricted environments automatically use the same full scanner on the main thread. Option Type / accepted values Default What it does constraints MediaStreamConstraints Environment camera, ideal 1280×720 @ 30 fps Passed directly to navigator.mediaDevices.getUserMedia() . Supply your own object to select a camera or alter resolution/frame-rate constraints. scanInterval number, milliseconds; minimum 24 Worker: 33 Main-thread fallback: 80 Minimum start-to-start cadence for live scan attempts. Worker mode can examine a 30 fps stream frame-for-frame when decoding is fast enough. If one scan takes longer, QuadQR drops stale frames rather than queueing work. Main-thread fallback uses a gentler default to preserve page responsiveness. cameraWorker boolean true Uses a dual-worker camera engine when Worker , createImageBitmap , and worker-side OffscreenCanvas are available. The fast worker stays responsive to fresh frames while the second worker runs the full recovery stack. Set false to force the main-thread engine. cameraWorkerRequired boolean false If true , worker initialization failure is thrown instead of automatically falling back to the full main-thread scanner. Useful when an application requires off-thread scanning. cameraWorkerUrl string or URL-compatible value bundled camera-scanner-worker.js Overrides the module-worker URL. Normally unnecessary; use it when a strict CSP, custom asset host, or bundler requires the worker file at a specific URL. useVideoFrameCallback boolean true when supported Uses requestVideoFrameCallback() to schedule work from real camera frames instead of blindly polling. QuadQR keeps only one scheduled attempt, so stale frames are not queued while a scan is still running. cameraGeometryReuse boolean true Reuses the most recent valid finder/perspective homography on following frames before running a new full finder search. If the code or camera moves, the same frame automatically falls back to normal detection. cameraGeometryReuseMaxMisses integer, minimum 1 5 Number of consecutive frames without a refreshed geometry solution before the cached camera homography is discarded. maxDimension positive pixels Camera scanner: minimum 480, default 640 Direct scanVideoFrame() : 960 Maximum width/height used for the normal fast camera scan. In worker mode the visible preview crop is resized before transfer. stopOnResult boolean true Stops the camera tracks after the first successful decode. Set false for continuous scanning. multiFrame boolean true Enables confidence-weighted fusion of compatible observations from several missed frames before another Reed-Solomon attempt. multiFrameWindow integer, clamped 2..8 4 Maximum compatible frame observations retained for temporal fusion. multiFrameMinFrames integer, 2..multiFrameWindow 2 Minimum observations required before the scanner tries a fused multi-frame decode. multiFrameMinAgreement ratio 0.62 Minimum high-confidence data-cell agreement required to keep consecutive RGBW observations in the same fusion history. Lower agreement resets the history to avoid mixing two different codes. multiFrameMinAgreementHighDensity ratio 0.58 Triangle16 version of the observation-agreement threshold. It is slightly lower because half-cell sampling is naturally noisier. onResult function (result, frameMeta) => void unset Called on successful decode before optional camera stop. onDecode function (result, frameMeta) => void unset Alias callback invoked alongside onResult for successful decodes. onScanMiss function (error) => void unset Called after the progressive recovery sequence and multi-frame fusion fail for a scan cycle. onDiagnostic function (event) => void unset Receives live camera diagnostic events such as camera-ready , frame , method , and success . Exceptions thrown by this callback are ignored so UI diagnostics cannot break scanning. Visible camera crop # On phones, the camera sensor is often 16:9 while the preview element is portrait with object-fit: cover . QuadQR scans the source pixels corresponding to what the user can actually see instead of silently scanning the hidden full sensor frame. Option Type / accepted values Default What it does videoCropMode \"visible\" behavior by default; \"full\" disables cropping visible/automatic For object-fit: cover previews, maps the visible element box back to the matching camera-source rectangle so hidden sensor pixels do not shrink the QR during scanning. videoObjectFit CSS object-fit value computed style, otherwise \"cover\" Overrides the object-fit value used when mapping the visible video crop. Cropping is applied only for cover . videoObjectPosition CSS-like position string computed style, otherwise \"50% 50%\" Controls where the visible crop lies inside the camera source. Supports left/right/top/bottom/center and percentages. videoCropInset fraction, clamped 0..0.18 0 Optionally trims a small extra border from the already-visible crop, useful when preview edges are soft or obstructed. canvas HTMLCanvasElement new scratch canvas Direct scanVideoFrame() option for reusing a caller-owned canvas and reducing allocations."},{"title":"Core live-camera controls","page":"Scanning & Camera","url":"scanning.html#camera-core","text":"Core live-camera controls The live scanner is frame-driven on browsers that support requestVideoFrameCallback() and does not queue stale fast-path frames. Supported browsers use a dual-worker engine: a fast worker continuously handles fresh normal finder/geometry/decode attempts while a second worker runs the complete perspective, precise-alignment, QuadQR Auto Color, QR-region enhancement, Spectrum ECC, multi-frame, and damaged-code recovery stack independently. A difficult recovery frame therefore cannot hold the camera scheduler hostage. Finder detection itself remains JavaScript. WASM accelerates grayscale/Otsu/binary preprocessing and CRC beneath the same detector. The visible camera crop is resized to the normal scan dimension before worker transfer, reducing mobile bitmap and memory pressure. Unsupported or CSP-restricted environments automatically use the same full scanner on the main thread. Option Type / accepted values Default What it does constraints MediaStreamConstraints Environment camera, ideal 1280×720 @ 30 fps Passed directly to navigator.mediaDevices.getUserMedia() . Supply your own object to select a camera or alter resolution/frame-rate constraints. scanInterval number, milliseconds; minimum 24 Worker: 33 Main-thread fallback: 80 Minimum start-to-start cadence for live scan attempts. Worker mode can examine a 30 fps stream frame-for-frame when decoding is fast enough. If one scan takes longer, QuadQR drops stale frames rather than queueing work. Main-thread fallback uses a gentler default to preserve page responsiveness. cameraWorker boolean true Uses a dual-worker camera engine when Worker , createImageBitmap , and worker-side OffscreenCanvas are available. The fast worker stays responsive to fresh frames while the second worker runs the full recovery stack. Set false to force the main-thread engine. cameraWorkerRequired boolean false If true , worker initialization failure is thrown instead of automatically falling back to the full main-thread scanner. Useful when an application requires off-thread scanning. cameraWorkerUrl string or URL-compatible value bundled camera-scanner-worker.js Overrides the module-worker URL. Normally unnecessary; use it when a strict CSP, custom asset host, or bundler requires the worker file at a specific URL. useVideoFrameCallback boolean true when supported Uses requestVideoFrameCallback() to schedule work from real camera frames instead of blindly polling. QuadQR keeps only one scheduled attempt, so stale frames are not queued while a scan is still running. cameraGeometryReuse boolean true Reuses the most recent valid finder/perspective homography on following frames before running a new full finder search. If the code or camera moves, the same frame automatically falls back to normal detection. cameraGeometryReuseMaxMisses integer, minimum 1 5 Number of consecutive frames without a refreshed geometry solution before the cached camera homography is discarded. maxDimension positive pixels Camera scanner: minimum 480, default 640 Direct scanVideoFrame() : 960 Maximum width/height used for the normal fast camera scan. In worker mode the visible preview crop is resized before transfer. stopOnResult boolean true Stops the camera tracks after the first successful decode. Set false for continuous scanning. multiFrame boolean true Enables confidence-weighted fusion of compatible observations from several missed frames before another Reed-Solomon attempt. multiFrameWindow integer, clamped 2..8 4 Maximum compatible frame observations retained for temporal fusion. multiFrameMinFrames integer, 2..multiFrameWindow 2 Minimum observations required before the scanner tries a fused multi-frame decode. multiFrameMinAgreement ratio 0.62 Minimum high-confidence data-cell agreement required to keep consecutive RGBW observations in the same fusion history. Lower agreement resets the history to avoid mixing two different codes. multiFrameMinAgreementHighDensity ratio 0.58 Triangle16 version of the observation-agreement threshold. It is slightly lower because half-cell sampling is naturally noisier. onResult function (result, frameMeta) => void unset Called on successful decode before optional camera stop. onDecode function (result, frameMeta) => void unset Alias callback invoked alongside onResult for successful decodes. onScanMiss function (error) => void unset Called after the progressive recovery sequence and multi-frame fusion fail for a scan cycle. onDiagnostic function (event) => void unset Receives live camera diagnostic events such as camera-ready , frame , method , and success . Exceptions thrown by this callback are ignored so UI diagnostics cannot break scanning."},{"title":"Visible camera crop","page":"Scanning & Camera","url":"scanning.html#video-framing","text":"Visible camera crop On phones, the camera sensor is often 16:9 while the preview element is portrait with object-fit: cover . QuadQR scans the source pixels corresponding to what the user can actually see instead of silently scanning the hidden full sensor frame. Option Type / accepted values Default What it does videoCropMode \"visible\" behavior by default; \"full\" disables cropping visible/automatic For object-fit: cover previews, maps the visible element box back to the matching camera-source rectangle so hidden sensor pixels do not shrink the QR during scanning. videoObjectFit CSS object-fit value computed style, otherwise \"cover\" Overrides the object-fit value used when mapping the visible video crop. Cropping is applied only for cover . videoObjectPosition CSS-like position string computed style, otherwise \"50% 50%\" Controls where the visible crop lies inside the camera source. Supports left/right/top/bottom/center and percentages. videoCropInset fraction, clamped 0..0.18 0 Optionally trims a small extra border from the already-visible crop, useful when preview edges are soft or obstructed. canvas HTMLCanvasElement new scratch canvas Direct scanVideoFrame() option for reusing a caller-owned canvas and reducing allocations."},{"title":"Progressive camera recovery","page":"Scanning & Camera","url":"scanning.html#progressive-camera-recovery","text":"Progressive camera recovery After a miss, the scanner increases effort in bounded stages. The *Every options are cadence controls measured in missed scan cycles. They are performance knobs, not millisecond timers. cameraFinderRecoveryEvery example Default 2 means finder recovery is eligible on the first retry after a miss, then every other missed cycle. 1 runs it on every missed cycle. 4 uses it much less often, reducing CPU but potentially delaying recovery when lighting or perspective is difficult. Option Type / accepted values Default What it does cameraFinderRecoveryEvery integer, minimum 1 2 Controls how often the more expensive finder-recovery path is allowed after misses. 1 means every missed scan cycle; 2 means every other missed cycle. Recovery is first eligible on the first retry after a miss. cameraAutoEnhanceEvery integer, minimum 1 2 Cadence for progressive Auto Tone / Contrast / Color recovery after misses. Higher values reduce CPU at the cost of slower recovery on difficult frames. cameraAutoColorEvery integer, minimum 1 1 Cadence for camera-specific QuadQR-specific QuadQR Auto Color recovery. The default allows it on every missed cycle because this is a targeted recovery path. cameraAutoColorRecovery boolean true Master switch for camera-specific cropped QuadQR Auto Color recovery after a fast scan miss. cameraAutoColorCropInsets array of fractions, each clamped 0..0.30 [0.08, 0.16, 0.22, 0] Ordered centered crop profiles used by camera QuadQR Auto Color. The scanner tries tighter QR-centric histograms first and full frame last. cameraAutoColorAnalysisInsets array of fractions unset Per-profile histogram analysis insets matching cameraAutoColorCropInsets . Useful for explicitly tuning each crop profile. cameraAutoColorAnalysisInset fraction 0..0.30 profile defaults 0.10, 0.08, 0.04, 0.10 Single fallback analysis inset used when a per-profile value is not supplied. cameraAutoColorBlackClip number 0.0001 Shadow clipping for the camera-specific strong QuadQR Auto Color pass. cameraAutoColorWhiteClip number 0.004 Highlight clipping fallback for camera QuadQR Auto Color. cameraAutoColorHighlightPercentile number 0.70..0.9999 0.95 Bright-reference percentile used by camera QuadQR Auto Color. cameraAutoColorOutputHighlight number, clamped 96..250 190 Target highlight output used by the strong camera QuadQR Auto Color levels mapping. cameraAutoColorMinimumInputRange number 72 Minimum assumed input tonal range for camera QuadQR Auto Color. cameraAutoColorTargetSamples positive integer 90000 Histogram sample budget for camera QuadQR Auto Color. cameraHighResolutionRecovery boolean true Allows a bounded high-resolution retry when the fast pass already sees enough finder patterns but dense modules may have been lost by normal downscaling. cameraHighResolutionMinFinders integer 2 Minimum finder count needed before the high-resolution retry is considered. This prevents wasting the expensive pass on empty frames. cameraHighResolutionMaxDimension positive pixels, never below normal maxDimension 960 Maximum dimension used by the full recovery worker for high-detail geometry/color recovery. cameraHighResolutionEvery integer, minimum 1 2 Cadence for eligible high-resolution retries after misses. cameraRecoveryStrongFinderInterval milliseconds, minimum 80 120 Minimum interval for parallel full recovery when at least two finders are already visible. cameraRecoveryWeakFinderInterval milliseconds 260 Minimum interval after a persistent one-finder candidate is allowed to trigger recovery. cameraRecoveryWeakFinderFrames integer, minimum 2 2 Consecutive fresh frames that must report exactly one finder before the full recovery worker is armed. Zero-finder frames never trigger recovery."},{"title":"How the recovery sequence behaves","page":"Scanning & Camera","url":"scanning.html#recovery-sequence","text":"How the recovery sequence behaves Fresh fast scan: a 640 px visible ROI is immediately checked for normal finder geometry, calibrated sampling, ECC, and CRC. Parallel recovery dispatch: finder evidence controls whether the independent full-recovery worker receives a fresh frame. Two or more finders trigger immediately; one finder must persist; zero-finder scenes stay fast-only. High-resolution geometry retry: the recovery worker can use up to 960 px when dense modules need more pixels. QuadQR Camera Auto Color crops: progressively QR-centric histogram windows remain available for strong yellow/warm casts. QR-region enhancement: if geometry is strong, enhance only the rectified code area. Multi-frame fusion: combine compatible high-confidence observations and retry Spectrum ECC 2.0."},{"title":"Multi-frame confidence fusion","page":"Scanning & Camera","url":"scanning.html#multi-frame-confidence-fusion","text":"Multi-frame confidence fusion Fusion is not simple majority voting. Each cell vote is weighted by source confidence, frame quality, and recency. Second-best cell hypotheses contribute weak evidence. Before frames are fused, high-confidence data-cell agreement is checked so two different QuadQR symbols of the same version do not contaminate each other."},{"title":"Camera callbacks and frame metadata","page":"Scanning & Camera","url":"scanning.html#camera-callbacks","text":"Camera callbacks and frame metadata onResult and onDecode receive the same successful decode plus frame metadata. onDiagnostic receives live attempt events. Full field definitions are in Configuration Reference → Camera callback payloads ."},{"title":"Understanding scanner confidence","page":"Scanning & Camera","url":"scanning.html#scanner-confidence","text":"Understanding scanner confidence Successful image scans include an overall confidence plus geometry, calibration, structure, and ECC-related diagnostics. Confidence is a ranking/debug aid, not a cryptographic guarantee. Payload acceptance still requires Reed-Solomon validation and the final CRC."},{"title":"Secure and signed scans","page":"Scanning & Camera","url":"scanning.html#secure-scans","text":"Secure and signed scans A secure result can be geometrically and cryptographically intact while still returning text: null . Use decryptDecoded() with the password or raw key. Signed payload verification is separate: call verifyDecodedSignature() with a trusted Ed25519 public key or trusted-key registry."},{"title":"Reliability testing","page":"Scanning & Camera","url":"scanning.html#reliability-testing","text":"Reliability testing Use runReliabilityLab() , runPerspectiveSweep() , and assessScanability() to test scanner behavior under blur, lighting shifts, downscaling, and 3D perspective before shipping a physical workflow. Previous ← Node.js Next Compression → QuadQR Experimental RGBW matrix symbology. AGPL-3.0 licensed"},{"title":"Secure Payloads","page":"Secure Payloads","url":"security.html","text":"QUADQR DOCUMENTATION Secure Payloads Complete option definitions All password, raw-key, signing, and verification fields are listed in the Configuration Reference . QuadQR Secure Payload v1 is an optional authenticated-encryption layer above the matrix codec and Spectrum ECC. TEXT Copy plaintext -> encryption + authentication -> secure envelope -> QuadQR framing -> Spectrum ECC -> RGBW matrix Normal QuadQR symbols are unencrypted by default. Secure Payload is used only when explicitly requested. Which mode should I use? Mode Use it when Password A person will enter or share a secret password to unlock the payload Raw 256-bit key Your application, service, device, or scanner already manages cryptographic keys Both modes use AES-256-GCM for confidentiality and authenticated integrity. Password mode Password mode uses: PBKDF2-HMAC-SHA-256 random 16-byte salt configurable iteration count, default 600,000 AES-256-GCM random 12-byte nonce authenticated envelope metadata JS Copy import { encodeSecureText } from \"quadqr-js\"; const code = await encodeSecureText(\"private\", { security: { mode: \"password\", password: \"long unique password\" } }); The password is never stored in the QuadQR symbol. Raw 256-bit key mode Raw-key mode accepts exactly 32 secret bytes, either as bytes or a 64-character hexadecimal string. JS Copy import { encodeSecureText, generateRaw256Key } from \"quadqr-js\"; const key = generateRaw256Key(); const code = await encodeSecureText(\"machine secret\", { security: { mode: \"raw-key\", key } }); By default, the secure envelope can include a short SHA-256 key fingerprint/key ID. This is a non-secret routing hint that can help an application choose the correct key. The actual secret key is never embedded in the symbol. Scanning secure QuadQR Scanning does not automatically reveal protected plaintext. The scanner first returns a locked result: JS Copy const locked = await scanFile(file); console.log(locked.secure); // true console.log(locked.requiresDecryption); // true console.log(locked.security.mode); // password | raw-key console.log(locked.text); // null Then unlock it with the correct credential: JS Copy const unlocked = await decryptDecoded(locked, { password: \"long unique password\" }); console.log(unlocked.text); For raw-key mode: JS Copy const unlocked = await decryptDecoded(locked, { key }); Image upload, camera, matrix, RGBA, and Node.js scan results all use the same secure-result model. Authentication behavior AES-GCM authentication rejects: an incorrect password; an incorrect raw key; modified ciphertext; modified authenticated secure-envelope metadata. QuadQR's CRC and Spectrum ECC still handle matrix/image corruption before decryption is attempted. Cryptographic authentication is a separate final security check. Key management guidance Use long, unique passwords for password mode. Generate raw keys with generateRaw256Key() or another cryptographically secure random source. Store long-lived raw keys in an appropriate secret manager, operating-system key store, HSM, or equivalent secure storage for your application. Do not put the secret password or raw key inside the same QuadQR symbol. Do not hard-code a long-lived raw key in public client-side JavaScript if it is intended to remain secret. Security boundaries Secure Payload protects payload confidentiality and integrity, but it does not solve every application-security problem: It does not hide the fact that a QuadQR symbol exists. A photographed symbol can still be copied. Encryption by itself is not replay protection. Ticket/access systems should use server-side state, nonces, expiry, signatures, or another replay-control mechanism appropriate to the application. Decoded plaintext is still untrusted application input and should be validated before use. Previous ← Compression Next High Density Mode → QuadQR Experimental RGBW matrix symbology. AGPL-3.0 licensed"},{"title":"Which mode should I use?","page":"Secure Payloads","url":"security.html#which-mode-should-i-use","text":"Which mode should I use? Mode Use it when Password A person will enter or share a secret password to unlock the payload Raw 256-bit key Your application, service, device, or scanner already manages cryptographic keys Both modes use AES-256-GCM for confidentiality and authenticated integrity."},{"title":"Password mode","page":"Secure Payloads","url":"security.html#password-mode","text":"Password mode Password mode uses: PBKDF2-HMAC-SHA-256 random 16-byte salt configurable iteration count, default 600,000 AES-256-GCM random 12-byte nonce authenticated envelope metadata JS Copy import { encodeSecureText } from \"quadqr-js\"; const code = await encodeSecureText(\"private\", { security: { mode: \"password\", password: \"long unique password\" } }); The password is never stored in the QuadQR symbol."},{"title":"Raw 256-bit key mode","page":"Secure Payloads","url":"security.html#raw-256-bit-key-mode","text":"Raw 256-bit key mode Raw-key mode accepts exactly 32 secret bytes, either as bytes or a 64-character hexadecimal string. JS Copy import { encodeSecureText, generateRaw256Key } from \"quadqr-js\"; const key = generateRaw256Key(); const code = await encodeSecureText(\"machine secret\", { security: { mode: \"raw-key\", key } }); By default, the secure envelope can include a short SHA-256 key fingerprint/key ID. This is a non-secret routing hint that can help an application choose the correct key. The actual secret key is never embedded in the symbol."},{"title":"Scanning secure QuadQR","page":"Secure Payloads","url":"security.html#scanning-secure-quadqr","text":"Scanning secure QuadQR Scanning does not automatically reveal protected plaintext. The scanner first returns a locked result: JS Copy const locked = await scanFile(file); console.log(locked.secure); // true console.log(locked.requiresDecryption); // true console.log(locked.security.mode); // password | raw-key console.log(locked.text); // null Then unlock it with the correct credential: JS Copy const unlocked = await decryptDecoded(locked, { password: \"long unique password\" }); console.log(unlocked.text); For raw-key mode: JS Copy const unlocked = await decryptDecoded(locked, { key }); Image upload, camera, matrix, RGBA, and Node.js scan results all use the same secure-result model."},{"title":"Authentication behavior","page":"Secure Payloads","url":"security.html#authentication-behavior","text":"Authentication behavior AES-GCM authentication rejects: an incorrect password; an incorrect raw key; modified ciphertext; modified authenticated secure-envelope metadata. QuadQR's CRC and Spectrum ECC still handle matrix/image corruption before decryption is attempted. Cryptographic authentication is a separate final security check."},{"title":"Key management guidance","page":"Secure Payloads","url":"security.html#key-management-guidance","text":"Key management guidance Use long, unique passwords for password mode. Generate raw keys with generateRaw256Key() or another cryptographically secure random source. Store long-lived raw keys in an appropriate secret manager, operating-system key store, HSM, or equivalent secure storage for your application. Do not put the secret password or raw key inside the same QuadQR symbol. Do not hard-code a long-lived raw key in public client-side JavaScript if it is intended to remain secret."},{"title":"Security boundaries","page":"Secure Payloads","url":"security.html#security-boundaries","text":"Security boundaries Secure Payload protects payload confidentiality and integrity, but it does not solve every application-security problem: It does not hide the fact that a QuadQR symbol exists. A photographed symbol can still be copied. Encryption by itself is not replay protection. Ticket/access systems should use server-side state, nonces, expiry, signatures, or another replay-control mechanism appropriate to the application. Decoded plaintext is still untrusted application input and should be validated before use. Previous ← Compression Next High Density Mode → QuadQR Experimental RGBW matrix symbology. AGPL-3.0 licensed"},{"title":"Technical Specification","page":"Technical Specification","url":"specification.html","text":"QUADQR DOCUMENTATION Technical Specification Status QuadQR is an experimental RGBW matrix symbology. Normal mode uses 4-state RGBW cells, while the optional High Density Mode is experimental and uses the 16-state Triangle16 physical layout. It is not ISO/IEC QR Code and is not intended to be decoded by standard QR readers. The physical matrix format remains QuadQR Format v6 . Normal application data is always treated simply as UTF-8 text or arbitrary bytes. Compression, signatures, encryption, rendering, and diagnostics are optional features layered around that stable matrix codec. For exact matrix geometry and Reed-Solomon framing, see format reference . 1. Layer model TEXT Copy Application bytes / UTF-8 text │ ├─ optional compression metadata (internal) ├─ optional Ed25519 signature metadata (internal) ├─ optional Secure Payload v1 (AES-256-GCM) │ └─ QuadQR Format v6 ├─ protected header ├─ CRC-32 ├─ GF(256) Reed-Solomon Spectrum ECC ├─ spectral-spatial interleaving └─ normal RGBW or High Density Triangle16 matrix There is deliberately no public payload-type registry . Applications do not select URL, JSON, contact, Wi-Fi, or other semantic types. They encode text or bytes and interpret that data themselves. 2. Physical cell alphabet Data modules use exactly four states: State Bits Internal value Red 00 0 Green 01 1 Blue 10 2 White 11 3 Structural black is separate from the data alphabet. One encoded byte maps to exactly four RGBW data cells. High Density Mode When highDensity: true is selected, each payload module has a fixed / diagonal and two independently classified RGBW regions. This creates 16 states and 4 raw bits per body cell. The protected header stays solid-color at 2 bits per cell for robust bootstrap recovery, then header flag bit 6 tells the decoder that the ECC/body stream uses Triangle16 packing. Scanner sampling uses three interior anchors per triangle, robust aggregation, and a spatial-instability penalty while excluding the diagonal boundary. 3. Matrix sizing TEXT Copy size = 21 + 4 × (version - 1) version = 1..40 Three 7×7 finder patterns are always present. Version 1 has the legacy QuadQR 5×5 bottom-right alignment marker. Versions 2–40 use the distributed alignment schedule defined in format reference . 4. Format v6 header flags The protected Format v6 header uses: TEXT Copy bit 0 UTF-8 text flag bits 1..2 ECC profile id bit 3 Secure Payload v1 envelope bit 4 internal payload-extension metadata present bit 5 signed-payload hint bit 6 High Density Mode flag bit 7 reserved Bit 4 is an implementation/interoperability hint used only when compression or signing requires metadata. It is not a user-selectable payload mode. 5. Spectrum ECC The field remains: TEXT Copy GF(2^8) = GF(256) primitive polynomial = 0x11d Version 2+ parity profiles: Profile Parity bytes/block Correctable unknown byte errors/block L 12 6 M 24 12 Q 36 18 H 48 24 The decoder retains per-cell color confidence and one bounded alternate hypothesis from image/camera classification. Low-confidence bytes can first be promoted to known erasures, allowing Reed-Solomon recovery to use the existing parity budget more efficiently. If hard and error/erasure decoding still fail, Spectrum ECC 2.0 may try the alternate state for a bounded set of the least-confident cells. Candidate search is limited to single substitutions and a small pair set by default, and every successful path must still satisfy Reed-Solomon verification and CRC-32. 6. Internal payload extension envelope Compression and signing require a small amount of metadata. QuadQR stores that metadata in an internal extension envelope only when needed. Applications should normally use encodeText() , encodeBytes() , encodeSignedText() , or encodeSignedBytes() and never construct this envelope themselves. Current envelope magic: TEXT Copy QPX1 Fixed header size: 16 bytes . Offset Size Field 0 4 ASCII magic QPX1 4 1 Extension version ( 3 ; decoder also accepts legacy 1 and 2 ) 5 1 Flags 6 1 Compression ID 7 1 Signature algorithm ID 8 4 Original application payload length, big-endian 12 1 Signing key-ID length (v2+); legacy signer-label length in v1 13 1 Optional embedded public-key length 14 1 Signature length 15 1 Reserved ( 0 ) Variable bytes follow as: TEXT Copy keyId || optionalPublicKey || signature || storedPayload Envelope flags currently use: TEXT Copy bit 0 signed bit 1 compressed bit 2 public key embedded The envelope contains no semantic content type. The Format v6 text flag still determines whether the recovered application payload should be decoded as UTF-8 text. 7. Compression Compression IDs: TEXT Copy 0 = none 1 = QuadQR portable LZ (legacy) 2 = QuadQR portable raw DEFLATE 3 = bundled Brotli Compression ID 1 is the original LZSS-style stream with groups of up to eight tokens. Each group begins with one flag byte. A flag bit of 0 means a one-byte literal. A flag bit of 1 means a two-byte back-reference: TEXT Copy 12-bit offset: 1..4095 bytes 4-bit length: stored value + 3, therefore 3..18 bytes Compression ID 1 keeps the original QuadQR LZSS-style stream. Encoder levels 1..9 control candidate-history depth and bounded lazy-match effort; level 6 preserves the historical 32-candidate search depth. The 4095-byte window, 3..18 byte back-reference format, and decoder remain unchanged, and the level is not serialized. Compression ID 2 is a raw RFC 1951 DEFLATE stream. QuadQR's portable encoder uses a deterministic fixed-Huffman block, a 32 KiB LZ77 window, distances up to 32768 bytes, and matches up to 258 bytes. Encoder levels 1..9 control candidate-chain depth and lazy-match effort; the level is not serialized and does not change decoder behavior. The QuadQR decoder accepts the stored/fixed block forms emitted by the library. Compression ID 3 is a standard Brotli stream produced and decoded by QuadQR's bundled synchronous JavaScript codec. It uses no Node zlib , browser CompressionStream , DOM API, native addon, or network-loaded codec at runtime. Brotli is especially effective for repetitive text and structured payloads, while DEFLATE and legacy LZ remain available for deterministic selection and backward compatibility. Public compression modes are: TEXT Copy none auto smart brotli deflate lz compression: \"auto\" is the fast balanced policy. It evaluates LZ level 6, DEFLATE level 6, and Brotli quality 6 exactly once and chooses the smallest complete stored representation. For unsigned payloads that comparison includes the 16-byte extension-envelope cost, so Auto remains zero-overhead when compression is not useful. compression: \"smart\" is the opt-in CPU-heavy policy. It begins with the Auto candidates, computes the resulting Format v6 QuadQR version using the selected ECC/profile/version bounds, and only escalates when the next smaller matrix is plausibly reachable. The strong stage tests DEFLATE 8 and Brotli 9. If the symbol remains close to a smaller-version boundary, the maximum stage tests DEFLATE 9 and Brotli 11. With an explicitly requested fixed version, Smart does not chase a smaller matrix, but it may escalate when the balanced result does not fit and stronger compression can plausibly make that requested version fit. Signed and secure pipelines include their fixed envelope overhead when evaluating those version boundaries. Explicit compression: \"lz\" and compression: \"deflate\" accept compressionLevel 1..9 and default to 6. Explicit compression: \"brotli\" accepts compressionLevel 0..11 and defaults to 11. Compression levels are encoder-only parameters and are intentionally not stored in the extension envelope because LZ, RFC 1951, and Brotli decoders do not require them. Auto and Smart use LZ level 6; Smart's staged escalation remains focused on DEFLATE/Brotli. Compression occurs before signing, encryption, and Format v6 ECC. 8. Signed QuadQR Signature algorithm ID 1 is Ed25519 . Signed payloads in extension v2/v3 store: TEXT Copy optional compact key ID 64-byte Ed25519 signature optional 32-byte raw Ed25519 public key only when explicitly requested The normal production profile does not embed the public key. The signature covers: TEXT Copy 16-byte extension header || key ID || optional embedded public key || stored payload bytes The signature field itself is excluded from the signed message. The private Ed25519 key is used only by the issuer to create signatures and must never be embedded in a QuadQR. Verification uses a trusted public key supplied externally by the application, server, certificate, or trusted-key registry. The optional key ID is only an identifier that helps the verifier select the correct trusted public key. For compatibility or self-contained integrity checks, implementations may set embedPublicKey: true . A signature verified only against a key embedded in the same QuadQR proves integrity and key possession, but does not establish a trusted signer identity. Such verification should be reported separately from verification against an external trust anchor. Legacy extension v1 symbols used a signer label and embedded public key. Decoders may continue to read and verify those symbols for backward compatibility. 9. Secure + signed composition When compression, signing, and encryption are combined, QuadQR uses: TEXT Copy application payload → optional compression → optional Ed25519 signature metadata → AES-256-GCM Secure Payload v1 → Format v6 ECC/matrix After scanning: TEXT Copy Format v6 decode → AES-GCM authentication/decryption → internal compression/signature metadata processing → application payload → optional Ed25519 signature verification This keeps signature metadata confidential when encryption is enabled while preserving offline verification after decryption. 10. Text and binary APIs Text and bytes are first-class without semantic payload types: encodeText() / decodeMatrix().text for UTF-8 text; encodeBytes() for arbitrary bytes; encodeUint8Array() / decodeUint8Array() as explicit byte-oriented convenience APIs. Compression works on both text and byte payloads through the same compression option. 11. Rendering profiles Rendering does not modify the encoded matrix. Screen mode Uses the normal RGBW palette and permits: TEXT Copy classic soft depth inset Print mode mode: \"print\" applies print-oriented defaults: caller-controlled quiet-zone sizing, identical to screen mode (4 modules recommended by default); print-safe darker RGB primaries; Classic solid-module rendering by default; physical-size guidance through getPrintGuidance() . Recommended general-purpose starting module size is 0.40 mm/module . Real printer, paper, ink/toner, lamination, lighting, and camera validation remains necessary for production deployments. 12. Logo safety Logos are rendering overlays and never modify Format v6 data structures. size: \"auto\" estimates a conservative logo ratio from: ECC profile; encoded utilization; version 1 compact-profile penalty; clear-background usage; screen vs print mode. findMaxSafeLogoSize() can empirically search the largest decodable logo size when an ImageData-like logo is available. Generated symbols should still be verified after final rendering. 13. Scanner diagnostics Successful image scans expose normalized diagnostic fields including: TEXT Copy confidence geometryConfidence calibrationConfidence structureConfidence eccUtilization correctedErrors erasureSymbols With debug: true , the scanner can also report stage state, geometry candidates, finder/vision passes, latest sampled matrix, confidence matrix, color-normalization method, and the failed stage when decoding does not complete. Debug output is diagnostic evidence, not a cryptographic assurance score. 14. Scanability score and torture testing runImageStressTest() and assessScanability() use deterministic synthetic distortions to estimate robustness against: blur; low brightness; high exposure; uneven shadow; contrast loss; perspective distortion; JPEG-like quantization/block artifacts; downscaling. Current rating bands: TEXT Copy 90–100 Excellent 75–89 Good 50–74 Risky 0–49 Likely unscannable These scores are regression/testing aids. They do not replace validation with real phone cameras, printers, displays, paper stocks, lighting conditions, and physical damage. 15. Capacity planning Capacity is determined from the actual Format v6 layout, protected header, CRC, and Spectrum ECC plan. Compression can reduce stored payload bytes, while signatures and encryption add metadata bytes before the matrix codec. The benchmark helper can report: minimum QuadQR version; encoded bytes; remaining capacity; utilization; approximate same-letter standard QR byte-mode version. When only a payload byte count is known, compression: \"auto\" cannot predict the gain because compressibility depends on the actual bytes. Standard QR comparisons use the same nominal ECC letter only . QuadQR and ISO QR recovery strengths are not equivalent and should not be presented as such. 16. Compatibility principles Implementations should follow these rules: Keep RGBW mapping exactly R=00, G=01, B=10, W=11 . Keep GF(256) Spectrum ECC and its errors+erasures behavior. Preserve Format v5 decoding as a legacy compatibility path while encoding new symbols as Format v6. Keep application semantics outside the QuadQR codec. Do not require a growing content-type registry. Treat compression/signature metadata as internal transport metadata, not a separate user payload mode. Treat keyId only as an identifier. Signer trust comes from an external trusted public-key binding. Keep rendering effects out of structural finder/timing/alignment/calibration modules. Validate public format changes with both matrix and rendered-image scanner tests. 17. Reference implementation The JavaScript implementation in this repository is the current reference implementation. Public entry points are covered throughout this documentation and exercised by tests/self-test.js and tests/package-test.js . Previous ← Format Reference QuadQR Experimental RGBW matrix symbology. AGPL-3.0 licensed"},{"title":"Status","page":"Technical Specification","url":"specification.html#status","text":"Status QuadQR is an experimental RGBW matrix symbology. Normal mode uses 4-state RGBW cells, while the optional High Density Mode is experimental and uses the 16-state Triangle16 physical layout. It is not ISO/IEC QR Code and is not intended to be decoded by standard QR readers. The physical matrix format remains QuadQR Format v6 . Normal application data is always treated simply as UTF-8 text or arbitrary bytes. Compression, signatures, encryption, rendering, and diagnostics are optional features layered around that stable matrix codec. For exact matrix geometry and Reed-Solomon framing, see format reference ."},{"title":"1. Layer model","page":"Technical Specification","url":"specification.html#1-layer-model","text":"1. Layer model TEXT Copy Application bytes / UTF-8 text │ ├─ optional compression metadata (internal) ├─ optional Ed25519 signature metadata (internal) ├─ optional Secure Payload v1 (AES-256-GCM) │ └─ QuadQR Format v6 ├─ protected header ├─ CRC-32 ├─ GF(256) Reed-Solomon Spectrum ECC ├─ spectral-spatial interleaving └─ normal RGBW or High Density Triangle16 matrix There is deliberately no public payload-type registry . Applications do not select URL, JSON, contact, Wi-Fi, or other semantic types. They encode text or bytes and interpret that data themselves."},{"title":"2. Physical cell alphabet","page":"Technical Specification","url":"specification.html#2-physical-cell-alphabet","text":"2. Physical cell alphabet Data modules use exactly four states: State Bits Internal value Red 00 0 Green 01 1 Blue 10 2 White 11 3 Structural black is separate from the data alphabet. One encoded byte maps to exactly four RGBW data cells. High Density Mode # When highDensity: true is selected, each payload module has a fixed / diagonal and two independently classified RGBW regions. This creates 16 states and 4 raw bits per body cell. The protected header stays solid-color at 2 bits per cell for robust bootstrap recovery, then header flag bit 6 tells the decoder that the ECC/body stream uses Triangle16 packing. Scanner sampling uses three interior anchors per triangle, robust aggregation, and a spatial-instability penalty while excluding the diagonal boundary."},{"title":"High Density Mode","page":"Technical Specification","url":"specification.html#high-density-mode","text":"High Density Mode When highDensity: true is selected, each payload module has a fixed / diagonal and two independently classified RGBW regions. This creates 16 states and 4 raw bits per body cell. The protected header stays solid-color at 2 bits per cell for robust bootstrap recovery, then header flag bit 6 tells the decoder that the ECC/body stream uses Triangle16 packing. Scanner sampling uses three interior anchors per triangle, robust aggregation, and a spatial-instability penalty while excluding the diagonal boundary."},{"title":"3. Matrix sizing","page":"Technical Specification","url":"specification.html#3-matrix-sizing","text":"3. Matrix sizing TEXT Copy size = 21 + 4 × (version - 1) version = 1..40 Three 7×7 finder patterns are always present. Version 1 has the legacy QuadQR 5×5 bottom-right alignment marker. Versions 2–40 use the distributed alignment schedule defined in format reference ."},{"title":"4. Format v6 header flags","page":"Technical Specification","url":"specification.html#4-format-v6-header-flags","text":"4. Format v6 header flags The protected Format v6 header uses: TEXT Copy bit 0 UTF-8 text flag bits 1..2 ECC profile id bit 3 Secure Payload v1 envelope bit 4 internal payload-extension metadata present bit 5 signed-payload hint bit 6 High Density Mode flag bit 7 reserved Bit 4 is an implementation/interoperability hint used only when compression or signing requires metadata. It is not a user-selectable payload mode."},{"title":"5. Spectrum ECC","page":"Technical Specification","url":"specification.html#5-spectrum-ecc","text":"5. Spectrum ECC The field remains: TEXT Copy GF(2^8) = GF(256) primitive polynomial = 0x11d Version 2+ parity profiles: Profile Parity bytes/block Correctable unknown byte errors/block L 12 6 M 24 12 Q 36 18 H 48 24 The decoder retains per-cell color confidence and one bounded alternate hypothesis from image/camera classification. Low-confidence bytes can first be promoted to known erasures, allowing Reed-Solomon recovery to use the existing parity budget more efficiently. If hard and error/erasure decoding still fail, Spectrum ECC 2.0 may try the alternate state for a bounded set of the least-confident cells. Candidate search is limited to single substitutions and a small pair set by default, and every successful path must still satisfy Reed-Solomon verification and CRC-32."},{"title":"6. Internal payload extension envelope","page":"Technical Specification","url":"specification.html#6-internal-payload-extension-envelope","text":"6. Internal payload extension envelope Compression and signing require a small amount of metadata. QuadQR stores that metadata in an internal extension envelope only when needed. Applications should normally use encodeText() , encodeBytes() , encodeSignedText() , or encodeSignedBytes() and never construct this envelope themselves. Current envelope magic: TEXT Copy QPX1 Fixed header size: 16 bytes . Offset Size Field 0 4 ASCII magic QPX1 4 1 Extension version ( 3 ; decoder also accepts legacy 1 and 2 ) 5 1 Flags 6 1 Compression ID 7 1 Signature algorithm ID 8 4 Original application payload length, big-endian 12 1 Signing key-ID length (v2+); legacy signer-label length in v1 13 1 Optional embedded public-key length 14 1 Signature length 15 1 Reserved ( 0 ) Variable bytes follow as: TEXT Copy keyId || optionalPublicKey || signature || storedPayload Envelope flags currently use: TEXT Copy bit 0 signed bit 1 compressed bit 2 public key embedded The envelope contains no semantic content type. The Format v6 text flag still determines whether the recovered application payload should be decoded as UTF-8 text."},{"title":"7. Compression","page":"Technical Specification","url":"specification.html#7-compression","text":"7. Compression Compression IDs: TEXT Copy 0 = none 1 = QuadQR portable LZ (legacy) 2 = QuadQR portable raw DEFLATE 3 = bundled Brotli Compression ID 1 is the original LZSS-style stream with groups of up to eight tokens. Each group begins with one flag byte. A flag bit of 0 means a one-byte literal. A flag bit of 1 means a two-byte back-reference: TEXT Copy 12-bit offset: 1..4095 bytes 4-bit length: stored value + 3, therefore 3..18 bytes Compression ID 1 keeps the original QuadQR LZSS-style stream. Encoder levels 1..9 control candidate-history depth and bounded lazy-match effort; level 6 preserves the historical 32-candidate search depth. The 4095-byte window, 3..18 byte back-reference format, and decoder remain unchanged, and the level is not serialized. Compression ID 2 is a raw RFC 1951 DEFLATE stream. QuadQR's portable encoder uses a deterministic fixed-Huffman block, a 32 KiB LZ77 window, distances up to 32768 bytes, and matches up to 258 bytes. Encoder levels 1..9 control candidate-chain depth and lazy-match effort; the level is not serialized and does not change decoder behavior. The QuadQR decoder accepts the stored/fixed block forms emitted by the library. Compression ID 3 is a standard Brotli stream produced and decoded by QuadQR's bundled synchronous JavaScript codec. It uses no Node zlib , browser CompressionStream , DOM API, native addon, or network-loaded codec at runtime. Brotli is especially effective for repetitive text and structured payloads, while DEFLATE and legacy LZ remain available for deterministic selection and backward compatibility. Public compression modes are: TEXT Copy none auto smart brotli deflate lz compression: \"auto\" is the fast balanced policy. It evaluates LZ level 6, DEFLATE level 6, and Brotli quality 6 exactly once and chooses the smallest complete stored representation. For unsigned payloads that comparison includes the 16-byte extension-envelope cost, so Auto remains zero-overhead when compression is not useful. compression: \"smart\" is the opt-in CPU-heavy policy. It begins with the Auto candidates, computes the resulting Format v6 QuadQR version using the selected ECC/profile/version bounds, and only escalates when the next smaller matrix is plausibly reachable. The strong stage tests DEFLATE 8 and Brotli 9. If the symbol remains close to a smaller-version boundary, the maximum stage tests DEFLATE 9 and Brotli 11. With an explicitly requested fixed version, Smart does not chase a smaller matrix, but it may escalate when the balanced result does not fit and stronger compression can plausibly make that requested version fit. Signed and secure pipelines include their fixed envelope overhead when evaluating those version boundaries. Explicit compression: \"lz\" and compression: \"deflate\" accept compressionLevel 1..9 and default to 6. Explicit compression: \"brotli\" accepts compressionLevel 0..11 and defaults to 11. Compression levels are encoder-only parameters and are intentionally not stored in the extension envelope because LZ, RFC 1951, and Brotli decoders do not require them. Auto and Smart use LZ level 6; Smart's staged escalation remains focused on DEFLATE/Brotli. Compression occurs before signing, encryption, and Format v6 ECC."},{"title":"8. Signed QuadQR","page":"Technical Specification","url":"specification.html#8-signed-quadqr","text":"8. Signed QuadQR Signature algorithm ID 1 is Ed25519 . Signed payloads in extension v2/v3 store: TEXT Copy optional compact key ID 64-byte Ed25519 signature optional 32-byte raw Ed25519 public key only when explicitly requested The normal production profile does not embed the public key. The signature covers: TEXT Copy 16-byte extension header || key ID || optional embedded public key || stored payload bytes The signature field itself is excluded from the signed message. The private Ed25519 key is used only by the issuer to create signatures and must never be embedded in a QuadQR. Verification uses a trusted public key supplied externally by the application, server, certificate, or trusted-key registry. The optional key ID is only an identifier that helps the verifier select the correct trusted public key. For compatibility or self-contained integrity checks, implementations may set embedPublicKey: true . A signature verified only against a key embedded in the same QuadQR proves integrity and key possession, but does not establish a trusted signer identity. Such verification should be reported separately from verification against an external trust anchor. Legacy extension v1 symbols used a signer label and embedded public key. Decoders may continue to read and verify those symbols for backward compatibility."},{"title":"9. Secure + signed composition","page":"Technical Specification","url":"specification.html#9-secure-signed-composition","text":"9. Secure + signed composition When compression, signing, and encryption are combined, QuadQR uses: TEXT Copy application payload → optional compression → optional Ed25519 signature metadata → AES-256-GCM Secure Payload v1 → Format v6 ECC/matrix After scanning: TEXT Copy Format v6 decode → AES-GCM authentication/decryption → internal compression/signature metadata processing → application payload → optional Ed25519 signature verification This keeps signature metadata confidential when encryption is enabled while preserving offline verification after decryption."},{"title":"10. Text and binary APIs","page":"Technical Specification","url":"specification.html#10-text-and-binary-apis","text":"10. Text and binary APIs Text and bytes are first-class without semantic payload types: encodeText() / decodeMatrix().text for UTF-8 text; encodeBytes() for arbitrary bytes; encodeUint8Array() / decodeUint8Array() as explicit byte-oriented convenience APIs. Compression works on both text and byte payloads through the same compression option."},{"title":"11. Rendering profiles","page":"Technical Specification","url":"specification.html#11-rendering-profiles","text":"11. Rendering profiles Rendering does not modify the encoded matrix. Screen mode # Uses the normal RGBW palette and permits: TEXT Copy classic soft depth inset Print mode # mode: \"print\" applies print-oriented defaults: caller-controlled quiet-zone sizing, identical to screen mode (4 modules recommended by default); print-safe darker RGB primaries; Classic solid-module rendering by default; physical-size guidance through getPrintGuidance() . Recommended general-purpose starting module size is 0.40 mm/module . Real printer, paper, ink/toner, lamination, lighting, and camera validation remains necessary for production deployments."},{"title":"Screen mode","page":"Technical Specification","url":"specification.html#screen-mode","text":"Screen mode Uses the normal RGBW palette and permits: TEXT Copy classic soft depth inset"},{"title":"Print mode","page":"Technical Specification","url":"specification.html#print-mode","text":"Print mode mode: \"print\" applies print-oriented defaults: caller-controlled quiet-zone sizing, identical to screen mode (4 modules recommended by default); print-safe darker RGB primaries; Classic solid-module rendering by default; physical-size guidance through getPrintGuidance() . Recommended general-purpose starting module size is 0.40 mm/module . Real printer, paper, ink/toner, lamination, lighting, and camera validation remains necessary for production deployments."},{"title":"12. Logo safety","page":"Technical Specification","url":"specification.html#12-logo-safety","text":"12. Logo safety Logos are rendering overlays and never modify Format v6 data structures. size: \"auto\" estimates a conservative logo ratio from: ECC profile; encoded utilization; version 1 compact-profile penalty; clear-background usage; screen vs print mode. findMaxSafeLogoSize() can empirically search the largest decodable logo size when an ImageData-like logo is available. Generated symbols should still be verified after final rendering."},{"title":"13. Scanner diagnostics","page":"Technical Specification","url":"specification.html#13-scanner-diagnostics","text":"13. Scanner diagnostics Successful image scans expose normalized diagnostic fields including: TEXT Copy confidence geometryConfidence calibrationConfidence structureConfidence eccUtilization correctedErrors erasureSymbols With debug: true , the scanner can also report stage state, geometry candidates, finder/vision passes, latest sampled matrix, confidence matrix, color-normalization method, and the failed stage when decoding does not complete. Debug output is diagnostic evidence, not a cryptographic assurance score."},{"title":"14. Scanability score and torture testing","page":"Technical Specification","url":"specification.html#14-scanability-score-and-torture-testing","text":"14. Scanability score and torture testing runImageStressTest() and assessScanability() use deterministic synthetic distortions to estimate robustness against: blur; low brightness; high exposure; uneven shadow; contrast loss; perspective distortion; JPEG-like quantization/block artifacts; downscaling. Current rating bands: TEXT Copy 90–100 Excellent 75–89 Good 50–74 Risky 0–49 Likely unscannable These scores are regression/testing aids. They do not replace validation with real phone cameras, printers, displays, paper stocks, lighting conditions, and physical damage."},{"title":"15. Capacity planning","page":"Technical Specification","url":"specification.html#15-capacity-planning","text":"15. Capacity planning Capacity is determined from the actual Format v6 layout, protected header, CRC, and Spectrum ECC plan. Compression can reduce stored payload bytes, while signatures and encryption add metadata bytes before the matrix codec. The benchmark helper can report: minimum QuadQR version; encoded bytes; remaining capacity; utilization; approximate same-letter standard QR byte-mode version. When only a payload byte count is known, compression: \"auto\" cannot predict the gain because compressibility depends on the actual bytes. Standard QR comparisons use the same nominal ECC letter only . QuadQR and ISO QR recovery strengths are not equivalent and should not be presented as such."},{"title":"16. Compatibility principles","page":"Technical Specification","url":"specification.html#16-compatibility-principles","text":"16. Compatibility principles Implementations should follow these rules: Keep RGBW mapping exactly R=00, G=01, B=10, W=11 . Keep GF(256) Spectrum ECC and its errors+erasures behavior. Preserve Format v5 decoding as a legacy compatibility path while encoding new symbols as Format v6. Keep application semantics outside the QuadQR codec. Do not require a growing content-type registry. Treat compression/signature metadata as internal transport metadata, not a separate user payload mode. Treat keyId only as an identifier. Signer trust comes from an external trusted public-key binding. Keep rendering effects out of structural finder/timing/alignment/calibration modules. Validate public format changes with both matrix and rendered-image scanner tests."},{"title":"17. Reference implementation","page":"Technical Specification","url":"specification.html#17-reference-implementation","text":"17. Reference implementation The JavaScript implementation in this repository is the current reference implementation. Public entry points are covered throughout this documentation and exercised by tests/self-test.js and tests/package-test.js . Previous ← Format Reference QuadQR Experimental RGBW matrix symbology. AGPL-3.0 licensed"},{"title":"WebAssembly","page":"WebAssembly","url":"wasm.html","text":"QUADQR DOCUMENTATION WebAssembly QuadQR includes an optional prebuilt WebAssembly module at: TEXT Copy dist/wasm/quadqr-core.wasm The normal JavaScript implementation works without WASM. Applications can opt in when they want to use the bundled accelerator. Enable WASM JS Copy import { initWasm } from \"quadqr-js\"; const state = await initWasm(); console.log(state.accelerators); // [\"crc32\", \"scanner-preprocess\"] The bundled WASM module accelerates CRC-32 and the scanner's hot full-frame preprocessing: RGBA-to-grayscale conversion, Otsu threshold calculation, and binary finder-image generation. Finder-pattern detection, geometry estimation, perspective solving, color classification, and recovery orchestration remain JavaScript. Once initialized, scanImageData() and camera scanning use the faster preprocessing path automatically while preserving the JavaScript fallback. Check the current state JS Copy import { getWasmState } from \"quadqr-js\"; console.log(getWasmState()); Before initialization, or after disabling WASM, the result is null . Disable WASM JS Copy import { disableWasm } from \"quadqr-js\"; disableWasm(); QuadQR immediately returns to the JavaScript implementation. Custom WASM URL Browser and CDN applications can override the asset location: JS Copy await initWasm({ url: \"https://example.com/assets/quadqr-core.wasm\" }); You can also provide WASM bytes directly through the bytes option. CDN behavior When the classic quadqr.min.js global build is loaded from a CDN, QuadQR.initWasm() resolves the bundled sibling WASM asset from the same package/version location. HTML Copy <script src=\"https://cdn.jsdelivr.net/npm/quadqr-js@1.5.5/dist/quadqr.min.js\"></script> <script> await QuadQR.initWasm(); </script> Building WASM from source QuadQR keeps the generated WASM binary and its verification metadata in the repository: TEXT Copy wasm/quadqr-core.wasm wasm/quadqr-core.build.json For normal development, run: BASH Copy npm run build The build verifies SHA-256 fingerprints for the C source, WASM compiler flags, and checked-in binary. If they match, the verified prebuilt binary is reused. If wasm-src/quadqr_core.c or the build flags changed, the normal build automatically recompiles WASM. If recompilation is required but clang is unavailable in PATH , the build fails rather than silently reusing stale WASM. To force only the WASM rebuild: BASH Copy npm run build:wasm A forced rebuild always requires LLVM/Clang. Rust, Cargo, and Emscripten are not required by the current build. npm publish runs the normal build through prepublishOnly , so the same stale-WASM protection applies before publishing. Fallback behavior WASM is never required for the QuadQR wire format or scanner correctness. If WebAssembly cannot load, the JavaScript scanner remains available. For performance-sensitive scanning, initialize WASM once during application startup before repeated scanImageData() calls or camera scanning. Previous ← CLI Reference Next Format Reference → QuadQR Experimental RGBW matrix symbology. AGPL-3.0 licensed"},{"title":"Enable WASM","page":"WebAssembly","url":"wasm.html#enable-wasm","text":"Enable WASM JS Copy import { initWasm } from \"quadqr-js\"; const state = await initWasm(); console.log(state.accelerators); // [\"crc32\", \"scanner-preprocess\"] The bundled WASM module accelerates CRC-32 and the scanner's hot full-frame preprocessing: RGBA-to-grayscale conversion, Otsu threshold calculation, and binary finder-image generation. Finder-pattern detection, geometry estimation, perspective solving, color classification, and recovery orchestration remain JavaScript. Once initialized, scanImageData() and camera scanning use the faster preprocessing path automatically while preserving the JavaScript fallback."},{"title":"Check the current state","page":"WebAssembly","url":"wasm.html#check-the-current-state","text":"Check the current state JS Copy import { getWasmState } from \"quadqr-js\"; console.log(getWasmState()); Before initialization, or after disabling WASM, the result is null ."},{"title":"Disable WASM","page":"WebAssembly","url":"wasm.html#disable-wasm","text":"Disable WASM JS Copy import { disableWasm } from \"quadqr-js\"; disableWasm(); QuadQR immediately returns to the JavaScript implementation."},{"title":"Custom WASM URL","page":"WebAssembly","url":"wasm.html#custom-wasm-url","text":"Custom WASM URL Browser and CDN applications can override the asset location: JS Copy await initWasm({ url: \"https://example.com/assets/quadqr-core.wasm\" }); You can also provide WASM bytes directly through the bytes option."},{"title":"CDN behavior","page":"WebAssembly","url":"wasm.html#cdn-behavior","text":"CDN behavior When the classic quadqr.min.js global build is loaded from a CDN, QuadQR.initWasm() resolves the bundled sibling WASM asset from the same package/version location. HTML Copy <script src=\"https://cdn.jsdelivr.net/npm/quadqr-js@1.5.5/dist/quadqr.min.js\"></script> <script> await QuadQR.initWasm(); </script>"},{"title":"Building WASM from source","page":"WebAssembly","url":"wasm.html#building-wasm-from-source","text":"Building WASM from source QuadQR keeps the generated WASM binary and its verification metadata in the repository: TEXT Copy wasm/quadqr-core.wasm wasm/quadqr-core.build.json For normal development, run: BASH Copy npm run build The build verifies SHA-256 fingerprints for the C source, WASM compiler flags, and checked-in binary. If they match, the verified prebuilt binary is reused. If wasm-src/quadqr_core.c or the build flags changed, the normal build automatically recompiles WASM. If recompilation is required but clang is unavailable in PATH , the build fails rather than silently reusing stale WASM. To force only the WASM rebuild: BASH Copy npm run build:wasm A forced rebuild always requires LLVM/Clang. Rust, Cargo, and Emscripten are not required by the current build. npm publish runs the normal build through prepublishOnly , so the same stale-WASM protection applies before publishing."},{"title":"Fallback behavior","page":"WebAssembly","url":"wasm.html#fallback-behavior","text":"Fallback behavior WASM is never required for the QuadQR wire format or scanner correctness. If WebAssembly cannot load, the JavaScript scanner remains available. For performance-sensitive scanning, initialize WASM once during application startup before repeated scanImageData() calls or camera scanning. Previous ← CLI Reference Next Format Reference → QuadQR Experimental RGBW matrix symbology. AGPL-3.0 licensed"}];
